---
phase: 07-scenario-stress-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/scenario.test.mjs
  - js/analytics/index.js
autonomous: true
requirements: [DEPTH-03]
user_setup: []

must_haves:
  truths:
    - "runScenario(TAIWAN_SEMI) impacts EXACTLY the 7 real dependents {NVDA,AAPL,AVGO,000660.KS,AMD,AMAT,KLAC}"
    - "totalMarketCapExposed === 11360589871184 (sum of the 7 real marketcaps, raw USD)"
    - "every impacted firm goes k=5→4 and concentrationAfter(HHI 0.25) >= concentrationBefore(HHI 0.20)"
    - "empty/no-op disruption returns {impactedCompanies:[], totalMarketCapExposed:0} safely"
    - "companyConcentration with excludeSuppliers undefined returns byte-identical legacy output (242 stay green)"
    - "single disableSupplier:'tsmc' impacts only KLAC (label-fragmentation guard)"
  artifacts:
    - path: "js/analytics/index.js"
      provides: "runScenario + SCENARIO_PRESETS exports + excludeSuppliers opt on companyConcentration"
      contains: "export function runScenario"
    - path: "tests/scenario.test.mjs"
      provides: "pure engine unit tests pinned to real fixtures"
      contains: "11360589871184"
    - path: "package.json"
      provides: "scripts.test registers tests/scenario.test.mjs"
      contains: "tests/scenario.test.mjs"
  key_links:
    - from: "runScenario"
      to: "companyConcentration({excludeSuppliers})"
      via: "before/after HHI recompute"
      pattern: "excludeSuppliers"
    - from: "runScenario"
      to: "buildSupplierFanIn"
      via: "fan-in lookup of disabled labels"
      pattern: "fan(In|\\.get)"
---

<objective>
Ship the pure, DOM-free scenario engine (DEPTH-03): `runScenario(disruption, ctx)` + the `TAIWAN_SEMI` preset in `js/analytics/index.js`, plus the minimal additive `excludeSuppliers` opt on `companyConcentration`. This is the first vertical slice — the engine computes the real downstream impact of disabling a supplier set, with the test file pinning the exact ground-truth fixtures (7 companies, $11.36T, HHI 0.20→0.25) computed live from the frozen `data/top100-map.json`.

Purpose: The signature differentiator of the whole site is a what-if disruption that reports honest, derived impact. This plan delivers the compute core that the UI slice (Plan 02) renders.
Output: New `runScenario`/`SCENARIO_PRESETS` exports, one additive `excludeSuppliers` filter line, and `tests/scenario.test.mjs` registered in `package.json` and green.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scenario-stress-tests/07-RESEARCH.md
@.planning/phases/07-scenario-stress-tests/07-VALIDATION.md

@js/analytics/index.js
@tests/criticality-wiring.test.mjs

<interfaces>
<!-- Existing primitives the engine composes. Extracted from js/analytics/index.js — use directly, no exploration. -->

From js/analytics/index.js:
- export function buildSupplierFanIn(profiles = DATA.profiles) -> Map<normalizedSupplierLabel, Set<symbol>>
- export function companyConcentration(symbol, opts) -> { symbol, suppliers:k, hhi:1/k, sharedCount, sharedFrac, score, n } | null
  opts already accepts { wHHI, wShared, sharedThreshold, profiles, fanIn }
- function supplierSet(profile) (module-local) builds the distinct normalized supplier-label Set via normalizeEntityLabel(node.l.split("\n")[0]) over kind==='supplier' nodes
- import { DATA, normalizeEntityLabel } from "../data/index.js"

From data/top100-map.json (test fixtures — pass profiles AND nodes explicitly, never rely on window):
- profiles[symbol].nodes[] carry the supplier nodes; marketcap is NOT on profiles
- top-level nodes[] carry { symbol, marketcap } in raw USD
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Register scenario test file + write the failing pure-engine spec (RED)</name>
  <read_first>package.json (scripts.test line 11), tests/criticality-wiring.test.mjs (fixture-load pattern: JSON.parse(readFileSync("data/top100-map.json")) + pass {profiles,nodes}), 07-RESEARCH.md "Real Impact Data" table + "Phase Requirements → Test Map"</read_first>
  <behavior>
    - Taiwan preset impacts EXACTLY the symbol set {NVDA, AAPL, AVGO, 000660.KS, AMD, AMAT, KLAC} (7), order-independent
    - result.totalMarketCapExposed === 11360589871184 (exact integer sum of the 7 real marketcaps)
    - result.impactedCompanies.length === 7; result.supplierCount === 5 (all 5 labels present in graph)
    - for every impacted firm: suppliersBefore === 5, suppliersAfter === 4, concentrationBefore ≈ 0.2, concentrationAfter ≈ 0.25, and concentrationAfter >= concentrationBefore (HHI monotonic)
    - runScenario({}) and runScenario({disableSuppliers:[]}) both return { impactedCompanies:[], totalMarketCapExposed:0, supplierCount:0 } (no-op safe)
    - runScenario({disableSupplier:'tsmc'}) impacts ONLY KLAC (length 1) — label-fragmentation guard
    - companyConcentration(symbol,{profiles}) deep-equals companyConcentration(symbol,{profiles,excludeSuppliers:undefined}) for a sampled symbol (back-compat)
    - companyConcentration('AAPL',{profiles,excludeSuppliers:['taiwan semiconductor manufacturing company (tsmc)']}).suppliers === companyConcentration('AAPL',{profiles}).suppliers - 1 (exclusion lowers k)
  </behavior>
  <action>Append `tests/scenario.test.mjs` to the `scripts.test` file list in package.json (after `tests/criticality-wiring.test.mjs`, single space-separated token, no trailing edits to other scripts). Create tests/scenario.test.mjs mirroring tests/criticality-wiring.test.mjs's harness: import test from "node:test", assert from "node:assert/strict", load the rich fixture via JSON.parse(readFileSync("data/top100-map.json","utf8")), extract const profiles = data.profiles and const nodes = data.nodes. Import runScenario, SCENARIO_PRESETS, companyConcentration from "../js/analytics/index.js". Write the eight assertions in <behavior>, passing { profiles, nodes } into every runScenario call and { profiles } into every companyConcentration call. Use the exact integer 11360589871184 from 07-RESEARCH (do NOT round). For HHI float comparisons use a small epsilon (Math.abs(x-0.2) < 1e-9). These tests MUST FAIL now because runScenario/SCENARIO_PRESETS/excludeSuppliers do not exist yet — that is the intended RED state. Do NOT inline any implementation in the test beyond computing the expected marketcap sum directly from the 7 known symbols against the fixture nodes (proves the number is derived from real data, not hardcoded in the engine).</action>
  <verify>
    <automated>node --test tests/scenario.test.mjs 2>&1 | grep -q "fail" &amp;&amp; echo "RED as intended"</automated>
  </verify>
  <done>tests/scenario.test.mjs registered in package.json and present; running it FAILS (RED) because the engine exports do not exist yet; the marketcap-sum assertion derives 11360589871184 from the real fixture nodes, not from a literal in the engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement excludeSuppliers opt + runScenario + SCENARIO_PRESETS (GREEN)</name>
  <files>js/analytics/index.js</files>
  <read_first>js/analytics/index.js (companyConcentration lines 46-69: the opts destructure + `const uniq = [...supplierSet(p)]`; buildSupplierFanIn lines 34-43), 07-RESEARCH.md "Engine API", "Concentration-After Mechanics", "The shipped preset"</read_first>
  <behavior>
    - excludeSuppliers (Set | string[] | undefined) added to companyConcentration opts; one filter line removes excluded labels from `uniq` BEFORE k/HHI/sharedFrac; undefined => no change (back-compat)
    - runScenario(disruption,ctx) returns the documented shape with concentrationBefore/After = HHI (before.hhi / after.hhi), severity by share-of-suppliers-lost (>=0.5 high, >=0.25 medium, else low), impacted sorted by marketcap desc then symbol asc
    - SCENARIO_PRESETS.TAIWAN_SEMI.disruption.disableSuppliers = the 5 real normalized TSMC label variants
  </behavior>
  <action>Per 07-RESEARCH Engine API, add the additive `excludeSuppliers` opt to companyConcentration: destructure it from opts (default undefined), coerce to a Set when given (Set passthrough, Array→new Set, else null), and change `const uniq = [...supplierSet(p)]` to filter out excluded labels (`.filter((s) => !exclude || !exclude.has(s))`). This MUST be purely additive — undefined leaves uniq unchanged so all 242 existing concentration tests stay green. Add `export function runScenario(disruption = {}, ctx = {})` exactly as specified in 07-RESEARCH "Engine API": normalize the disabled set from disableSuppliers + disableSupplier via normalizeEntityLabel; build a marketcap lookup `mcap[symbol]` from ctx.nodes (top-level nodes carry marketcap, profiles do NOT — Pitfall 3); compute candidate symbols as the union of fan-in sets for the disabled labels; for each candidate compute before = companyConcentration(symbol,{profiles,fanIn}) and after = companyConcentration(symbol,{profiles,fanIn,excludeSuppliers:disabled}); keep only firms losing >=1 supplier; emit {symbol,company,marketcap,lostSuppliers,suppliersBefore,suppliersAfter,concentrationBefore:before.hhi,concentrationAfter:after.hhi,severity}; sum totalMarketCapExposed; report supplierCount = disabled labels present in fan-in; return disabled array. Report concentration delta as HHI (1/k), NOT the composite score — the composite is non-monotonic under removal (Pitfall 1: AAPL 20→15). Add `export const SCENARIO_PRESETS` with TAIWAN_SEMI bundling the 5 real labels from 07-RESEARCH "The shipped preset" (taiwan semiconductor manufacturing company (tsmc) / tsmc foundry capacity / tsmc (hbm4 and cowos collaboration) / tsmc n2 process technology / tsmc). Add a short module comment stating: single-hop only (cascade deferred), HHI is the monotonic delta metric, "market cap exposed" is exposure not loss. No DOM, no window — keep the module pure.</action>
  <verify>
    <automated>node --test tests/scenario.test.mjs</automated>
  </verify>
  <done>node --test tests/scenario.test.mjs passes all eight assertions (GREEN); runScenario/SCENARIO_PRESETS/excludeSuppliers exported from js/analytics/index.js; module remains DOM-free; the Taiwan preset yields exactly 7 firms / $11.36T / HHI 0.20→0.25.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression gate (242 + new stay green)</name>
  <files>package.json</files>
  <read_first>package.json scripts.test (confirm tests/scenario.test.mjs is the only new entry; tests/scenario-wiring.test.mjs is added in Plan 02), STATE.md baseline (242 pass / 0 fail)</read_first>
  <action>Run the full suite and confirm the additive engine change broke nothing: the 242 prior tests stay green and the new scenario unit tests are now counted. Confirm the excludeSuppliers opt is genuinely back-compatible (no concentration/criticality test regressed). Do NOT add tests/scenario-wiring.test.mjs to package.json here — that file is authored and registered in Plan 02 (registering it now would make the suite RED on a missing file).</action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>`npm test` exits 0; total = 242 prior + new scenario unit tests, 0 fail; no prior concentration/criticality assertion regressed (excludeSuppliers proven additive).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen data → engine | runScenario reads DATA.profiles/nodes read-only; must never mutate the frozen dataset |
| derived number → investor | scenario output is computed, not observed; honesty enforced by Plan 02 provenance + methodology copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | runScenario mutating frozen DATA | mitigate | engine only reads; builds new Sets/objects; tests load fixtures via JSON.parse (fresh copy) and assert no mutation by re-reading |
| T-07-02 | Information disclosure | dishonest headline ("40 refs" / loss not exposure) | mitigate | engine returns derived HHI delta + "exposed" semantics; numbers pinned to real fixtures in tests/scenario.test.mjs |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (Package Legitimacy Audit: N/A — node --test is built in) |
</threat_model>

<verification>
- `node --test tests/scenario.test.mjs` green (engine unit fixtures)
- `npm test` green (242 prior + new, 0 fail)
- grep confirms `export function runScenario` and `export const SCENARIO_PRESETS` in js/analytics/index.js
- engine is DOM-free (no `window`/`document` token added to js/analytics)
</verification>

<success_criteria>
- runScenario(TAIWAN_SEMI) → exactly 7 firms, totalMarketCapExposed === 11360589871184, every firm HHI 0.20→0.25
- excludeSuppliers additive (undefined → unchanged; 242 green)
- single 'tsmc' → only KLAC; no-op → empty safe
- tests/scenario.test.mjs registered + green
</success_criteria>

<output>
Create `.planning/phases/07-scenario-stress-tests/07-01-SUMMARY.md` when done
</output>
