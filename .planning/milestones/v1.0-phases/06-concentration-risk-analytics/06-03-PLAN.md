---
phase: 06-concentration-risk-analytics
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - docs/perf/_render-smoke-0603.cjs
autonomous: false
requirements: [DEPTH-01, DEPTH-02]

must_haves:
  truths:
    - "The full suite (214 prior + new analytics tests) is green via npm test, 0 fail"
    - "Over http-server, opening a company profile shows the concentration score with a Derived (not Observed) badge"
    - "The Critical chokepoints panel renders real fan-in counts and highlighting dims non-chokepoint nodes in the graph"
    - "The Methodology modal shows the concentration + criticality formulas including the equal-weight limit"
  artifacts:
    - path: "docs/perf/_render-smoke-0603.cjs"
      provides: "Playwright integration smoke booting http-server, opening a profile, asserting Derived badge + chokepoints highlight + methodology copy"
      contains: "_render-smoke"
  key_links:
    - from: "docs/perf/_render-smoke-0603.cjs"
      to: "index.html (served over http-server)"
      via: "Playwright page.goto on a real free port"
      pattern: "http-server|createServer|goto"
---

<objective>
Close Phase 6 with an integration gate: the full unit suite green plus a real-browser smoke proving the analytics actually render end-to-end (profile concentration with a Derived badge, chokepoints panel + graph highlight, Methodology copy), then a human-verify checkpoint.

Purpose: Unit tests prove the math and string-wiring; only a served-page smoke proves the analytics paint and the badge/highlight wiring works at runtime (mirrors the Phase 3/4 gate pattern). This is the DEPTH-01/02 acceptance gate before /gsd:verify-work.
Output: docs/perf/_render-smoke-0603.cjs; recorded full-suite-green result; human verification sign-off.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-concentration-risk-analytics/06-VALIDATION.md
@.planning/phases/06-concentration-risk-analytics/06-01-SUMMARY.md
@.planning/phases/06-concentration-risk-analytics/06-02-SUMMARY.md

<interfaces>
<!-- Prior smoke harness to mirror. Use directly. -->

From docs/perf/_render-smoke-0304.cjs (Phase 3 gate, per STATE.md 03-04):
- Pattern: boot http-server on a FREE port at the real repo root, page.goto over http:// (NOT file://), drive a real node hover, assert .prov-badge + literal text, open #methodologyModal via #bMethodology and ESC to close, assert ZERO console/page errors. d3 loads from CDN; if unreachable the harness has a synthetic-dataset fallback (Phase 3 did not need it).

Display targets to assert (from Plan 02):
- Opening a profile populates #cardConcentration with "Supplier concentration: NN/100" and a .prov-badge reading "Derived" (NOT "Observed").
- #chokepointsPanel lists supplier rows with integer counts; #bChokepoints triggers a highlight (non-matching nodes dim to low opacity).
- #methodologyModal body contains the concentration + criticality formula copy + the equal-weight limit text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green + Playwright integration smoke for the analytics</name>
  <read_first>docs/perf/_render-smoke-0304.cjs (Phase 3 smoke harness to mirror — free port, real repo root, http:// not file://, synthetic-dataset fallback); package.json scripts.test (registered files); 06-VALIDATION.md (smoke checklist: concentration in profile, chokepoints highlight, derived badges); STATE.md blocker note (committed snapshot may not paint locally + CDN may be blocked → keep the synthetic-dataset fallback)</read_first>
  <files>docs/perf/_render-smoke-0603.cjs</files>
  <action>Run `npm test` and confirm the full suite is green (214 prior + the new concentration + criticality-wiring tests), 0 fail; if any prior test regressed, fix the regression in the owning module before proceeding (do not weaken assertions). Then author docs/perf/_render-smoke-0603.cjs mirroring _render-smoke-0304.cjs: boot http-server on a free port at the real repo root, page.goto over http://, open a company profile (e.g. GILD), assert #cardConcentration shows "Supplier concentration: NN/100" and a .prov-badge with text "Derived" and NOT "Observed"; assert #chokepointsPanel renders ≥1 supplier row with an integer count; click #bChokepoints and assert non-matching nodes drop to low fill-opacity (highlight active); open #methodologyModal via #bMethodology and assert the body contains the concentration + criticality formula copy incl. the equal-weight limit phrase, then ESC closes; assert ZERO console/page errors. Reuse the Phase-3 synthetic-dataset fallback if the committed snapshot does not paint or the d3 CDN is unreachable (STATE.md blocker). Record the smoke result in the SUMMARY.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 214 prior + new analytics tests, 0 fail; new test file names appear in output.
    - docs/perf/_render-smoke-0603.cjs runs and asserts: Derived (not Observed) badge on the concentration line, chokepoints panel rows, highlight dims non-chokepoints, Methodology formula copy present, zero console errors.
  </acceptance_criteria>
  <done>Full suite green; integration smoke proves the analytics render and wire end-to-end over a served page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 6 analytics rendered end-to-end: per-company "Supplier concentration: NN/100" with a Derived badge + Methodology link, a Critical chokepoints panel with real fan-in counts and a graph highlight, and Methodology copy documenting both formulas and the equal-weight limit. Full unit suite + Playwright smoke green.</what-built>
  <how-to-verify>
    1. Run `npx http-server -p 8099 .` from the repo root, open http://localhost:8099 in a browser.
    2. Open a company profile (search/click e.g. Gilead/GILD). Confirm a line "Supplier concentration: NN/100" with a badge reading "Derived" (NOT "Observed") and a Methodology link.
    3. Open the Critical chokepoints panel; confirm the top supplier is "credit and risk data inputs" with a count of 4 and the list is descending by count.
    4. Click "Highlight in graph" on the chokepoints panel; confirm non-chokepoint nodes dim and the chokepoint suppliers stand out, then reset clears it.
    5. Open Methodology (#bMethodology); confirm it explains the concentration formula (0.6·HHI + 0.4·sharedFrac, HHI=1/k equal-weight) AND the criticality fan-in ranking AND states the equal-weight (constant-volume) limit.
    6. Confirm no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| served page → browser | http-server serves the real index.html + js modules; smoke drives real DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | rendered badge/panel at runtime | mitigate | Smoke asserts the Derived (not Observed) label + escaped panel content render correctly over a served page; zero console errors required |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages; smoke uses existing playwright + http-server devDeps |
</threat_model>

<verification>
- `npm test` exits 0 (214 prior + new analytics tests, 0 fail).
- docs/perf/_render-smoke-0603.cjs asserts the Derived badge, chokepoints highlight, and Methodology copy with zero console errors.
- Human verification sign-off recorded.
</verification>

<success_criteria>
- DEPTH-01 + DEPTH-02 visible and honest end-to-end: concentration + chokepoints render with derived provenance over a served page.
- Full suite green; human checkpoint approved; Phase 6 ready for /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/06-concentration-risk-analytics/06-03-SUMMARY.md` when done.
</output>
