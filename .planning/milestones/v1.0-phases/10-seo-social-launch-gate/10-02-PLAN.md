---
phase: 10-seo-social-launch-gate
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/perf/launch-lighthouse-2026-06-21.md
  - LAUNCH.md
autonomous: false
requirements: [PERF-05]
must_haves:
  truths:
    - "npm test is fully green at the gate (all existing tests + tests/seo-social.test.mjs)"
    - "A Lighthouse run against the served site is attempted and its result recorded in docs/perf/ (honoring the paint caveat — recorded either way, never blocks on the number)"
    - "deploy-pages.yml is confirmed to ship index.html + styles/ + js/ + data/ + assets/(og-card) + favicon + logo; auto-update-data.yml weekly pipeline is confirmed intact"
    - "LAUNCH.md exists with real status across trust/depth/UX/perf/SEO/gate for all 10 phases"
    - "A human confirms the social-card preview and that no GitHub-Pages custom domain is set"
  artifacts:
    - path: "docs/perf/launch-lighthouse-2026-06-21.md"
      provides: "Recorded Lighthouse / Playwright-paint result for launch gate"
      min_lines: 15
    - path: "LAUNCH.md"
      provides: "Launch readiness checklist across all 10 phases"
      min_lines: 30
  key_links:
    - from: "LAUNCH.md"
      to: ".github/workflows/deploy-pages.yml"
      via: "deploy completeness checklist line"
      pattern: "deploy-pages"
    - from: "LAUNCH.md"
      to: ".github/workflows/auto-update-data.yml"
      via: "weekly-pipeline-intact checklist line"
      pattern: "auto-update-data"
---

## Phase Goal

**As a** project owner about to launch, **I want to** run one final gate that proves tests pass, the deploy ships everything, the weekly pipeline is intact, and the social card previews correctly, **so that** I can ship with documented confidence and no hidden regressions.

<objective>
Run the final launch gate (PERF-05): full suite green, a recorded Lighthouse/paint result, confirmed deploy + auto-update workflow integrity, and a LAUNCH.md checklist; then a human-verify checkpoint for the social-card preview and custom-domain confirmation.

Purpose: This is the last gate before the milestone lifecycle. It proves the buildless static deploy and weekly pipeline still work and that the site is shareable, without blocking launch on a Lighthouse number.
Output: `docs/perf/launch-lighthouse-2026-06-21.md`, `LAUNCH.md`, and a passed human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-social-launch-gate/10-RESEARCH.md

<interfaces>
<!-- From 10-RESEARCH.md (VERIFIED). Use for the gate checks. -->

deploy-pages.yml copies (confirm, no edit needed): index.html, favicon.svg, logo.png, data/, assets/ (cp -R assets _site/ — carries og-card.png), styles/, js/, CNAME (if present), .nojekyll.

auto-update-data.yml: weekly Mon 06:00 UTC; runs no-xx-country-codes / profile-link-metadata / supply-chain-research-quality data-validation tests; commits data/. Intact + independent of this phase. KNOWN non-blocking cosmetic bug (do NOT fix here, just note in LAUNCH.md): line 34 echo "timestamp=$(date ...) >> $GITHUB_OUTPUT" is missing a closing quote.

Lighthouse caveat: Phase-1 baseline aborted NO_FCP; Phase-3 now serves real data so a fresh run MAY score. Attempt the run, record whichever outcome occurs, fall back to Playwright Navigation/Paint timing. Do NOT gate launch on the number.

LAUNCH.md section template is in 10-RESEARCH.md "Launch Gate (PERF-05) — Checklist Structure for LAUNCH.md".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Final gate — full suite, Lighthouse record, workflow integrity</name>
  <files>docs/perf/launch-lighthouse-2026-06-21.md</files>
  <read_first>
    - .planning/phases/10-seo-social-launch-gate/10-RESEARCH.md ("Launch Gate" + "Pitfall 4" + "Deploy completeness check")
    - .github/workflows/deploy-pages.yml
    - .github/workflows/auto-update-data.yml
    - docs/perf/baseline-2026-06-20.md (the established NO_FCP caveat + Playwright-timing fallback format)
  </read_first>
  <acceptance_criteria>
    - npm test exits 0 with every file in package.json scripts.test green (includes tests/seo-social.test.mjs).
    - A Lighthouse run is attempted against the locally served site (http-server) and the result — score OR NO_FCP caveat with Playwright Navigation/Paint timing fallback — is written to docs/perf/launch-lighthouse-2026-06-21.md. The gate passes regardless of the numeric score.
    - deploy-pages.yml verified to copy index.html, favicon.svg, logo.png, data/, assets/ (cp -R assets, carrying og-card.png), styles/, js/; auto-update-data.yml verified present with the weekly schedule + three data-validation tests intact.
  </acceptance_criteria>
  <action>Run npm test and confirm fully green; if anything is red, stop and fix before proceeding (do not weaken tests). Serve the site with the installed http-server and run a Lighthouse pass (npx lighthouse against the served URL); if Lighthouse aborts NO_FCP or is unavailable, capture Playwright Navigation/Paint timing as the authoritative fallback (the established Phase-1 pattern). Write docs/perf/launch-lighthouse-2026-06-21.md recording the command, the outcome, and the paint caveat — record either way and do NOT block on the number. Statically confirm deploy-pages.yml ships index.html + favicon.svg + logo.png + data/ + assets/(og-card) + styles/ + js/ and that auto-update-data.yml is intact (weekly Mon 06:00 UTC, three data-validation tests, commits data/). Do NOT edit either workflow. This implements PERF-05 (gate half).</action>
  <verify>
    <automated>npm test 2>&1 | tail -3 && node -e "const fs=require('fs'); const wf=fs.readFileSync('.github/workflows/deploy-pages.yml','utf8'); if(!/cp -R assets _site\//.test(wf)){console.error('deploy missing assets copy');process.exit(1)} fs.accessSync('.github/workflows/auto-update-data.yml'); fs.accessSync('docs/perf/launch-lighthouse-2026-06-21.md'); console.log('gate checks OK')"</automated>
  </verify>
  <done>npm test is green, the Lighthouse/paint result is recorded in docs/perf/, and both workflows are confirmed intact.</done>
</task>

<task type="auto">
  <name>Task B: Write LAUNCH.md launch-readiness checklist</name>
  <files>LAUNCH.md</files>
  <read_first>
    - .planning/phases/10-seo-social-launch-gate/10-RESEARCH.md ("Launch Gate — Checklist Structure for LAUNCH.md")
    - .planning/REQUIREMENTS.md (the 25 v1 requirements / per-phase status for honest checkmarks)
    - docs/perf/launch-lighthouse-2026-06-21.md (the result written in Task A)
  </read_first>
  <acceptance_criteria>
    - LAUNCH.md (repo root) exists with sections Credibility (Trust), Depth, UX, Performance, SEO & Social (PERF-04), Gate (PERF-05), each carrying REAL status drawn from the actual phases 1-9 deliverables and this phase.
    - SEO section confirms meta description/canonical/og:*/twitter:*/JSON-LD present + well-formed, assets/og-card.png exists (1200x630) and is shipped, all absolute URLs use https://akgularda.github.io/supplychain/.
    - Gate section confirms npm test green, Lighthouse recorded (paint caveat referenced), deploy completeness, auto-update-data.yml intact (notes the non-blocking timestamp-echo quoting bug), and a no-custom-domain line pending human confirmation.
  </acceptance_criteria>
  <action>Write LAUNCH.md at repo root using the 10-RESEARCH.md checklist structure. Fill each box with honest, real status: trust (provenance/confidence/methodology/freshness from Phases 2-3), depth (concentration/criticality/scenario from Phases 6-7), UX (design tokens/motion/hero/mobile/keyboard from Phases 4-5-9), perf (memoization/no-restart from Phase 8), SEO & Social (this phase's tags + og-card), gate (this phase). Reference docs/perf/launch-lighthouse-2026-06-21.md for the Lighthouse line. Note the auto-update-data.yml cosmetic timestamp-echo quoting bug as non-blocking. No false claims — only check what is actually true. This completes PERF-05 (doc half).</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('LAUNCH.md','utf8'); for(const s of ['Credibility','Depth','UX','Performance','SEO','Gate','og-card','auto-update-data','akgularda.github.io/supplychain']){ if(!m.includes(s)){console.error('LAUNCH.md missing',s);process.exit(1)} } console.log('LAUNCH.md complete')"</automated>
  </verify>
  <done>LAUNCH.md exists at root with all six sections and real, honest status across all 10 phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task C: Human-verify — social-card preview + no custom domain</name>
  <files>—</files>
  <action>Human confirms the social-card preview renders correctly and that no GitHub-Pages custom domain is configured. See what-built / how-to-verify below.</action>
  <what-built>Full SEO/social metadata + 1200x630 og:image in index.html, a recorded Lighthouse/paint result, and LAUNCH.md. The deploy ships the card automatically via the existing cp -R assets.</what-built>
  <how-to-verify>
    1. Open assets/og-card.png locally and confirm it is a clean 1200x630 card (eyebrow "Monarch Castle Technologies", title "Market Intelligence", honest subtitle) — not a raw screenshot or square logo.
    2. After deploy, paste https://akgularda.github.io/supplychain/ into a card validator (e.g. a Slack/Twitter/opengraph.xyz preview) and confirm a large social card renders with the og-card image and honest title/description.
    3. In GitHub repo Settings -> Pages, confirm NO custom domain is configured (no CNAME file present; the site serves at https://akgularda.github.io/supplychain/). If a custom domain IS set, the absolute URLs in index.html must be revisited.
  </how-to-verify>
  <resume-signal>Type "approved" if the card previews correctly and no custom domain is set, or describe what differs (e.g., custom domain found -> URLs need updating).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> GitHub Pages deploy | CI assembles _site from static files; weekly Action commits data/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-D | Denial of Service | deploy completeness (missing asset at launch) | mitigate | Gate statically asserts deploy-pages.yml copies index/styles/js/data/assets(og-card)/favicon/logo before launch |
| T-10-02-I | Integrity | auto-update-data.yml weekly pipeline | mitigate | Gate confirms workflow intact (schedule + 3 data-validation tests); notes known non-blocking timestamp-echo bug without altering pipeline |
| T-10-02-R | Repudiation | unverified launch claims | mitigate | LAUNCH.md records only true status; Lighthouse result recorded with caveat; human-verify confirms card + domain |
</threat_model>

<verification>
- `npm test` fully green at the gate.
- `docs/perf/launch-lighthouse-2026-06-21.md` exists with a recorded result + paint caveat.
- `LAUNCH.md` exists with all six sections and honest status.
- deploy-pages.yml ships assets/(og-card); auto-update-data.yml intact.
- Human-verify checkpoint passed (card preview + no custom domain).
</verification>

<success_criteria>
- PERF-05 gate satisfied: tests green, Lighthouse recorded (not blocking on number), workflows intact, LAUNCH.md produced.
- Social-card preview and no-custom-domain assumption confirmed by a human.
</success_criteria>

<output>
Create `.planning/phases/10-seo-social-launch-gate/10-02-SUMMARY.md` when done.
</output>
