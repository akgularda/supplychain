---
phase: 04-design-system-smooth-motion
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - docs/perf/_design-motion-smoke-0404.cjs
autonomous: false
requirements: [STORY-01, STORY-03]
must_haves:
  truths:
    - "The full suite (178 existing + design-tokens + viz-motion) is green"
    - "The site paints with real served data under http-server"
    - "Switching global<->profile is smooth (no layout explosion / no flash) and badges keep correct colors"
    - "prefers-reduced-motion is honored end-to-end"
  artifacts:
    - path: "docs/perf/_design-motion-smoke-0404.cjs"
      provides: "http-server + Playwright smoke: paint, mode-switch smoothness, badge colors, reduced-motion"
      min_lines: 40
  key_links:
    - from: "smoke script"
      to: "running app (http-server)"
      via: "Playwright navigates http:// and drives a mode switch"
      pattern: "forceSimulation|node|profile"
---

## Phase Goal

**As an** investor, **I want to** see a visually coherent interface that moves smoothly, **so that** the experience feels world-class and the trust affordances remain correct.

<objective>
Prove the phase is done: full test suite green, the app paints with real data, mode switches are smooth and mental-map-preserving, provenance badge colors are correct under the new tokens, and reduced-motion is honored. Gate with a Playwright smoke and a human visual verification.

Purpose: Node string/structure tests cannot prove runtime motion smoothness or rendered color correctness. The served data now paints (Phase 3), so a real smoke can switch global<->profile and observe behavior; a human confirms the aesthetic and motion.

Output: docs/perf/_design-motion-smoke-0404.cjs (the smoke harness) plus a passing gate. Closes STORY-01 and STORY-03 verification.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-design-system-smooth-motion/04-RESEARCH.md
@.planning/phases/04-design-system-smooth-motion/04-VALIDATION.md

<read_first>
- docs/perf/_render-smoke-0304.cjs (the Phase-3 smoke pattern: boots http-server on a free port at the real repo root, navigates http:// not file://, drives a real node hover, asserts .prov-badge + Confidence:NN%, opens #methodologyModal, checks #lastUpdated, asserts ZERO console/page errors; falls back to a synthetic dataset only if the CDN d3 is unreachable)
- package.json (scripts.test now includes design-tokens + viz-motion; playwright + http-server are the only deps)
- styles/components.css .confidence-high/medium/low (expected rendered colors: observed green #66bb6a, estimated amber #ffb300, unknown neutral #9e9e9e)
- js/viz/index.js render/updateGraph (mode switch entry; openProfile/openGlobal route here)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the design + motion render smoke and run the full gate</name>
  <files>docs/perf/_design-motion-smoke-0404.cjs</files>
  <action>
Create docs/perf/_design-motion-smoke-0404.cjs modeled on docs/perf/_render-smoke-0304.cjs: boot http-server on a free port at the real repo root, navigate Playwright (chromium) to the http:// URL, and assert ZERO console/page errors. Verify the design system applied: read getComputedStyle of document.documentElement and assert the token custom properties resolve (--color-bg, --color-observed, --fs-base, --dur-base non-empty). Verify a node tooltip still shows the .prov-badge and that the badge's computed color matches its confidence class (observed -> green rgb of #66bb6a, estimated -> amber rgb of #ffb300, unknown -> neutral rgb of #9e9e9e) - assert at least one badge resolves to one of those three colors (proves the trust color contract survived restyle). Verify smooth motion: capture the node count and a sample of node x/y, trigger a mode switch (click a company node in global to openProfile, or call the exported handler) and confirm after the switch that the simulation was NOT recreated by asserting nodes shared between views kept approximately their positions (no full explosion) - e.g. at least one shared node id moved less than a large threshold, and the node group was not fully torn down (selection count stays > 0 throughout). Verify reduced-motion: reload with Playwright emulateMedia reducedMotion 'reduce' and confirm the page still paints and switches without error. Keep the Phase-3 synthetic-dataset fallback only if the d3 CDN is unreachable. Print a single PASS/FAIL summary line.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -4 && node docs/perf/_design-motion-smoke-0404.cjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - npm test exit 0 (178 existing + design-tokens + viz-motion all green)
    - Smoke boots http-server, paints with real (or fallback synthetic) data, ZERO console/page errors
    - Token custom properties resolve on :root at runtime; at least one provenance badge resolves to the correct green/amber/neutral color
    - Mode switch does not tear down the node selection or fling shared nodes (mental map preserved); reduced-motion reload paints without error
  </acceptance_criteria>
  <done>Full suite green and the smoke confirms paint + correct badge colors + smooth mode switch + reduced-motion - the automated half of the phase gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification (design coherence, badge colors, smooth motion, reduced-motion)</name>
  <action>Pause for the user to visually verify the restyled, motion-refactored site after the automated smoke passes. Present the how-to-verify steps below and wait for the resume signal before completing the phase.</action>
  <what-built>The full design-token system applied site-wide (base + layout + components + theme consume tokens) and the build-once D3 motion refactor (buildSimulation + updateGraph), with reduced-motion honored. Automated suite green and the Playwright smoke passed.</what-built>
  <how-to-verify>
    1. Start the server: npx http-server -p 8080 (from the repo root), then open http://localhost:8080 in a browser.
    2. Visual coherence: confirm the dark "Bloomberg-terminal" aesthetic is intact and consistent (type rhythm, spacing, accent, depth) with no obvious broken styling, no unstyled flashes.
    3. Trust affordances: hover a node; confirm the provenance badge shows the correct color - observed = green, estimated = amber, unknown = neutral - and that Confidence:NN%, the source link, the methodology modal (#bMethodology), and the footer freshness still appear and are correct.
    4. Smooth motion: switch from the global map into a company profile and back (click a company, then Back/global). Confirm the transition is smooth - nodes that exist in both views do NOT jump/explode to new positions; there is no full reset flash.
    5. Reduced motion: enable your OS "reduce motion" setting (or DevTools Rendering > Emulate prefers-reduced-motion: reduce), reload, and confirm switches snap without animation and nothing breaks.
  </how-to-verify>
  <verify>
    <human-check>User confirms design coherence, correct badge colors, smooth mental-map-preserving motion, and reduced-motion honored.</human-check>
  </verify>
  <resume-signal>Type "approved" if the design is coherent, badges are correct, motion is smooth, and reduced-motion is honored; otherwise describe the issue.</resume-signal>
  <done>User has visually confirmed the design system and smooth motion; phase ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke harness to running app | Playwright drives the real served site over http:// |
| rendered colors to trust semantics | Badge color must still map observed/estimated/unknown to green/amber/neutral |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | rendered trust badge colors after restyle | mitigate | Smoke asserts a badge resolves to the exact green/amber/neutral rgb; human verifies semantics |
| T-04-10 | Denial of Service (UX) | regression to jank on view change | mitigate | Smoke asserts shared-node position stability across a switch; human verifies smoothness |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- npm test exit 0 (full suite + new files)
- docs/perf/_design-motion-smoke-0404.cjs prints PASS: paint, token resolution, badge color, mode-switch stability, reduced-motion
- Human checkpoint approved (coherent design, correct badges, smooth motion, reduced-motion honored)
</verification>

<success_criteria>
- STORY-01 verified: consistent design system applied site-wide, trust affordances correct under it
- STORY-03 verified: smooth, mental-map-preserving D3 motion with no full-simulation restart; reduced-motion honored
- Full suite green; smoke + human visual gate passed
</success_criteria>

<output>
Create `.planning/phases/04-design-system-smooth-motion/04-04-SUMMARY.md` when done
</output>
