---
phase: 08-interaction-performance
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified: []
autonomous: false
requirements: [PERF-01]
must_haves:
  truths:
    - "The full test suite (257 prior + analytics-memo + no-restart-invariant) is green with all anchors unchanged"
    - "Both new phase test files actually run under the gate command (registered in package.json scripts.test)"
    - "The latency record exists under docs/perf/ and the memoized interaction feels immediate to a human"
  artifacts: []
  key_links:
    - from: "package.json scripts.test"
      to: "tests/analytics-memo.test.mjs + tests/no-restart-invariant.test.mjs"
      via: "both files in the npm test command (registered Plan 01 Task 0)"
      pattern: "analytics-memo.test.mjs.*no-restart-invariant.test.mjs|no-restart-invariant.test.mjs"
---

<objective>
Phase-8 integration gate. Prove the whole suite is green with the memoization + no-restart guard in
place and every real anchor unchanged, confirm both new test files actually execute under the gate
command, confirm the latency record is committed, and run a human-verify checkpoint that filtering and
repeated profile/chokepoint interactions feel immediate.

Purpose: PERF-01 SC1+SC2+SC3 — memoized interactions, no full restart, suite green after the perf refactor.
Output: a verified-green phase + a human sign-off on perceptual immediacy (no source edits).
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-interaction-performance/08-RESEARCH.md
@.planning/phases/08-interaction-performance/08-VALIDATION.md

<read_first>
- package.json — scripts.test must list BOTH tests/analytics-memo.test.mjs and tests/no-restart-invariant.test.mjs (24 entries total) after Plan 01 Task 0.
- 08-VALIDATION.md "Validation Sign-Off" — the four checkboxes this gate closes.
- docs/perf/interaction-2026-06-21.md — the recorded SC2 latency number (Plan 03 output).
- The anchor tests already in the suite: tests/concentration.test.mjs (GILD=36, NVDA=12), tests/criticality-wiring.test.mjs (fan-in top "credit and risk data inputs"=4), tests/scenario.test.mjs (Taiwan 7 / $11,360,589,871,184).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green gate + registration + anchors-unchanged proof</name>
  <files>(none — verification only)</files>
  <read_first>package.json scripts.test (must contain both new files); the anchor test files listed above.</read_first>
  <action>Run `npm test` and confirm exit 0 with the suite green (257 prior + the two new phase files; target ~270+). Confirm BOTH tests/analytics-memo.test.mjs and tests/no-restart-invariant.test.mjs appear in the package.json scripts.test command (an unregistered file silently never runs — this is the documented gate landmine). Confirm the anchor assertions are still green inside the run: GILD=36, NVDA=12, top chokepoint fan-in "credit and risk data inputs"=4, Taiwan 7 firms / totalMarketCapExposed===11360589871184 — caching changed cost only, not value. Confirm docs/perf/interaction-2026-06-21.md exists (Plan 03 latency record). Make NO source edits in this plan; if the suite is red, STOP and report which assertion failed (do not patch here — that routes back to the owning plan).</action>
  <acceptance_criteria>
    - `npm test` exit 0; full suite green including the two new phase files.
    - package.json scripts.test contains both new test filenames (registration proven).
    - Anchors unchanged: GILD=36, NVDA=12, fan-in top=4, Taiwan 7/$11360589871184 (assertions green in the run).
    - docs/perf/interaction-2026-06-21.md present.
  </acceptance_criteria>
  <verify>
    <automated>npm test && node -e "const t=require('./package.json').scripts.test;if(!t.includes('analytics-memo.test.mjs')||!t.includes('no-restart-invariant.test.mjs'))process.exit(1);require('fs').accessSync('docs/perf/interaction-2026-06-21.md');console.log('GATE OK')"</automated>
  </verify>
  <done>Suite green, both new files registered + executed, anchors intact, latency record present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Per-session memoization of the analytics hotspots (fan-in built once; companyConcentration/supplierCriticality/runScenario cached) plus a source-level guard proving simple filter/style/highlight changes never restart the simulation. Values are unchanged; only interaction cost dropped.</what-built>
  <how-to-verify>
    1. Start the local server: `npx http-server -p 8080 -c-1` from the repo root.
    2. Open http://localhost:8080/index.html. (If the committed snapshot does not paint locally — the documented NO_FCP condition — verify against the live GitHub-Pages deploy instead, or skip the visual step and rely on the recorded micro-bench in docs/perf/interaction-2026-06-21.md.)
    3. Apply a filter (bFilter → change a dropdown → Apply): the graph should dim/highlight by OPACITY only, with no full layout reshuffle / nodes flying apart (no simulation restart).
    4. Open several company profiles in a row, then the chokepoints highlight and the Taiwan scenario: repeated interactions should feel immediate (memoized analytics), with no growing lag.
    5. Confirm the concentration/chokepoint/scenario NUMBERS are identical to before (e.g. a known company's concentration score, Taiwan = 7 companies / $11.36T).
    6. Review docs/perf/interaction-2026-06-21.md — confirm the recorded warm-vs-cold speedup reads as an honest improvement.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any restart-on-filter jank, lag on repeated interaction, or changed number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate command → actual run | An unregistered new test file would make the gate falsely "green" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Repudiation | new test file present but not in scripts.test → never runs | mitigate | Gate explicitly asserts both filenames are in package.json scripts.test AND that `npm test` exits 0 (the registration-landmine check from prior phases). |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no packages installed; verification-only gate over Node built-in test runner + already-installed http-server. |
</threat_model>

<verification>
- `npm test` exit 0; both phase test files registered + executed; anchors GILD=36/NVDA=12/fan-in 4/Taiwan 7/$11360589871184 green.
- docs/perf/interaction-2026-06-21.md present with the recorded SC2 improvement.
- Human confirms opacity-only filtering (no restart jank) + immediate repeated interactions + unchanged numbers.
</verification>

<success_criteria>
- Full suite green after the perf refactor (SC3); no full simulation restart on simple changes (SC1, guard-proven); latency improvement recorded (SC2).
- Human sign-off on perceptual immediacy.
</success_criteria>

<output>
Create `.planning/phases/08-interaction-performance/08-04-SUMMARY.md` when done.
</output>
