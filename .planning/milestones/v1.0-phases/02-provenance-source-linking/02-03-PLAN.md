---
phase: 02-provenance-source-linking
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - js/ui/index.js
  - js/data/index.js
autonomous: true
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "The provenance source drawer (openProvenance) shows real human source titles, reading source.title (the src.t->title bug is fixed)"
    - "The company-card anchor figure carries a provenance badge derived via provenanceFor"
    - "The compare grid 'Verified Entities' count is derived through the centralized trust derivation, not a duplicated .includes('source') check"
    - "Unsourced/dangling-FK figures in the card/compare/drawer show the explicit unknown state, never a fabricated source"
    - "parseYearsFromSources reads source.title (fixing the latent src.t bug) so the timeline matches title years"
  artifacts:
    - path: "js/ui/index.js"
      provides: "ui render paths instrumented with provenanceFor; openProvenance reads title"
      contains: "provenanceFor"
    - path: "js/data/index.js"
      provides: "parseYearsFromSources reads source.title"
      contains: "source.title"
  key_links:
    - from: "js/ui/index.js"
      to: "js/trust/index.js"
      via: "import { provenanceFor, renderProvenanceBadge }"
      pattern: "from \\\"\\.\\./trust/index\\.js\\\""
    - from: "js/ui/index.js openProvenance"
      to: "profile.sources[].title"
      via: "src.title (was src.t bug)"
      pattern: "src\\.title"
---

<objective>
Wire the trust API (plan 02-01) into the ui-owned render paths — the company card, the compare grid,
and the provenance source drawer — and FIX the latent `src.t`->`title` source-key bug in
`openProvenance` (js/ui) and `parseYearsFromSources` (js/data). The real source key is `title`
(0/407 sources have `t`), so the drawer currently shows source IDs and the timeline misses title years.

Purpose: ui-side slice of TRUST-01/TRUST-02 — profile figures get data-derived badges and the source
drawer/timeline read the correct field so links resolve to real human source titles.
Output: updated `js/ui/index.js` and the one-line `parseYearsFromSources` fix in `js/data/index.js`.
This plan owns these two files exclusively (no overlap with plan 02's js/viz/index.js → runs parallel).

User story: As an investor opening a company profile, I want its figures badged and the sources drawer
to list real, named, reachable sources.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provenance-source-linking/02-RESEARCH.md
@.planning/phases/02-provenance-source-linking/02-01-PLAN.md

<interfaces>
<!-- The trust API delivered by plan 02-01. Import and call. -->
From js/trust/index.js:
  provenanceFor(input, ctx = {}) -> { tag:'observed'|'estimated'|'unknown', source?:{label,url} }
  renderProvenanceBadge(el, prov) -> void
  badgeHtml(prov) -> string

ctx to pass in ui: { sourceIndex: <built from the active profile's sources>, meta: DATA.meta }
  - ui does not hold STATE.sourceIndex pre-built per arbitrary profile; build it inline where needed:
    Object.fromEntries((profile.sources||[]).map(s=>[s.id,s]))  (mirrors data/index.js:153)

Existing ui render paths (verified line refs; locate by function name):
  - openProvenance()      js/ui/index.js L123-135 : maps profile.sources; BUG reads `src.t` (L129) — must read src.title (id fallback)
  - updateCompanyCard()   L258-325 : supplier/service/channel counts L293-295; company anchor node has confidence/sourceId
  - renderCardInsights()  L405-458 : Source-backed ratio L430; timeline L448 (parseYearsFromSources)
  - showCompare()         L535-562 : 'Verified Entities' count L547 (p.nodes.filter(n=>n.confidence&&n.confidence.includes('source')))
  - applyFilters()        L474-496 : duplicated confidence-string parsing (optional centralization)

Existing data path:
  - parseYearsFromSources(profile)  js/data/index.js L66-75 : BUG reads `source.t` (L69) — must read source.title
</interfaces>

<test_caution>
parseYearsFromSources is consumed by the card timeline. RESEARCH Open-Question 2: fix it here (it is
directly a provenance display path) but FIRST run `npm test` to confirm no existing test pins the buggy
`src.t` behavior. The 116 read the rich JSON where the real key is `title`, so reading `title` is the
correct behavior — but verify the suite stays green after the change.
</test_caution>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the src.t->title bug in openProvenance and parseYearsFromSources</name>
  <files>js/ui/index.js, js/data/index.js</files>
  <read_first>
    - js/ui/index.js openProvenance L123-135 (reads src.t at L129)
    - js/data/index.js parseYearsFromSources L66-75 (reads source.t at L69)
    - .planning/phases/02-provenance-source-linking/02-RESEARCH.md "Pitfall 2: The t vs title source-key bug" + "Open Questions" Q2
    - tests/profile-link-metadata.test.mjs and tests/supply-chain-research-quality.test.mjs (confirm none assert the buggy src.t fallback before changing)
  </read_first>
  <action>
    In js/ui/index.js openProvenance, change the title resolution at L129 from `escapeHtml(src.t || src.id || "Source")` to `escapeHtml(src.title || src.id || "Source")` so the drawer shows the real human title (RESEARCH verified all 407 sources have `title`, 0 have `t`). In js/data/index.js parseYearsFromSources, change L69 from ``${source.id || ""} ${source.t || ""} ${source.url || ""}`` to use `source.title` in place of `source.t`. Make ONLY these two key-name substitutions — do not restructure either function. These are the explicit src.t bug-fix tasks called out in the quality gate.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "src.title" js/ui/index.js` and `grep -q "source.title" js/data/index.js`
    - `grep -c "src\\.t " js/ui/index.js` is 0 (no lingering bare src.t read) and parseYearsFromSources no longer references `source.t`
    - `npm test` exits 0 — confirms no existing test pinned the buggy behavior (Open-Question 2 resolved green)
  </acceptance_criteria>
  <done>The drawer reads real source titles and the timeline matches title years; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire provenance badges into the company card, compare grid, and source drawer</name>
  <files>js/ui/index.js</files>
  <read_first>
    - js/ui/index.js imports L5-14; updateCompanyCard L258-325; renderCardInsights L405-458; showCompare L535-562; openProvenance L123-135
    - .planning/phases/02-provenance-source-linking/02-RESEARCH.md "Major Figure Render Paths" (ui table) + "Open Questions" Q1 + Pitfalls 1 & 3
    - js/trust/index.js (API from plan 02-01)
  </read_first>
  <behavior>
    - The company-card anchor figure shows a provenance badge derived from the company node's confidence/sourceId (observed/estimated/unknown)
    - The compare-grid 'Verified Entities' count is computed via provenanceFor over each profile's nodes (observed+sourced), replacing the inline .includes('source')
    - A profile whose anchor lacks confidence/source renders the explicit unknown badge (no fabricated label)
    - openProvenance keeps listing every profile.sources row with its real title + reachable link
  </behavior>
  <action>
    Add `import { provenanceFor, renderProvenanceBadge, badgeHtml } from "../trust/index.js";` to the ui import block (L11-14 area). In updateCompanyCard, build `const sourceIndex = Object.fromEntries((profile?.sources||[]).map(s=>[s.id,s]));` and locate the company anchor node (`profile?.nodes?.find(n=>n.kind==='company')` or the node whose id matches the symbol); compute `const prov = provenanceFor(anchor || {}, { sourceIndex, meta: DATA.meta });` and render its badge next to the company name/market-cap figure via renderProvenanceBadge into a located/created element in the card (do NOT edit index.html — create the span in JS and append to the existing card name element's parent). Per RESEARCH Q1, badge the money/entity anchor figure, not the raw supplier/service/channel counts. In showCompare, replace the inline `p.nodes.filter(n => n.confidence && n.confidence.includes('source')).length` (L547) with a count using provenanceFor over `p.nodes` with that profile's inline sourceIndex (verified = tag 'observed' AND a resolving source). Leave openProvenance's list structure intact (its title fix landed in Task 1). Guard everything for the thin-JS runtime (missing profiles -> provenanceFor returns unknown, no crash; Pitfall 1).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "provenanceFor" js/ui/index.js` >= 2 (card anchor + compare grid)
    - `grep -q "from \"../trust/index.js\"" js/ui/index.js`
    - showCompare 'Verified Entities' no longer uses a standalone `.includes('source')` filter — it goes through provenanceFor
    - `node --check js/ui/index.js` passes; `npm test` exits 0
    - An unsourced anchor renders the unknown badge (no fabricated source) — verify by reading the card path logic
  </acceptance_criteria>
  <done>Card anchor + compare grid show data-derived badges; verified counts are trust-derived; unsourced figures show the honest unknown state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.sources -> drawer/card innerHTML | title/url interpolated into innerHTML (escaping via existing escapeHtml + trust badgeHtml) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | card/compare badge innerHTML injection | mitigate | badge markup comes from trust badgeHtml (escapes title, gates url on http); openProvenance keeps escapeHtml on title+url |
| T-02-07 | Information Disclosure | drawer new-tab links reverse-tabnabbing | mitigate | openProvenance already emits rel="noopener noreferrer" (preserve) |
| T-02-08 | Crash | dangling/absent FK in card/compare derivation | mitigate | provenanceFor null-checks FK; degrade to unknown badge |
| T-02-SC | Tampering | npm installs | accept | No packages this phase (buildless) |
</threat_model>

<verification>
- `npm test` exits 0 (116 + provenance suite).
- openProvenance reads src.title; parseYearsFromSources reads source.title.
- ui imports + calls provenanceFor at the card anchor + compare grid.
</verification>

<success_criteria>
- Profile-side major figures show data-derived provenance badges; the source drawer lists real titles with reachable links; the src.t bug is gone; unsourced figures show the honest unknown state.
</success_criteria>

<output>
Create `.planning/phases/02-provenance-source-linking/02-03-SUMMARY.md` when done.
</output>
