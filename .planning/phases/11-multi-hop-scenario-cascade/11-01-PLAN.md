---
phase: 11-multi-hop-scenario-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - js/analytics/index.js
  - tests/scenario-cascade.test.mjs
autonomous: true
requirements: [CASC-01, CASC-04]
must_haves:
  truths:
    - "Disabling TSMC at maxHops:1 (or default) impacts exactly 7 firms / $11.36T (11360589871184), byte-identical to v1.0"
    - "Disabling TSMC at maxHops>=2 impacts 8 firms / $13.28T with byHop {1:7, 2:1} (TSM added at hop 2 via AMAT)"
    - "A synthetic cyclic dependency fixture terminates (visited-set guard), visiting each node once"
    - "multi-hop impacted set is always a superset of the single-hop set on real data (8 superset of 7)"
    - "selfLabels derives exactly the 6 real bridges (TSM, TCEHY, ASML, AZN, AMAT, LIN)"
    - "npm test runs tests/scenario-cascade.test.mjs (registered) and the full suite stays green"
  artifacts:
    - path: "js/analytics/index.js"
      provides: "runScenario maxHops BFS + buildSelfLabels bridge map + byHop/maxHopReached/hop output"
      contains: "maxHops"
    - path: "tests/scenario-cascade.test.mjs"
      provides: "Cascade correctness unit tests (cycle, hop-count, superset, bridges, memo, backward-compat)"
      contains: "scenario-cascade"
    - path: "package.json"
      provides: "scripts.test registers tests/scenario-cascade.test.mjs"
      contains: "tests/scenario-cascade.test.mjs"
  key_links:
    - from: "runScenario"
      to: "_runScenarioCompute"
      via: "maxHops BFS over fanIn + selfLabels"
      pattern: "selfLabels"
    - from: "scenarioKey memo"
      to: "maxHops"
      via: "|h${maxHops} suffix"
      pattern: "h\\$\\{maxHops\\}"
---

<objective>
Extend the pure `runScenario` engine in `js/analytics/index.js` with bounded, cycle-safe multi-hop
cascade propagation over the REAL frozen graph, and register + author the cascade correctness unit
tests. The engine default is `maxHops:1` so every existing call site and the v1.0 fixture/memo tests
reproduce byte-identically; multi-hop is a true superset enabled by exactly 6 real company-as-supplier
bridge edges (TSM, TCEHY, ASML, AZN, AMAT, LIN). No edges are fabricated.

Purpose: CASC-01 (multi-hop, bounded, cycle-safe over the real graph) and CASC-04 (registered unit
tests assert cascade correctness; full 301-suite stays green).
Output: Extended `runScenario` + new `buildSelfLabels`, new `tests/scenario-cascade.test.mjs`,
`package.json scripts.test` updated.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-hop-scenario-cascade/11-RESEARCH.md
@.planning/phases/11-multi-hop-scenario-cascade/11-VALIDATION.md

@js/analytics/index.js
@js/data/index.js
@tests/scenario.test.mjs
@tests/analytics-memo.test.mjs

<interfaces>
<!-- Key contracts the executor needs. Extracted from js/analytics/index.js — use directly, no exploration. -->

From js/analytics/index.js (current):
- `export function runScenario(disruption = {}, ctx = {})` — disruption: { disableSuppliers?: string[], disableSupplier?: string }; ctx: { profiles, nodes, fanIn }
- `function _runScenarioCompute(disabled, profiles, nodes, fan)` — current single-hop compute
- `export function buildSupplierFanIn(profiles)` — memoized Map<normalizedSupplierLabel, Set<symbol>>
- `function supplierSet(profile)` — Set<normalizedLabel> for a profile's `kind === "supplier"` nodes
- `export function companyConcentration(symbol, { profiles, fanIn, excludeSuppliers })` — memoized; excludeSuppliers is Set|string[]
- `function _memo(name, key, compute)`, `function _profilesTag(profiles)`, `export function __resetAnalyticsCache()`, `export function __memoStats()`
- Memo bucket `_stats.scenario` exists; current scenarioKey = `${[...disabled].sort().join(",")}|${_profilesTag(profiles)}`

From js/data/index.js:
- `export function normalizeEntityLabel(label)` — the SAME normalization that builds fanIn keys (lowercases/trims first line). Use it for company name, symbol, and paren-acronym when deriving bridges.

Real fixture anchors (VERIFIED in 11-RESEARCH.md against data/top100-map.json):
- TAIWAN_SEMI maxHops:1 => 7 firms, totalMarketCapExposed === 11360589871184, byHop {1:7}, maxHopReached 1
- TAIWAN_SEMI maxHops>=2 => 8 firms ($13.28T), byHop {1:7, 2:1}, maxHopReached 2; hop-2 set = [TSM]
- hop-1 set = [000660.KS, AAPL, AMAT, AMD, AVGO, KLAC, NVDA]; the 6 bridges = TSM, TCEHY, ASML, AZN, AMAT, LIN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Register cascade test file + author the failing cascade test suite</name>
  <read_first>
    - package.json (scripts.test — the explicit `node --test ...` file list)
    - tests/scenario.test.mjs (v1.0 anchor assertions: 7 firms / 11360589871184; field-level asserts only, no full-array deepEqual on impactedCompanies)
    - tests/analytics-memo.test.mjs (memo: two maxHops-less calls => builds===1; the 7/11360589871184 anchor)
    - 11-RESEARCH.md §Validation Architecture (the Phase Requirements -> Test Map rows) and §CRITICAL FINDING (the 6 bridges, Taiwan byHop table)
  </read_first>
  <action>
    Edit package.json: append `tests/scenario-cascade.test.mjs` to the `scripts.test` `node --test ...` file
    list (add it after `tests/scenario-wiring.test.mjs` is fine; order is irrelevant). Do not remove or
    reorder existing entries.

    Create tests/scenario-cascade.test.mjs (Node `node:test` + `node:assert/strict`, ESM `.mjs`). Import
    `runScenario`, `buildSupplierFanIn`, `__resetAnalyticsCache`, `__memoStats` from `../js/analytics/index.js`,
    `SCENARIO_PRESETS` from the same, and `DATA` from `../js/data/index.js`. Call `__resetAnalyticsCache()` in a
    beforeEach (mirror tests/analytics-memo.test.mjs). Author these tests (they MUST fail now — runScenario does
    not yet accept maxHops — that is the RED state):
    1. Backward-compat default: `runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption, {profiles:DATA.profiles, nodes:DATA.nodes})`
       (NO maxHops) => impactedCompanies.length === 7 AND totalMarketCapExposed === 11360589871184 AND maxHopReached === 1.
    2. Explicit maxHops:1 equals the default result (same length 7, same total 11360589871184, byHop has only key 1 with 7 entries).
    3. Multi-hop maxHops:3 (and separately maxHops:2) on TAIWAN_SEMI => impactedCompanies.length === 8,
       byHop[1].length === 7, byHop[2].length === 1, byHop[2] includes "TSM", maxHopReached === 2,
       totalMarketCapExposed > 11360589871184 (the $13.28T superset). Assert each impacted entry has a numeric `hop`.
    4. Superset: the set of symbols at maxHops:3 is a superset of the set at maxHops:1 (every hop-1 symbol present; TSM added).
    5. selfLabels bridges: build via the engine's exported `buildSelfLabels(DATA.profiles, buildSupplierFanIn(DATA.profiles))`
       and assert the symbols whose selfLabels are non-empty equal exactly {TSM, TCEHY, ASML, AZN, AMAT, LIN}.
    6. Cycle termination: construct a tiny synthetic `{profiles, nodes}` fixture inline where company A lists B as a
       supplier and B lists A as a supplier (a real 2-cycle), pass it as ctx, disable A's label, run at maxHops:5, and
       assert the call RETURNS (terminates) and each impacted company appears once (no duplicate symbol in impactedCompanies).
    7. maxHops bound: on the synthetic fixture (or a small acyclic chain), maxHops:1 stops at hop 1 (maxHopReached===1)
       while a deeper maxHops reaches further — proving the bound is respected.
    8. Memo key includes maxHops: after reset, call TAIWAN_SEMI at maxHops:1 then at maxHops:3; assert results differ
       (7 vs 8) AND `__memoStats().scenario.builds === 2` (distinct cache entries). Then a second maxHops:1 call is a hit.
    NO fenced code blocks beyond the test file itself; this task's deliverable IS the test file + package.json edit.
  </action>
  <acceptance_criteria>
    - `grep -F 'tests/scenario-cascade.test.mjs' package.json` matches (file registered in scripts.test).
    - `node --test tests/scenario-cascade.test.mjs` RUNS and FAILS (RED) because runScenario does not yet honor maxHops/byHop — confirming the tests exercise the new behavior.
    - The test file references: maxHops 1/2/3/5, byHop, maxHopReached, the literal 11360589871184, the 6 bridge symbols, an inline synthetic cyclic fixture, and __memoStats().scenario.builds.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/scenario-cascade.test.mjs; test $? -ne 0 &amp;&amp; grep -Fq 'tests/scenario-cascade.test.mjs' package.json &amp;&amp; echo "RED-as-expected + registered"</automated>
  </verify>
  <done>tests/scenario-cascade.test.mjs exists with the 8 cascade assertions and is registered in package.json scripts.test; it currently fails (RED) pending the engine change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend runScenario with maxHops BFS, buildSelfLabels bridges, hop/byHop/maxHopReached + memo key</name>
  <read_first>
    - js/analytics/index.js (runScenario lines 250-271, _runScenarioCompute 273-313, buildSupplierFanIn, companyConcentration excludeSuppliers path, _memo, _profilesTag, supplierSet)
    - 11-RESEARCH.md §Engine Design (API, output shape, memo key), Pattern 1 (_cascade BFS), Pattern 2 (_buildSelfLabels), §Anti-Patterns (re-disable guard, lostSuppliers semantics A2)
  </read_first>
  <behavior>
    - Default / maxHops:1: impacted set, totalMarketCapExposed (11360589871184), and every existing impactedCompanies field are byte-identical to v1.0; each entry additionally carries `hop:1`; byHop is {1:[...7]}; maxHopReached:1.
    - maxHops>=2 on TAIWAN_SEMI: TSM appears at hop 2 (TSMC disabled -> AMAT impacted at hop1 -> AMAT is a supplier-label TSM depends on -> TSM hop2); 8 firms total; byHop {1:7,2:1}.
    - Cyclic fixture: traversal terminates via the visited (hopOf) set; each company visited once.
    - Memo: maxHops:1 and maxHops:3 are distinct cache entries; two default(=1) calls collide (builds counted once).
  </behavior>
  <action>
    Add an exported `buildSelfLabels(profiles, fanIn)` returning a memoized `Map&lt;symbol, label[]&gt;` of the
    company's own identity labels (normalizeEntityLabel of `profiles[sym].company`, of `sym`, and of any
    parenthetical acronym matched by `/\(([^)]+)\)/` in the company name) that appear as keys in `fanIn`. Memoize it
    in a new `_memo("fanIn"-sibling)` bucket keyed by the profiles tag (or reuse a dedicated `selfLabels` bucket) so
    it builds once per session like buildSupplierFanIn. On the real data this yields exactly the 6 bridges.

    Extend `runScenario(disruption, ctx)`: compute `const maxHops = Math.max(1, Number(disruption.maxHops ?? ctx.maxHops ?? 1));`
    (disruption.maxHops wins over ctx.maxHops). Derive `selfLabels = ctx.selfLabels || buildSelfLabels(profiles, fan)`.
    Change scenarioKey to append `|h${maxHops}`: `${[...disabled].sort().join(",")}|${_profilesTag(profiles)}|h${maxHops}`.
    Pass selfLabels and maxHops into `_runScenarioCompute(disabled, profiles, nodes, fan, selfLabels, maxHops)`.

    Rewrite `_runScenarioCompute` as a bounded cycle-safe BFS (Pattern 1): maintain `hopOf = Map&lt;symbol, hop&gt;` (the
    visited set / cycle guard) and `byHop = {}`. frontier starts as the `disabled` label set. For hop = 1..maxHops:
    impactedThisHop = union of `fan.get(label)` over frontier labels, EXCLUDING symbols already in hopOf; record
    hopOf[sym]=hop and push to byHop[hop]; set maxHopReached=hop. If impactedThisHop empty -> break. Build the next
    frontier = union of `selfLabels.get(sym)` over impactedThisHop, MINUS any label already in `disabled` (the
    re-disable guard); if next empty -> break; else frontier = next. After the BFS, for every symbol in hopOf build the
    enriched impactedCompanies entry exactly as v1.0 (companyConcentration before / after-with-excludeSuppliers, lostSuppliers,
    suppliersBefore/After, concentrationBefore/After HHI, severity) PLUS `hop: hopOf.get(symbol)`. For hop-1 companies,
    lostSuppliers stays the `disabled`-set labels that reached them (v1.0 semantics, unchanged). For hop>=2 companies,
    lostSuppliers = the frontier labels (selfLabels of the impacting hop-(n-1) company) that reached them — i.e. the wave
    labels, NOT the original disabled set (research A2; e.g. TSM lost "applied materials"). totalMarketCapExposed sums ALL
    hops. supplierCount and disabled stay as v1.0. Return `{impactedCompanies, byHop, maxHopReached, totalMarketCapExposed,
    supplierCount, disabled}`. Keep the existing impactedCompanies sort (marketcap desc, then symbol). Update the v1.0
    "SINGLE-HOP ONLY ... deferred" doc comment to describe the bounded multi-hop model. NO fenced code in this action.
  </action>
  <acceptance_criteria>
    - `node --test tests/scenario-cascade.test.mjs` exits 0 (all 8 cascade assertions GREEN), including: maxHops:1 => 7/11360589871184; maxHops:2 and :3 => 8 firms, byHop {1:7,2:1}, TSM at hop2, $13.28T; superset; the 6 bridges; cyclic fixture terminates; maxHops bound; memo builds===2.
    - `node --test tests/scenario.test.mjs tests/analytics-memo.test.mjs` exits 0 (v1.0 anchors + memo test unchanged: two default calls => builds===1).
    - `grep -F 'h${maxHops}' js/analytics/index.js` matches (memo key includes maxHops).
    - `grep -F 'buildSelfLabels' js/analytics/index.js` matches (bridge map exported).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/scenario-cascade.test.mjs tests/scenario.test.mjs tests/analytics-memo.test.mjs</automated>
  </verify>
  <done>runScenario honors maxHops (default 1), derives the 6-bridge selfLabels map, performs a bounded cycle-safe BFS emitting hop/byHop/maxHopReached, keys the memo on maxHops; all cascade + v1.0 + memo tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression gate (301 baseline + cascade tests green)</name>
  <read_first>
    - package.json (scripts.test — confirm tests/scenario-cascade.test.mjs present)
    - 11-VALIDATION.md §Sampling Rate (per-task: npm test; phase gate: full suite green)
  </read_first>
  <action>
    Run the full registered suite via `npm test`. Confirm exit 0 with zero failing assertions: the new
    cascade file runs, the v1.0 scenario + memo anchors stay green, and the previously-green 301 baseline is
    not regressed by the added `hop` field or the memo-key change. If any prior test fails, diagnose against
    11-RESEARCH.md §Backward-Compat / §Output shape (no full-array deepEqual on impactedCompanies exists, so
    adding `hop` is safe) and fix the engine — do NOT edit the v1.0 anchor tests to accommodate a wrong result.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0; output shows tests/scenario-cascade.test.mjs ran; no failing assertions across the full file list.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test is green (301 baseline + new cascade tests); the registered suite passes end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen data -> engine | `data/top100-map.json` is read-only input to a pure function; no external/user input crosses into the engine in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | runScenario BFS over a potentially cyclic graph | mitigate | visited-set (`hopOf`) guarantees termination + `maxHops` bound; proven by the synthetic cyclic fixture test (Task 0 #6) |
| T-11-02 | Tampering | bridge-edge fabrication (inventing company->company edges) | accept | exact name/symbol/paren-acronym matching only via normalizeEntityLabel; test asserts exactly the 6 real bridges — no alias map, no fabrication |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | zero new packages introduced (pure node:test over frozen data); RESEARCH §Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `npm test` exits 0 (full registered suite incl. tests/scenario-cascade.test.mjs).
- maxHops:1 / default reproduces 7 firms / 11360589871184 (v1.0 byte-identical).
- maxHops>=2 reproduces 8 firms / $13.28T, byHop {1:7,2:1}, TSM at hop 2.
- Synthetic cyclic fixture terminates; selfLabels = exactly the 6 bridges; memo keyed on maxHops.
</verification>

<success_criteria>
- runScenario propagates multi-hop over the real graph, bounded (maxHops) and cycle-safe (visited set) — CASC-01.
- Engine default maxHops:1 keeps every v1.0 fixture/memo test green; multi-hop set is a true superset.
- New unit tests (cycle termination, hop-count accuracy, superset, the 6 bridges, memo, backward-compat) registered and green — CASC-04.
</success_criteria>

<output>
Create `.planning/phases/11-multi-hop-scenario-cascade/11-01-SUMMARY.md` when done.
</output>
