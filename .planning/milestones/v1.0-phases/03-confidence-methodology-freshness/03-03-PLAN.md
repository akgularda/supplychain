---
phase: 03-confidence-methodology-freshness
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - index.html
  - js/ui/index.js
  - tests/methodology-wiring.test.mjs
  - tests/freshness-wiring.test.mjs
autonomous: true
requirements: [TRUST-04, TRUST-05, TRUST-06]
must_haves:
  truths:
    - "An investor can open a dedicated, accessible Methodology modal (role=dialog, focus trap, ESC/close) from a clear entry point near the existing controls/help."
    - "The Methodology modal states REAL facts: 407 sources, the high(120)/medium(3447) qualifier tiers, ~131 sources with parseable years, the weighting + age-decay explanation, observed/estimated/unknown semantics, and honest known limits (75 dangling source FKs, estimated relationships)."
    - "A visible freshness indicator displays the live meta value and stays accurate after a weekly refresh (no hardcoded date)."
    - "Exactly ONE owner writes #lastUpdated (updateStatusIndicator in js/ui); the freshness test proves the viz duplicate write is gone and the binding is live."
  artifacts:
    - path: "index.html"
      provides: "#methodologyModal (role=dialog) + a #bMethodology entry button, mirroring #helpModal"
      contains: "methodologyModal"
    - path: "js/ui/index.js"
      provides: "openMethodology/closeMethodology + methodology content + wireUI binding + ESC handling; sole #lastUpdated owner"
      contains: "openMethodology"
    - path: "tests/methodology-wiring.test.mjs"
      provides: "Asserts modal presence, a11y attrs, real-fact copy, entry-point wiring"
      min_lines: 25
    - path: "tests/freshness-wiring.test.mjs"
      provides: "Asserts live meta binding, single #lastUpdated owner (viz write gone), no hardcoded date"
      min_lines: 20
  key_links:
    - from: "index.html #bMethodology"
      to: "js/ui openMethodology"
      via: "wireUI onclick binding"
      pattern: "bMethodology"
    - from: "js/ui updateStatusIndicator"
      to: "window.SUPPLY_MAP_DATA.meta"
      via: "live read of meta.generatedAt/lastUpdated for #lastUpdated"
      pattern: "meta\\.(generatedAt|lastUpdated)"
---

<objective>
Add the honest Methodology modal (TRUST-04) and make the freshness indicator correct and single-owned (TRUST-05), with registered tests (TRUST-06).

The Methodology modal reuses the existing modal infrastructure (openModal/closeModal/trapFocus + the global ESC/Tab keydown handler) and states only verified facts from the real dataset. For freshness, Plan 02 already removed the viz-side duplicate #lastUpdated write; this plan confirms js/ui updateStatusIndicator is the sole, live-bound owner and adds the freshness regression test that proves it. This plan touches NO js/viz file (kept in Plan 02), so it runs in parallel with Plan 02.

Purpose: Investors can read how the trust numbers are derived and see a freshness guarantee that survives the weekly auto-update.
Output: a #methodologyModal + entry button in index.html, openMethodology + freshness single-owner confirmation in js/ui, and two registered tests.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-confidence-methodology-freshness/03-RESEARCH.md

<interfaces>
<!-- Verified this session. Use directly — no exploration. -->

index.html structure to mirror:
  #helpModal (line 37): <div id="helpModal" role="dialog" aria-modal="true" aria-labelledby="helpTitle" tabindex="-1">
     <div class="content" style="position:relative"><button class="close" onclick="toggleHelp()" aria-label="Close help dialog">&times;</button>
       <h2 id="helpTitle">...</h2> ... </div></div>
  Control buttons row (lines 120-130): ... <button id="bHelp" ... aria-label="Open help dialog">?</button>  (add #bMethodology near here)
  Footer (lines 237-243): <div id="footer"...><span class="updateStatus"><span class="statusDot" id="updateStatusDot"></span>
     <span id="updateStatusText">Checking...</span></span><span>|</span><span>Data updated: <span id="lastUpdated">--</span></span></div>

js/ui/index.js:
  modal infra: trapFocus(event,container) ~72, openModal(modal,display="flex") ~88 (sets activeModal, focuses first focusable, saves focusBeforeModal), closeModal(modal) ~98 (restores focus).
  helpModalEl/compareModalEl resolved at ~48-49; pattern: const helpModalEl = document.getElementById("helpModal").
  toggleHelp(forceOpen) ~599 uses openModal(helpModalEl,"flex").
  updateStatusIndicator() ~721-749: reads window.SUPPLY_MAP_DATA.meta; sets #updateStatusText/#updateStatusDot from daysOld; sets #lastUpdated = new Date(meta.generatedAt).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}). THIS is the chosen sole owner.
  global keydown ~880: if(activeModal && key==='escape'){ if(activeModal===helpModalEl) toggleHelp(false); else if (activeModal===compareModalEl) closeCompare(); ... }  (add a methodology branch).
  wireUI() ~786-994: button bindings (e.g. document.getElementById("bHelp").onclick = toggleHelp); modal backdrop-click closers at ~788-796.
  export block ~996: { ..., updateStatusIndicator, renderTop10List, wireUI, jump }.
  How inline onclick handlers reach the global: toggleHelp/openCompanyProfile are exposed via main.js's window.* shim — mirror that to expose closeMethodology so onclick="closeMethodology()" resolves (or attach the close via wireUI addEventListener instead of an inline onclick).
  escapeHtml is imported into ui from js/data — use it for any interpolated string in modal content.

VERIFIED real dataset facts (computed this session from data/top100-map.json — use these exact numbers in copy):
  407 total profile sources (396 unique ids); qualifiers high=120, medium=3447, none=1; 131 sources with a parseable year;
  3565 source FKs total, 75 dangling (unresolved) FKs. (RESEARCH A3: cite 75, the current verified figure, not the Phase-2-era 33.)

NOTE: Plan 02 removes the js/viz render() #lastUpdated write. This plan does NOT edit js/viz; it only verifies (via tests/freshness-wiring.test.mjs) that js/viz no longer writes #lastUpdated and that js/ui is the sole live owner.

Test convention: node:test + node:assert/strict; readFileSync string-presence on index.html and js/ui/index.js (and js/viz for the freshness regression read-only assert).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the accessible Methodology modal (index.html) + openMethodology wiring (js/ui) with real-fact copy</name>
  <read_first>index.html lines 37-60 (#helpModal pattern) + 120-130 (control button row); js/ui/index.js modal infra ~48-105, toggleHelp ~599, keydown ESC ~880-895, wireUI ~786-994, export block ~996; the VERIFIED dataset facts in this plan's interfaces block; js/main.js window.* shim pattern (how toggleHelp is exposed to inline onclick)</read_first>
  <files>index.html, js/ui/index.js</files>
  <action>(a) index.html: add a `#methodologyModal` mirroring `#helpModal` — `<div id="methodologyModal" role="dialog" aria-modal="true" aria-labelledby="methodologyTitle" tabindex="-1">` with a `.content` wrapper, a close button (`class="close"` with `aria-label="Close methodology dialog"`), an `<h2 id="methodologyTitle">Methodology &amp; Data Sources</h2>`, and honest sections covering: Data sources (407 sources across the 100 profiles; ~131 carry a parseable publication year); Confidence weighting (source-type tiers — high disclosure = Observed, source-backed = Estimated, unsourced/unresolved = Unknown; counts: 120 high-qualifier and 3,447 medium-qualifier figures); Age decay (exponential half-life ~4 years against the dataset generation date; no date parsed => no decay applied); What the badges mean (Observed / Estimated / Unknown semantics); Known limits (75 dangling source references, many relationships are estimated rather than disclosed, weekly auto-updated market caps). Add a `<button id="bMethodology" ... aria-label="Open methodology and data sources">Methodology</button>` to the control button row near `#bHelp` (line ~130). (b) js/ui/index.js: resolve `const methodologyModalEl = document.getElementById("methodologyModal");` next to helpModalEl (~48); add `function openMethodology(){ openModal(methodologyModalEl, "flex"); }` and `function closeMethodology(){ closeModal(methodologyModalEl); }`; in wireUI add `document.getElementById("bMethodology")?.addEventListener("click", openMethodology);` and a backdrop-click closer mirroring the help one; in the global keydown ESC branch add `else if (activeModal === methodologyModalEl) closeMethodology();`. Wire the modal close: EITHER attach the close button via addEventListener inside wireUI (preferred — avoids the inline-handler shim), OR if you use an inline `onclick="closeMethodology()"`, expose closeMethodology on the global the SAME way toggleHelp is exposed (mirror js/main.js's window.* shim) and add it to the ui export block. All modal copy is static text or uses escapeHtml for any interpolated value; do NOT fabricate numbers beyond the verified facts listed.</action>
  <verify>
    <automated>node --test tests/methodology-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="methodologyModal"` with `role="dialog"` and `aria-modal="true"`, and a `#bMethodology` entry button.
    - The modal copy contains the real figures: 407 (sources), 120 and 3,447 (qualifier counts), 75 (dangling FKs), and explains weighting + age decay + observed/estimated/unknown.
    - js/ui defines openMethodology + closeMethodology, wires #bMethodology, and ESC closes the methodology modal via the existing keydown handler.
    - The modal's close control resolves at runtime (addEventListener binding, or closeMethodology reachable on the global if inline onclick is used).
  </acceptance_criteria>
  <done>An accessible, honest Methodology modal is reachable from a clear entry point and reuses the proven modal a11y infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm js/ui is the sole live #lastUpdated owner + author methodology and freshness tests</name>
  <read_first>js/ui/index.js updateStatusIndicator ~721-749 (sole owner; reads meta.generatedAt, sets #lastUpdated); js/viz/index.js render() region (confirm Plan 02 removed the #lastUpdated write — read-only); index.html footer ~237-243; tests/viz-provenance-wiring.test.mjs (string-presence convention)</read_first>
  <files>js/ui/index.js, tests/freshness-wiring.test.mjs, tests/methodology-wiring.test.mjs</files>
  <action>(a) Confirm `updateStatusIndicator` in js/ui reads the LIVE `window.SUPPLY_MAP_DATA.meta` each call (it already does) and is the single writer of #lastUpdated with no hardcoded date literal. If you prefer to display the source `meta.lastUpdated` string verbatim instead of the reformatted generatedAt date, change ONLY the assignment inside updateStatusIndicator — keep exactly one writer; do not reintroduce a second binding. (b) Write `tests/freshness-wiring.test.mjs` (replace the Plan-01 placeholder): assert js/ui updateStatusIndicator reads `meta.generatedAt` and/or `meta.lastUpdated` (live), assert it sets `#lastUpdated`, assert js/viz NO LONGER writes #lastUpdated (read js/viz/index.js as a string and assert it does NOT match `getElementById(['\"]lastUpdated['\"])`), and assert no hardcoded date literal drives the freshness text (e.g. the #lastUpdated assignment in js/ui does not use a literal ISO `2026-..` string). (c) Write/finalize `tests/methodology-wiring.test.mjs` (the verify gate for Task 1): assert index.html has `#methodologyModal` with role=dialog + aria-modal, has `#bMethodology`, and that the modal copy contains the literals 407, 120, 3,447 (or 3447), 75, and the words observed/estimated/unknown and weighting/decay; assert js/ui defines openMethodology and wires bMethodology and handles methodologyModalEl in the ESC branch.</action>
  <verify>
    <automated>node --test tests/freshness-wiring.test.mjs tests/methodology-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - js/ui updateStatusIndicator is the only setter of #lastUpdated and reads window.SUPPLY_MAP_DATA.meta live (no hardcoded date).
    - tests/freshness-wiring.test.mjs asserts live binding + single owner (viz write absent) + no hardcoded date.
    - tests/methodology-wiring.test.mjs asserts modal presence/a11y/real-fact copy + entry-point wiring.
    - Both test files pass via `node --test`.
  </acceptance_criteria>
  <done>Exactly one live-bound owner renders the freshness date; it stays accurate after a weekly refresh and is regression-guarded, and the methodology modal is gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset-derived counts / source titles → modal HTML | Methodology copy and any interpolated value cross into innerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Methodology modal interpolated strings | mitigate | Copy is static verified text; any interpolated value uses the existing escapeHtml (V5 control), exactly as openProvenance/badgeHtml do. |
| T-03-06 | Information Disclosure | Known-limits copy (dangling FKs, estimates) | accept | Intentional honesty about data limits is a product requirement (TRUST-04), not a leak. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH: zero installs); nothing to slopcheck. |
</threat_model>

<verification>
- `node --test tests/methodology-wiring.test.mjs tests/freshness-wiring.test.mjs` passes.
- `npm test` stays green (files registered in Plan 01).
- Methodology modal opens/closes with focus trap + ESC; freshness has a single live-bound owner.
</verification>

<success_criteria>
Investors can read an honest, accessible Methodology view and see a freshness indicator that reflects the live auto-update timestamp from a single owner.
</success_criteria>

<output>
Create `.planning/phases/03-confidence-methodology-freshness/03-03-SUMMARY.md` when done.
</output>
