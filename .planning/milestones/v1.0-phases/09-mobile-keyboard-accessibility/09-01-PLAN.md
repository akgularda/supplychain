---
phase: 09-mobile-keyboard-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/layout.css
  - styles/theme.css
  - index.html
  - js/ui/index.js
  - package.json
  - tests/mobile-keyboard-a11y.test.mjs
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "On every viewport, #chokepointsPanel and #scenarioPanel are positioned (position:fixed) and do not overlap the fixed #top bar or #bar toolbar"
    - "At <=768px, #filterPanel, #heroOverlay, the new panels, and the profile/compare/methodology modals have usable mobile width/positioning"
    - "On mobile, Methodology, Tour, Chokepoints, and Scenario are reachable via #mobileSheet and trigger their toolbar action, then auto-close the sheet"
    - "tests/mobile-keyboard-a11y.test.mjs is registered in package.json scripts.test and npm test reports 0 failures"
  artifacts:
    - path: "styles/layout.css"
      provides: "position:fixed token-anchored layout for #chokepointsPanel and #scenarioPanel"
      contains: "#chokepointsPanel"
    - path: "styles/theme.css"
      provides: "@media(max-width:768px) responsive rules for new panels + modals + filterPanel/heroOverlay"
      contains: "max-width:768px"
    - path: "index.html"
      provides: "4 new mobile-sheet buttons in #mobileSheet .mGrid"
      contains: "mMethodology"
    - path: "js/ui/index.js"
      provides: "wireMobile wiring for the 4 new mobile-sheet controls"
      contains: "mMethodology"
    - path: "tests/mobile-keyboard-a11y.test.mjs"
      provides: "Node static asserts for responsive rules + mobile-sheet controls + ARIA"
      min_lines: 30
    - path: "package.json"
      provides: "scripts.test registration of the new a11y test file"
      contains: "mobile-keyboard-a11y.test.mjs"
  key_links:
    - from: "index.html #mobileSheet .mGrid"
      to: "js/ui/index.js wireMobile"
      via: "wireMobile('mMethodology', ...) delegating to #bMethodology.click()"
      pattern: "wireMobile\\(\"m(Methodology|Tour|Chokepoints|Scenario)\""
    - from: "styles/theme.css @media(max-width:768px)"
      to: "#chokepointsPanel,#scenarioPanel"
      via: "mobile width/positioning rules"
      pattern: "#chokepointsPanel"
---

<objective>
Fix mobile usability (PERF-02): give the unstyled #chokepointsPanel and #scenarioPanel real
position:fixed token-based layout so they stop overlapping the fixed #top/#bar on every viewport, add
@media(max-width:768px) rules so #filterPanel, #heroOverlay, the new panels, and the profile/compare/
methodology modals are usable on phones, and add the 4 missing controls (Methodology, Tour, Chokepoints,
Scenario) to the mobile sheet wired through the existing wireMobile helper. Wave 0: author the Node
static a11y test and register it in scripts.test.

Purpose: The new Phase 5-7 surfaces were never given responsive CSS or mobile-sheet entries, so they
overlap the header on desktop and are unreachable on mobile. This is the single biggest defect for PERF-02.
Output: Positioned/responsive panels, 4 wired mobile-sheet controls, a registered Node static test.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-keyboard-accessibility/09-RESEARCH.md

<interfaces>
<!-- Existing primitives to REUSE — do not rebuild. Verified in repo. -->
- styles cascade order: base.css -> layout.css -> components.css -> theme.css (theme.css wins).
- theme.css already has an @media(max-width:768px) block (hides #bar, shows #mobileToggle, sizes
  #companyCard/#searchWrap) and an @media(prefers-reduced-motion:reduce) block — EXTEND these, don't duplicate.
- layout.css already defines the global focus ring:
  button:focus-visible,select:focus-visible,input:focus-visible{outline:1px solid var(--acc)} — PRESERVE.
- Phase-4 design tokens available: --space-5/--space-6, --radius-lg, --color-surface-raised, --acc.
- js/ui/index.js wireUI() already calls wireMobile(id, handler) for 9 controls; wireMobile auto-closes the
  sheet after the action (closeMobileAfterAction). New calls go next to the existing ones.
- index.html #mobileSheet contains a .mGrid (repeat(4,1fr) on mobile) with 9 existing buttons.
- Toolbar targets to delegate to: #bMethodology, #bTour, #bChokepoints, #bScenarioTaiwan (all real <button>).
- New panels already carry role="region" + aria-label in markup (do NOT re-add roles).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Author and register the Node static a11y test</name>
  <read_first>
    tests/macro-site-accessibility.test.mjs (pattern to mirror — fs.readFileSync + assert.match on markup/css strings)
    package.json (scripts.test is an EXPLICIT file list, not a glob — Pitfall 5)
    index.html (confirm new-control + panel ids/labels referenced by the asserts)
  </read_first>
  <action>
    Create tests/mobile-keyboard-a11y.test.mjs as a node:test + node:assert/strict suite that reads
    index.html, styles/theme.css and styles/layout.css via fs.readFileSync (process.cwd() relative) and
    asserts, as plain string/regex checks: (1) new toolbar controls #bMethodology, #bTour, #bChokepoints,
    #bScenarioTaiwan, #bScenarioReset and #scenarioChokepointSelect each have an aria-label; (2) hero
    controls #heroPrev,#heroPause,#heroNext,#heroSkip each have an aria-label; (3) #chokepointsPanel and
    #scenarioPanel carry role="region" + aria-label; (4) the mobile sheet contains #mMethodology,#mTour,
    #mChokepoints,#mScenario; (5) the combined css matches an @media rule with max-width:768px AND contains
    #chokepointsPanel and #scenarioPanel selectors; (6) layout.css still contains a :focus-visible ... outline
    rule (focus ring preserved). Then register the file in package.json scripts.test by appending
    "tests/mobile-keyboard-a11y.test.mjs" to the existing explicit list (per D PERF-02; Pitfall 5).
    Do NOT add the orphaned macro-site-*.test.mjs files to the run list.
  </action>
  <acceptance_criteria>
    - tests/mobile-keyboard-a11y.test.mjs exists and appears in package.json scripts.test
    - The test currently FAILS on the not-yet-added assertions (RED for the panel/sheet/css checks)
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/mobile-keyboard-a11y.test.mjs ; node -e "process.exit(require('./package.json').scripts.test.includes('mobile-keyboard-a11y.test.mjs')?0:1)"</automated>
  </verify>
  <done>Test file authored and registered; failing assertions describe the layout/sheet work below.</done>
</task>

<task type="auto" tdd="true">
  <name>Task A: Position + responsive CSS for the new panels, modals, filter, hero</name>
  <files>styles/layout.css, styles/theme.css</files>
  <behavior>
    - #chokepointsPanel and #scenarioPanel get position:fixed (no static-flow overlap of #top/#bar) on desktop
    - At <=768px the two panels become full-width-ish stacked (left/right inset) above the canvas, not clipped
    - At <=768px #filterPanel, #heroOverlay and the profile/compare/methodology modals get usable mobile width
    - layout.css :focus-visible outline rule is unchanged; no outline:none reintroduced
  </behavior>
  <read_first>
    styles/layout.css (existing focus-visible rule, token usage, current panel/modal positioning)
    styles/theme.css (existing @media(max-width:768px) block — extend it; cascade-last file)
    styles/components.css (modal/panel base styling so mobile overrides stay consistent)
    .planning/phases/09-mobile-keyboard-accessibility/09-RESEARCH.md (Pattern 1 example values)
  </read_first>
  <action>
    In styles/layout.css add desktop position:fixed rules for #chokepointsPanel and #scenarioPanel anchored
    with Phase-4 spacing tokens (e.g. left:var(--space-6), a high z-index below the toolbar, a fixed width,
    max-height with overflow-y:auto, token border/radius/padding, distinct bottom offsets so the two panels do
    not overlap each other), so they leave static flow and stop covering #top/#bar (RESEARCH Pattern 1; A1
    docked-on-desktop). In styles/theme.css EXTEND the existing @media(max-width:768px) block (do not create a
    second one) so #chokepointsPanel and #scenarioPanel become left/right-inset, auto width, bottom-anchored
    above the mobile toolbar with a bounded max-height; and so #filterPanel, #heroOverlay and the
    profile/compare/methodology modals (use their existing ids/classes) take a usable mobile width/positioning
    and are not clipped. Use only existing tokens; reuse the existing prefers-reduced-motion block as-is.
    Per D PERF-02. Do NOT change panel role/aria, do NOT touch js, do NOT add new tokens.
  </action>
  <verify>
    <automated>node --test tests/mobile-keyboard-a11y.test.mjs</automated>
  </verify>
  <done>Both panels positioned fixed; @media(max-width:768px) covers panels+filter+hero+modals; the css assertions in the a11y test pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task B: Add the 4 new mobile-sheet controls and wire them</name>
  <files>index.html, js/ui/index.js</files>
  <behavior>
    - #mobileSheet .mGrid contains #mMethodology, #mTour, #mChokepoints, #mScenario as real <button> elements
    - Tapping each triggers the matching toolbar action and the sheet auto-closes
  </behavior>
  <read_first>
    index.html (#mobileSheet .mGrid — existing 9 buttons; copy their markup shape/labels)
    js/ui/index.js (wireUI() existing wireMobile(...) calls + closeMobileAfterAction behavior)
  </read_first>
  <action>
    In index.html, inside #mobileSheet .mGrid, add four real <button type="button"> elements with ids
    mMethodology, mTour, mChokepoints, mScenario and short visible text labels (e.g. Method, Tour, Chokepts,
    Scenario), matching the markup shape of the existing sheet buttons (textContent only — no raw innerHTML,
    per the XSS note). In js/ui/index.js wireUI(), next to the existing wireMobile(...) calls, add
    wireMobile("mMethodology", () => document.getElementById("bMethodology").click()),
    wireMobile("mTour", () => document.getElementById("bTour").click()),
    wireMobile("mChokepoints", () => document.getElementById("bChokepoints").click()),
    wireMobile("mScenario", () => document.getElementById("bScenarioTaiwan").click()).
    Per D PERF-02; reuse wireMobile's auto-close (Pattern 2). Do NOT add a custom mobile menu or touchstart handlers.
  </action>
  <verify>
    <automated>node --test tests/mobile-keyboard-a11y.test.mjs tests/index-ui-integrity.test.mjs</automated>
  </verify>
  <done>Four new sheet buttons exist and are wired via wireMobile; a11y test "mobile sheet exposes the new controls" passes; index-ui-integrity stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing search input -> DOM | Only pre-existing user input; no new input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | new #mobileSheet buttons in index.html | mitigate | Add buttons as static <button> markup with textContent only; never build them via raw-string innerHTML (preserve escapeHtml posture) |
| T-09-02 | Information disclosure | static site / no new data flow | accept | No PII, no network, no auth; presentation-only change |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none); no install task |
</threat_model>

<verification>
- node --test tests/mobile-keyboard-a11y.test.mjs passes (css + sheet + ARIA asserts green)
- npm test reports 0 failures (existing 275 + the new a11y file)
- Manual spot check at 390px: #chokepointsPanel/#scenarioPanel do not overlap #top/#bar
</verification>

<success_criteria>
- #chokepointsPanel and #scenarioPanel are position:fixed and never overlap the fixed header on any viewport
- @media(max-width:768px) gives the new panels, #filterPanel, #heroOverlay, and modals usable mobile layout
- Methodology/Tour/Chokepoints/Scenario reachable from #mobileSheet on mobile and auto-close it
- tests/mobile-keyboard-a11y.test.mjs registered in scripts.test; npm test green
</success_criteria>

<output>
Create `.planning/phases/09-mobile-keyboard-accessibility/09-01-SUMMARY.md` when done
</output>
