---
phase: 09-mobile-keyboard-accessibility
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - tests/mobile-keyboard.spec.mjs
  - package.json
autonomous: false
requirements: [PERF-02, PERF-03]
must_haves:
  truths:
    - "A Playwright spec emulates 390x844 hasTouch, taps a node, and a profile (#companyCard) opens; #mobileSheet is reachable"
    - "A Playwright spec drives the keyboard-only journey: '/' focuses #q, type, Enter selects, Escape resets"
    - "The spec asserts the hero overlay traps focus (activeElement stays inside #heroOverlay after open)"
    - "tests/mobile-keyboard.spec.mjs is registered in package.json scripts.test"
    - "npm test reports 0 failures across the full suite (with a documented Node-static fallback if chromium is unavailable)"
  artifacts:
    - path: "tests/mobile-keyboard.spec.mjs"
      provides: "Playwright 390x844 mobile + keyboard-only + focus-trap smoke, http-server served origin"
      min_lines: 40
    - path: "package.json"
      provides: "scripts.test registration of the Playwright spec"
      contains: "mobile-keyboard.spec.mjs"
  key_links:
    - from: "tests/mobile-keyboard.spec.mjs"
      to: "#canvas .node tap -> #companyCard"
      via: "page.tap on a node opens the profile (D3 click fires on touch)"
      pattern: "companyCard"
    - from: "tests/mobile-keyboard.spec.mjs"
      to: "#q keyboard journey"
      via: "page.keyboard '/' -> type -> Enter -> Escape"
      pattern: "keyboard"
---

<objective>
Close the phase (PERF-02 + PERF-03) with a Playwright smoke that exercises the real browser behaviors the Node
static tests cannot: a 390x844 hasTouch context that taps a node to open the profile and confirms the mobile
sheet is reachable, and a keyboard-only journey ('/' -> type -> Enter -> Escape) that asserts the search focus,
selection, reset, and that the hero overlay actually traps focus. Register the spec in scripts.test, run the
full suite to a 0-fail gate (with a documented Node-static fallback if chromium is absent), then a human-verify
checkpoint for real device/keyboard feel.

Purpose: Only a browser can verify focus movement, tap->profile, and the trap; this is the phase gate.
Output: A registered Playwright spec, full green suite, human sign-off.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-keyboard-accessibility/09-RESEARCH.md
@.planning/phases/09-mobile-keyboard-accessibility/09-01-SUMMARY.md
@.planning/phases/09-mobile-keyboard-accessibility/09-02-SUMMARY.md

<interfaces>
<!-- Playwright + http-server are already installed deps. Verified in package.json. -->
- playwright ^1.58.2 (dependency); http-server ^14.1.1 (devDependency).
- Pattern (RESEARCH Code Examples): node:test before/after spins up createServer({root:cwd,cache:-1}) bound
  to 127.0.0.1 on an ephemeral port, base = http://127.0.0.1:<port>/index.html; closed in test.after.
- Mobile context: browser.newContext({ viewport:{width:390,height:844}, hasTouch:true, isMobile:true }).
- Selectors to gate on (D3 settles ~15s — wait on selectors, never fixed timeouts): #canvas .node, #companyCard,
  #mobileToggle, #mobileSheet, #q, #heroOverlay, #heroSkip.
- Keyboard journey: page.keyboard.press('/') focuses #q (assert document.activeElement.id==='q'); type a known
  ticker; Enter selects; Escape resets. For scenario select use page.selectOption(index:1) then await the impact
  list (Pitfall 4) — only if the spec touches scenario.
- Focus-trap assertion: open the hero (tour), then assert document.activeElement is inside #heroOverlay and that
  Tab keeps it inside (does not land on #bReset).
- Chromium binary may be absent in CI (A2). Add `npx playwright install chromium` to setup AND wrap launch so a
  missing binary degrades to a skip with a logged note — the Node static a11y test remains the structural gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Author + register the Playwright mobile/keyboard/focus-trap spec</name>
  <read_first>
    .planning/phases/09-mobile-keyboard-accessibility/09-RESEARCH.md (Code Examples: the exact Playwright spec skeleton + Pitfall 4)
    package.json (scripts.test explicit list — append the spec; deps already include playwright + http-server)
    index.html (confirm #canvas .node, #mobileToggle, #mobileSheet, #companyCard, #q, #heroOverlay, #heroSkip ids)
  </read_first>
  <action>
    Create tests/mobile-keyboard.spec.mjs (node:test + node:assert/strict + playwright chromium + http-server)
    following the RESEARCH Code Examples skeleton. test.before starts createServer({root:process.cwd(),cache:-1})
    on 127.0.0.1 ephemeral port; test.after closes it. Test 1 (PERF-02): newContext with viewport 390x844,
    hasTouch:true, isMobile:true; goto base; waitForSelector('#canvas .node'); tap('#mobileToggle') and assert
    #mobileSheet visible; tap the first '#canvas .node' and waitForSelector('#companyCard',{state:'visible'}).
    Test 2 (PERF-03): default desktop context; goto; wait for nodes; keyboard.press('/') then assert
    document.activeElement.id==='q'; type a known ticker; press Enter; wait for #companyCard visible; press Escape;
    then assert every control in #bMethodology,#bTour,#bChokepoints,#bScenarioTaiwan,#bScenarioReset,#heroSkip,
    #scenarioChokepointSelect has a non-empty accessible name. Test 3 (PERF-03 focus trap): open the hero (tour),
    assert document.activeElement is inside #heroOverlay after open, press Tab and assert focus is still inside
    #heroOverlay (not on #bReset). Gate all waits on selectors, not timeouts (D3 ~15s settle). Wrap chromium.launch
    in a guard so a missing browser binary logs a skip note rather than failing the suite (Node a11y test is the
    fallback gate, A2). Register tests/mobile-keyboard.spec.mjs in package.json scripts.test (per D PERF-02/03;
    Pitfall 5). Keep the server bound to 127.0.0.1 and closed in after (no exposure).
  </action>
  <acceptance_criteria>
    - tests/mobile-keyboard.spec.mjs exists and is listed in package.json scripts.test
    - The three tests pass when chromium is present, or skip-with-note when absent (suite still 0-fail)
  </acceptance_criteria>
  <verify>
    <automated>node -e "process.exit(require('./package.json').scripts.test.includes('mobile-keyboard.spec.mjs')?0:1)" && node --test tests/mobile-keyboard.spec.mjs</automated>
  </verify>
  <done>Spec authored + registered; mobile tap->profile, keyboard journey, and focus-trap assertions pass (or skip cleanly if no chromium).</done>
</task>

<task type="auto">
  <name>Task B: Full-suite green gate</name>
  <read_first>
    package.json (the full scripts.test list — must now include both 09-01 and 09-03 new files)
  </read_first>
  <action>
    Run the full suite via npm test and confirm 0 failures, including tests/mobile-keyboard-a11y.test.mjs,
    tests/mobile-keyboard.spec.mjs, no-restart-invariant.test.mjs, macro-site-accessibility.test.mjs and the
    pre-existing 275. If chromium is unavailable, confirm the Playwright spec skips cleanly (logged note) and the
    Node a11y test still passes as the structural fallback gate, and record that fact in the SUMMARY. Do NOT add
    the orphaned macro-site-*.test.mjs files to the run list. If any pre-existing test regressed, fix the cause in
    the offending plan's files (do not weaken assertions).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test reports 0 failures; both new files run; fallback behavior documented if chromium absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Mobile responsive layout for the new panels + modals, 4 new mobile-sheet controls, a focus-trapping hero
    overlay, and a Playwright 390x844 + keyboard-only + focus-trap smoke. Full suite is green.
  </what-built>
  <how-to-verify>
    1. Serve the site (e.g. `npx http-server -p 8080`) and open http://127.0.0.1:8080 in a phone-sized viewport (390x844, DevTools device mode).
    2. Confirm #chokepointsPanel and #scenarioPanel do not overlap the top bar/toolbar; open the mobile sheet (#mobileToggle) and verify Methodology, Tour, Chokepoints, Scenario each work and the sheet auto-closes.
    3. Tap a node — its profile (#companyCard) should open.
    4. On desktop, using ONLY the keyboard: press '/' to focus search, type a ticker, Enter to select, Escape to reset. Tab through the toolbar and confirm a visible focus ring on every control.
    5. Start the Tour: confirm focus moves into the hero overlay, Tab stays trapped inside it, and Escape closes it returning focus to where you were.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any overlap, unreachable control, missing focus ring, or focus leak.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test http-server -> localhost | Static site served only to the local Playwright browser during tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information disclosure | http-server in the Playwright spec | mitigate | Bind to 127.0.0.1 on an ephemeral port; close in test.after; no external interface |
| T-09-06 | Denial of service | flaky D3 ~15s settle in e2e | mitigate | Gate on selectors (waitForSelector), never fixed timeouts; await scenario impact list before asserting (Pitfall 4) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; playwright + http-server already present (RESEARCH audit: none new) |
</threat_model>

<verification>
- node --test tests/mobile-keyboard.spec.mjs passes (or skips cleanly with logged note if chromium absent)
- npm test reports 0 failures across the full suite including both new Phase-9 test files
- Human-verify checkpoint approved
</verification>

<success_criteria>
- Playwright spec asserts 390x844 tap->profile, keyboard '/'->type->Enter->Escape journey, and hero focus trap
- Both new test files registered in scripts.test; npm test green (Node-static fallback documented if no chromium)
- Human confirms mobile + keyboard usability on a real viewport
</success_criteria>

<output>
Create `.planning/phases/09-mobile-keyboard-accessibility/09-03-SUMMARY.md` when done
</output>
