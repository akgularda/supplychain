---
phase: 02-provenance-source-linking
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - js/viz/index.js
autonomous: true
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "The node tooltip shows an observed/estimated/unknown provenance badge derived via provenanceFor (not the old inline high/medium guess)"
    - "The relationship (link) tooltip shows a provenance badge derived from cf/sf via provenanceFor"
    - "The $cap stat bar figure (#sM) carries an observed provenance badge linking to the dataset market-cap source"
    - "Sourced figures expose a reachable target=_blank source link; dangling/empty-FK figures show the explicit unknown/no-link state (no fabricated URL)"
    - "The verified-node circle class is driven through the centralized trust derivation, not a duplicated string check"
  artifacts:
    - path: "js/viz/index.js"
      provides: "viz render paths instrumented with provenanceFor/renderProvenanceBadge"
      contains: "provenanceFor"
  key_links:
    - from: "js/viz/index.js"
      to: "js/trust/index.js"
      via: "import { provenanceFor, renderProvenanceBadge }"
      pattern: "from \\\"\\.\\./trust/index\\.js\\\""
    - from: "js/viz/index.js showTooltip"
      to: "STATE.sourceIndex"
      via: "ctx passed to provenanceFor"
      pattern: "provenanceFor\\("
---

<objective>
Wire the tested trust API (from plan 02-01) into the three viz render paths that show major figures to
the user: the node tooltip (`showTooltip`), the relationship tooltip (`showLinkTooltip`), and the
`$cap` stat bar figure in `updateStats`. Replace the duplicated, inconsistent inline confidence
parsing with `provenanceFor` + `renderProvenanceBadge`, and route the `verified-node` circle class
through the same derivation.

Purpose: Make viz-owned figures honestly observed/estimated/unknown with reachable links — the
visualization-side slice of TRUST-01/TRUST-02.
Output: an updated `js/viz/index.js` (this plan owns this file exclusively; runs parallel to plan 03).

User story: As an investor hovering a node or relationship, I want to see whether the figure is
observed or estimated and click through to the real source.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provenance-source-linking/02-RESEARCH.md
@.planning/phases/02-provenance-source-linking/02-01-PLAN.md

<interfaces>
<!-- The trust API delivered by plan 02-01. Import and call — do not re-derive confidence inline. -->
From js/trust/index.js:
  provenanceFor(input, ctx = {}) -> { tag:'observed'|'estimated'|'unknown', source?:{label,url} }
  renderProvenanceBadge(el, prov) -> void   (sets el.innerHTML to the accessible pill + optional link)
  badgeHtml(prov) -> string                 (use when you need the markup inside a larger innerHTML template)

ctx to pass: { sourceIndex: STATE.sourceIndex, meta: DATA.meta }
  - STATE.sourceIndex is populated in viz render() L395 (`STATE.sourceIndex = graph.sourceIndex || {}`)
  - DATA.meta is the rich-JSON meta (may be undefined in the live thin JS -> provenanceFor degrades to unknown; that is correct)

Existing viz render paths to instrument (verified line refs, may drift — locate by function name):
  - updateStats()      js/viz/index.js L68-80   : $cap figure animates into #sM; stat bar container is #bar (index.html L119), $cap is #sM (L107)
  - showTooltip(d,ev)  L170-237  : builds confidence-* badge L181/197; "View Source" link L228-232 (d.sourceId -> STATE.sourceIndex[d.sourceId].url)
  - showLinkTooltip()  L246-296  : cf badge L257/262; source link L287-291 (d.sf -> STATE.sourceIndex[d.sf].url)
  - node circle class  L497      : .classed("verified-node", d.confidence && d.confidence.includes("source"))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route node + link tooltips through provenanceFor/badgeHtml</name>
  <files>js/viz/index.js</files>
  <read_first>
    - js/viz/index.js (top imports L5-7; showTooltip L170-237; showLinkTooltip L246-296)
    - .planning/phases/02-provenance-source-linking/02-RESEARCH.md "Major Figure Render Paths" (viz table) + "Common Pitfalls" 1 & 3
    - js/trust/index.js (the API from plan 02-01)
  </read_first>
  <behavior>
    - A node with confidence 'high (...)' and a resolving sourceId renders an 'Observed' badge + a reachable source link in the tooltip
    - A node with confidence 'medium (...)' renders 'Estimated'
    - A node whose sourceId is dangling/absent renders the badge tag with NO source link (no crash, no fabricated url)
    - A link uses d.cf/d.sf the same way via provenanceFor
  </behavior>
  <action>
    Add `import { provenanceFor, renderProvenanceBadge, badgeHtml } from "../trust/index.js";` to the import block near L5-7. In showTooltip, replace the inline confidenceRaw/confidenceLower/confidenceLevel/verified block (L174-182) with `const prov = provenanceFor(d, { sourceIndex: STATE.sourceIndex, meta: DATA.meta });` and substitute the old `<span class="confidence-badge ...">{label} Confidence</span>` (L197) with `badgeHtml(prov)`. Replace the conditional "View Source" anchor (L228-232) so the link comes from `prov.source` (emit only when `prov.source` exists) instead of re-reading STATE.sourceIndex[d.sourceId] inline — this removes the dangling-FK crash risk. Do the identical transformation in showLinkTooltip using the same `provenanceFor(d, ctx)` (it reads d.cf/d.sf automatically) replacing L250-258 and L287-291. Keep all surrounding tooltip layout/markup unchanged. Import DATA at top is already present (L6) — add `provenanceFor` import only.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "provenanceFor" js/viz/index.js` >= 2 (showTooltip + showLinkTooltip)
    - `grep -q "from \"../trust/index.js\"" js/viz/index.js` (import wired)
    - `node --check js/viz/index.js` passes
    - `npm test` exits 0 (116 + provenance suite still green)
    - showTooltip/showLinkTooltip no longer contain the old inline `confidenceLower.includes('high')` derivation (centralized via trust)
  </acceptance_criteria>
  <done>Both tooltips derive their badge + source link from provenanceFor; dangling FKs no longer risk a crash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badge the $cap stat bar figure + centralize the verified-node class</name>
  <files>js/viz/index.js</files>
  <read_first>
    - js/viz/index.js (updateStats L68-80; node circle .classed L497; render L388-420 where STATE.sourceIndex is set)
    - .planning/phases/02-provenance-source-linking/02-RESEARCH.md "Open Questions" Q1 (counts omit badges; only $cap + per-node marketcap get observed) + Real Data Shape ($cap sums marketcap, meta.source)
  </read_first>
  <behavior>
    - The $cap figure carries an Observed provenance badge whose source link resolves to meta.source / top100_source_url
    - Pure derived counts (#sN nodes, #sL links, #sC countries, #sY layers) get NO badge (they are computed aggregates, not sourced) — per RESEARCH Q1
    - The verified-node circle class reflects provenanceFor's observed/sourced determination rather than the old ad-hoc .includes('source')
  </behavior>
  <action>
    In updateStats (after the animateCounter for sM, L79), compute `const capProv = provenanceFor({ marketcap: true }, { meta: DATA.meta });` and render its badge into a sibling element of #sM inside the #bar container — append a `<span>` next to the `$cap` figure (locate the `#sM` parent span in index.html L107 markup; inject via `renderProvenanceBadge` into a created/located element, do NOT edit index.html — create the span in JS and append to #sM.parentElement). Do NOT badge the count figures #sN/#sL/#sC/#sY (RESEARCH Q1: derived aggregates, omit). For the node circle class at L497, replace the inline `d.confidence && d.confidence.includes("source")` with a check derived from `provenanceFor(d, { sourceIndex: STATE.sourceIndex, meta: DATA.meta })` — verified when `prov.tag === 'observed'` AND `prov.source` exists (i.e. high-confidence AND a resolving source). Guard for the thin-JS runtime where DATA.meta/STATE.sourceIndex may be empty (provenanceFor already returns unknown -> no badge/no verified class, which is the correct graceful degradation per Pitfall 1).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "provenanceFor" js/viz/index.js` >= 3 (tooltips + stat bar/verified-node)
    - `grep -q "marketcap: true" js/viz/index.js` (the $cap observed marker)
    - The count figures (#sN/#sL/#sC/#sY) are NOT given a badge (verify by reading updateStats — only $cap/#sM is badged)
    - `node --check js/viz/index.js` passes; `npm test` exits 0
    - verified-node class no longer uses a standalone `.includes("source")` string check — it goes through provenanceFor
  </acceptance_criteria>
  <done>The $cap figure is observed-badged with a reachable market-cap source; counts stay unbadged; the verified-node marker is trust-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data record -> tooltip/stat-bar innerHTML | source.title/url interpolated into innerHTML via badgeHtml (escaping handled in trust module) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | tooltip source link injection | mitigate | use badgeHtml (which escapes title + gates url on http) instead of raw inline interpolation |
| T-02-05 | DoS/crash | dangling sourceId in showTooltip (33 rows) | mitigate | provenanceFor null-checks the FK; tooltip emits link only when prov.source present |
| T-02-SC | Tampering | npm installs | accept | No packages this phase (buildless) |
</threat_model>

<verification>
- `npm test` exits 0 (116 + provenance suite).
- viz imports and calls provenanceFor at all three render paths.
- $cap badged observed; counts unbadged; verified-node class trust-derived.
</verification>

<success_criteria>
- Node tooltip, link tooltip, and the $cap stat figure all show data-derived provenance badges with reachable links where sourced.
- No crash on the 33 dangling FKs; unsourced figures show the honest unknown/no-link state.
</success_criteria>

<output>
Create `.planning/phases/02-provenance-source-linking/02-02-SUMMARY.md` when done.
</output>
