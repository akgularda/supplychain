---
phase: 06-concentration-risk-analytics
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - js/trust/index.js
  - js/ui/index.js
  - js/viz/index.js
  - index.html
  - tests/criticality-wiring.test.mjs
autonomous: true
requirements: [DEPTH-01, DEPTH-02]

must_haves:
  truths:
    - "provenanceFor({derived:true, n}) returns {tag:'derived', note:'computed from N relationships', source:Methodology} and is NEVER tagged observed"
    - "badgeHtml emits a 'Derived' label (reusing confidence-* CSS + the http-only URL guard) for the derived tag"
    - "The company profile panel shows 'Supplier concentration: NN/100' with a Derived badge + Methodology link, computed via companyConcentration"
    - "A Critical chokepoints panel lists top suppliers by real dependent-company count and can highlight them in the graph via highlightBy"
    - "The Methodology modal explains the concentration + criticality formulas, their inputs, and the equal-weight (l.v=constant) limit"
  artifacts:
    - path: "js/trust/index.js"
      provides: "Derived provenance branch in provenanceFor + Derived label in badgeHtml"
      contains: "derived"
    - path: "js/ui/index.js"
      provides: "Profile concentration line + Derived badge + chokepoints panel wiring"
      contains: "companyConcentration"
    - path: "js/viz/index.js"
      provides: "Chokepoint highlight predicate (reuses highlightBy)"
      contains: "highlightBy"
    - path: "index.html"
      provides: "Methodology modal concentration + criticality formula copy; chokepoints panel host"
      contains: "concentration"
  key_links:
    - from: "js/ui/index.js"
      to: "js/analytics/index.js"
      via: "import companyConcentration / supplierCriticality"
      pattern: "from ['\"]\\.\\./analytics/index\\.js['\"]"
    - from: "js/ui/index.js"
      to: "js/trust/index.js"
      via: "provenanceFor({derived:true,n}) + badgeHtml"
      pattern: "derived\\s*:\\s*true"
    - from: "js/ui/index.js"
      to: "js/viz/index.js"
      via: "highlightBy chokepoint predicate"
      pattern: "highlightBy"
---

<objective>
Make the analytics visible and honestly sourced. Extend the trust vocabulary with a `derived` provenance tag, then wire the Plan-01 analytics into the UI: a concentration line + Derived badge in the company profile, a Critical chokepoints panel that highlights single-point suppliers in the graph, and Methodology copy that documents the formulas and the equal-weight limit.

Purpose: A derived number shown as `observed` would break the Phase 2–3 trust contract. The `derived` tag plus Methodology copy is what lets us display computed concentration/criticality without fabricating provenance.
Output: derived branch in js/trust; profile + chokepoints display in js/ui; chokepoint highlight in js/viz; Methodology copy + panel host in index.html; extended assertions in tests/criticality-wiring.test.mjs.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-concentration-risk-analytics/06-RESEARCH.md
@.planning/phases/06-concentration-risk-analytics/06-01-SUMMARY.md

# Files this plan edits
@js/trust/index.js
@js/ui/index.js
@index.html

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From js/analytics/index.js (created in Plan 01):
- `companyConcentration(symbol, opts)` -> { symbol, suppliers:k, hhi, sharedCount, sharedFrac, score, n } | null
- `sectorConcentration(sector, opts)` -> { sector, companies, distinctSuppliers, slots, reusePct, effectiveSuppliers }
- `supplierCriticality(opts)` -> [{ supplier, fanIn }] sorted desc

From js/trust/index.js (L23-46 provenanceFor, L87-108 badgeHtml):
- provenanceFor(input, ctx) — branch order: marketcap → confidence/cf → source FK. ADD a `derived` branch BEFORE the marketcap check returning {tag:'derived', note, source}. ctx may carry `methodologyUrl`.
- badgeHtml(prov) — label/cls map: observed→Observed/confidence-high, estimated→Estimated/confidence-medium, else Unknown/confidence-low. Gates source <a> on `url.startsWith("http")` with rel="noopener noreferrer". ADD derived→"Derived" (reuse confidence-medium class). NEVER map derived to "Observed".

From js/ui/index.js:
- imports already include { provenanceFor, renderProvenanceBadge, badgeHtml } from ../trust (L15) and DATA/escapeHtml/getTopOverlap from ../data (L6-10)
- renderCardInsights(symbol, profile) (L431-484) owns the profile insight cards; cardOverlap (#cardOverlap) is rendered at L460-472; cardRiskDetails at L452-458. Add the concentration line here.
- updateCompanyCard renders #cardNameProv badge (L265) — existing derived-badge precedent for the company name.
- openMethodology/closeMethodology + #bMethodology wiring exist (L601, L807); wireUI (L807+) is where new buttons/panels get wired.
- ui exports at L1013-1018.

From js/viz/index.js:
- highlightBy(fn) (L385) dims non-matching nodes; resetHighlight (L375) restores. Node datum carries d.symbol / d.l / d.bn. A chokepoint predicate matches global supplier nodes whose normalized label is in the chokepoint set.

From index.html:
- #methodologyModal body (L83-116) — append concentration + criticality <h3>/<p> sections.
- Company card / insight panel host is #companyCard with #cardOverlap, #cardRiskDetails children (find the card markup near these IDs to add a #cardConcentration host + a chokepoints panel host, e.g. #chokepointsPanel).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add derived provenance branch to js/trust + extend tests</name>
  <read_first>js/trust/index.js:23-46 (provenanceFor branches) + :87-108 (badgeHtml label/cls/url-guard); 06-RESEARCH.md Pattern 2 (derived branch code) + Anti-Patterns (never tag derived 'observed'); 06-RESEARCH.md Security Domain (http-only URL guard must be reused)</read_first>
  <files>js/trust/index.js, tests/criticality-wiring.test.mjs</files>
  <behavior>
    - provenanceFor({derived:true, n:5}, {methodologyUrl:'#methodology'}) → { tag:'derived', note:'computed from 5 relationships', source:{label:'Methodology', url:'#methodology'} }.
    - n===1 yields singular 'computed from 1 relationship'; missing/non-finite n yields 'computed from 0 relationships'.
    - With no ctx.methodologyUrl, the derived result has tag:'derived' + note and NO source key (never throws).
    - badgeHtml({tag:'derived',...}) contains the literal label "Derived" and class confidence-medium; it does NOT contain "Observed".
    - badgeHtml derived branch still gates the source link on url.startsWith('http') (a non-http url like '#methodology' emits no <a>, OR an http(s) methodology url emits rel="noopener noreferrer") — reuse the existing guard, no new XSS surface.
  </behavior>
  <action>Add a `derived` branch as branch 0 of provenanceFor in js/trust/index.js (before the marketcap check): if input && input.derived === true, return {tag:'derived', note:`computed from ${n} relationship${n===1?'':'s'}`, source: ctx.methodologyUrl ? {label:'Methodology', url:ctx.methodologyUrl} : undefined} where n = Number.isFinite(input.n)?input.n:0. In badgeHtml, extend the label map with derived→"Derived" and the cls map with derived→"confidence-medium"; keep the existing url.startsWith('http') guard and rel="noopener noreferrer" so a non-http methodology anchor degrades to label-only. NEVER let derived fall through to the observed label. Add the matching assertions to tests/criticality-wiring.test.mjs (singular/plural note, no-source-without-methodologyUrl, Derived-not-Observed label, http-guard).</action>
  <verify>
    <automated>node --test tests/criticality-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - provenanceFor derived branch returns tag 'derived' + correct singular/plural note + Methodology source (or no source when methodologyUrl absent).
    - badgeHtml derived emits "Derived" (confidence-medium), never "Observed"; http-only URL guard preserved.
    - tests/criticality-wiring.test.mjs green with the new derived assertions.
  </acceptance_criteria>
  <done>Derived provenance is a first-class honest tag in js/trust, covered by tests; derived numbers can never render as observed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire concentration + Derived badge into the profile panel and add the chokepoints panel + graph highlight</name>
  <read_first>js/ui/index.js:431-484 (renderCardInsights, cardOverlap/cardRiskDetails render) + :265 (#cardNameProv derived-badge precedent) + :807 (wireUI button wiring) + :1013 (exports); js/viz/index.js:385 (highlightBy) + :375 (resetHighlight); index.html company-card markup (near #cardOverlap) + #methodologyModal (L83-116); 06-CONTEXT.md specifics ("Supplier concentration: NN/100 (HHI-based)" + "Critical chokepoints" panel); 06-RESEARCH.md V5 (escapeHtml every interpolated supplier label)</read_first>
  <files>js/ui/index.js, js/viz/index.js, index.html</files>
  <action>In index.html: add a #cardConcentration host inside the company card insight area (near #cardOverlap) and a #chokepointsPanel host (list + a "Highlight in graph" button #bChokepoints) in the appropriate panel/sidebar; append two <h3>/<p> sections to #methodologyModal documenting (a) the concentration formula C=round(100·(0.6·HHI + 0.4·sharedFrac)), HHI=1/k equal-weight because the dataset has no per-supplier volume (all link weights constant) — state this limit explicitly — and (b) the criticality fan-in ranking (a supplier depended on by N companies; real top = 4), noting these are DERIVED aggregates. In js/ui/index.js: import { companyConcentration, supplierCriticality } from "../analytics/index.js"; in renderCardInsights, compute c = companyConcentration(symbol, {profiles: DATA.profiles}) and render into #cardConcentration a line "Supplier concentration: NN/100 (HHI-based)" followed by badgeHtml(provenanceFor({derived:true, n:c.n}, {methodologyUrl:'#methodology'})); render a renderChokepoints() function that lists supplierCriticality({limit:8}) rows (escapeHtml every supplier label, show the integer fanIn count) into #chokepointsPanel, and wire #bChokepoints in wireUI to call highlightBy with a predicate matching global supplier nodes whose normalized label is in the chokepoint set (and a way back to resetHighlight). Add renderChokepoints/openMethodology-adjacent exports as needed. Use methodologyUrl='#methodology' (or whatever the existing methodology anchor is) so the badge links to the Methodology view; if non-http it degrades to label-only per the Task-1 guard. Per D-04 keep d.bn rendering untouched (separate editorial signal). Do NOT use "v1"/"placeholder"/"static" — render the real computed numbers.</action>
  <verify>
    <automated>node --test tests/criticality-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - js/ui imports companyConcentration + supplierCriticality from ../analytics and renders "Supplier concentration: NN/100" + a Derived badge in the profile (string-wiring assertions in tests/criticality-wiring.test.mjs pass: ui source contains companyConcentration, derived:true, badgeHtml, and the chokepoints + highlightBy wiring).
    - #chokepointsPanel lists top suppliers by real fan-in with escaped labels; #bChokepoints triggers highlightBy.
    - index.html #methodologyModal contains the concentration AND criticality formula copy incl. the equal-weight limit.
    - npm-registered test file green.
  </acceptance_criteria>
  <done>Profile shows derived-badged concentration; chokepoints panel + graph highlight live; Methodology documents both formulas and the l.v=constant limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset → DOM (innerHTML) | Supplier labels + scores interpolated into profile/chokepoints panels and badge HTML |
| derived badge → external link | Methodology URL rendered as an anchor href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | new panel innerHTML (supplier labels, scores) | mitigate | escapeHtml every interpolated supplier label + score before innerHTML (reuse js/ui:468 pattern); scores are integers from pure math |
| T-06-04 | Tampering | derived badge source link | mitigate | Reuse badgeHtml's existing url.startsWith("http") guard + rel="noopener noreferrer"; non-http methodology anchor degrades to label-only |
| T-06-05 | Spoofing | derived number presented as observed | mitigate | provenanceFor never returns observed for derived input; badgeHtml maps derived→"Derived" (test-enforced) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages this phase |
</threat_model>

<verification>
- `node --test tests/criticality-wiring.test.mjs` exits 0 (derived-provenance + ui/viz/html string-wiring assertions green).
- `npm test` total green (214 prior + analytics tests), 0 fail.
- index.html Methodology modal contains both formulas + the equal-weight limit; profile renders a Derived badge (not Observed) on the concentration line.
</verification>

<success_criteria>
- DEPTH-01: concentration displayed per company with a Derived badge + Methodology link; Methodology documents the formula + sector reuse.
- DEPTH-02: Critical chokepoints panel surfaces real fan-in ranking and highlights single-points in the graph; d.bn left intact as a separate signal.
- No derived number is ever tagged observed; all interpolated labels escaped.
</success_criteria>

<output>
Create `.planning/phases/06-concentration-risk-analytics/06-02-SUMMARY.md` when done.
</output>
