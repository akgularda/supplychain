---
phase: 08-interaction-performance
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/no-restart-invariant.test.mjs
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "Every simple filter/style/highlight handler is proven (at the source level) to contain no full-simulation restart or re-render call"
    - "The legitimate reheat in bReset (alpha(0.22).restart()) is allow-listed and asserted to remain, documenting the invariant boundary"
    - "highlightBy and resetHighlight in js/viz are proven opacity-only"
  artifacts:
    - path: "tests/no-restart-invariant.test.mjs"
      provides: "source-level guard over the simple-change handler bodies + allow-list proof"
      contains: "doesNotMatch"
  key_links:
    - from: "tests/no-restart-invariant.test.mjs"
      to: "js/ui/index.js + js/viz/index.js handler bodies"
      via: "readFileSync + brace-matched body slice + BANNED regex set"
      pattern: "forceSimulation|\\.alpha\\(|\\.restart\\(|updateGraph|render\\("
---

<objective>
Lock the existing "simple changes never restart the simulation" behavior behind a source-level guard
test so it cannot silently regress. Slice the body of each simple-change handler (filter apply/reset,
the bLabels/bFlow/bBottlenecks toggles, the keydown l/f/b cases, highlightChokepoints) plus the viz
highlight primitives (highlightBy/resetHighlight) and assert NONE contain a banned restart/re-render
call. Explicitly ALLOW-LIST bReset's legitimate alpha(0.22).restart() and the render()/updateGraph()
reheats so the guard targets simple changes only — not a blanket ban.

Purpose: PERF-01 — guarantee + assert the opacity-only highlight path. No source behavior changes; this
plan adds ONE test file (already registered in package.json by Plan 01 Task 0).
Output: tests/no-restart-invariant.test.mjs.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-interaction-performance/08-RESEARCH.md

<read_first>
- 08-RESEARCH.md "Source-level guard test skeleton" (the bodyOf brace-matcher + BANNED regex set) and
  Pitfall 3 (scope to simple-change handler bodies; allow-list bReset + render/updateGraph).
- js/ui/index.js — the simple-change handlers and their exact shapes:
  * function applyFilters() at L523 — ends in highlightBy(fn) + style.display + syncUrlState(); no restart.
  * function resetFilters() at L547 — calls resetHighlight(); no restart.
  * function highlightChokepoints() at L659 — calls highlightBy(predicate); no restart.
  * INLINE arrow handlers inside wireUI: bLabels.onclick at L1115 (attr opacity only), bFlow.onclick
    at L1120 (=> toggleParticles()), bBottlenecks.onclick at L1121 (highlightBy only).
  * keydown switch cases at L1093 'l' (labelSel/subSel attr), L1094 'f' toggleParticles(), L1095 'b'
    highlightBy.
  * ALLOW-LIST: bReset.onclick at L1103-1114 contains `STATE.simulation.alpha(0.22).restart()` (L1108)
    — the legitimate reheat the guard must NOT ban and MUST assert remains.
- js/viz/index.js — function highlightBy( at L385 and function resetHighlight( at L375 are opacity-only
  (transition().attr fill-opacity/stroke-opacity); contain no restart/updateGraph/render.
- package.json — tests/no-restart-invariant.test.mjs is ALREADY registered (Plan 01 Task 0). Do not re-edit.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/no-restart-invariant.test.mjs guarding the simple-change handler bodies</name>
  <files>tests/no-restart-invariant.test.mjs</files>
  <read_first>08-RESEARCH.md guard skeleton (bodyOf + BANNED = [/\bd3\.forceSimulation\s*\(/, /\.alpha\s*\(/, /\.restart\s*\(/, /\bupdateGraph\s*\(/, /\brender\s*\(/]) and the L-numbers above for the inline-handler + keydown-case slicing.</read_first>
  <action>Create tests/no-restart-invariant.test.mjs. readFileSync js/ui/index.js (UI) and js/viz/index.js (VIZ). Define a bodyOf(src, signature) brace-matcher (find signature, slice from its first "{" to the matching "}") per the RESEARCH skeleton, and the BANNED regex array. For the NAMED-function handlers iterate ["function applyFilters(", "function resetFilters(", "function highlightChokepoints("] over UI and assert each body doesNotMatch every BANNED regex. For the VIZ primitives iterate ["function highlightBy(", "function resetHighlight("] over VIZ and assert opacity-only (doesNotMatch BANNED). For the INLINE wireUI handlers, slice each onclick arrow body — `document.getElementById("bLabels").onclick = () => {`, `document.getElementById("bBottlenecks").onclick = () => {` — via bodyOf and assert doesNotMatch BANNED (bLabels/bBottlenecks bodies only touch attr/highlightBy). For bFlow assert its single statement is a toggleParticles() call (match `bFlow"\).onclick = () => toggleParticles()` and doesNotMatch BANNED). For the keydown cases assert the substring between `case 'l':`/`case 'f':`/`case 'b':` and the next `break;` doesNotMatch BANNED (l = labelSel/subSel.attr, f = toggleParticles(), b = highlightBy). ALLOW-LIST PROOF: assert the whole UI source DOES match /alpha\(0\.22\)\.restart\(\)/ (bReset's legitimate reheat must remain) — this documents the boundary and guards against the reheat being accidentally deleted. Do NOT modify js/ui or js/viz — this is a verification-only plan.</action>
  <acceptance_criteria>
    - tests/no-restart-invariant.test.mjs exists; reads js/ui/index.js and js/viz/index.js via readFileSync.
    - applyFilters / resetFilters / highlightChokepoints bodies + highlightBy / resetHighlight (viz) +
      the bLabels/bBottlenecks inline bodies + the l/f/b keydown cases all doesNotMatch the BANNED set
      (no forceSimulation(/.alpha(/.restart(/updateGraph(/render( in any simple-change path).
    - bFlow handler proven to be a bare toggleParticles() call.
    - Allow-list assertion passes: UI source matches /alpha\(0\.22\)\.restart\(\)/ (bReset reheat present).
    - No edits to js/ui/index.js or js/viz/index.js.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/no-restart-invariant.test.mjs && npm test</automated>
  </verify>
  <done>Guard test GREEN; simple-change handlers proven restart-free; bReset reheat allow-listed; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard scope → handler set | A guard too broad fails on legitimate reheats; too narrow lets a regression slip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | guard over-reach banning the legitimate bReset/render reheat | mitigate | Scope assertions to the SLICED bodies of simple-change handlers only; explicit allow-list assertion proves bReset's alpha(0.22).restart() remains (RESEARCH Pitfall 3). |
| T-08-04 | Repudiation | a future regression adds a restart to a handler NOT in the guarded set | accept | Handler set enumerated from wireUI (RESEARCH A3, MEDIUM risk); reviewed against L1093-1124 + named fns. Adding handlers to the list is cheap if new simple-change handlers appear. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no packages installed (Node built-ins only). |
</threat_model>

<verification>
- `node --test tests/no-restart-invariant.test.mjs` GREEN.
- `npm test` exit 0 (no source edits; suite unchanged count + this guard).
</verification>

<success_criteria>
- Every simple filter/style/highlight handler body proven free of forceSimulation(/.alpha(/.restart(/updateGraph(/render(.
- highlightBy + resetHighlight proven opacity-only.
- bReset's legitimate reheat allow-listed and asserted present.
</success_criteria>

<output>
Create `.planning/phases/08-interaction-performance/08-02-SUMMARY.md` when done.
</output>
