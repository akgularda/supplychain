---
phase: 05-hero-moment-investor-narrative
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - index.html
  - js/ui/index.js
  - js/main.js
autonomous: true
requirements: [STORY-02, STORY-04, STORY-05]

must_haves:
  truths:
    - "First-visit (heroSeen !== '1') auto-plays the hero tour; the Quick-Start onboarding does not double-overlay during it"
    - "A #bTour button near #bHelp replays the tour on click regardless of heroSeen"
    - "ESC and #heroSkip both skip the tour (write heroSeen, reset highlight, hide overlay)"
    - "Overlay caption card shows step title, caption, and Step n/N progress; Next/Prev/Pause/Skip wired"
    - "All 89 existing IDs and the inline bootstrap <script> remain intact (index-ui-integrity green)"
    - "Full npm test suite is green including the two new Phase-5 files"
  artifacts:
    - path: "index.html"
      provides: "#heroOverlay (role=dialog, aria-modal) + #heroTitle/#heroCaption/#heroProgress/#heroNext/#heroPrev/#heroPause/#heroSkip + #bTour"
      contains: "id=\"heroOverlay\""
    - path: "js/ui/index.js"
      provides: "openGlobal added to module exports (needed as injected control)"
      contains: "openGlobal"
    - path: "js/main.js"
      provides: "Hero wiring: buildNarrative+createHeroController init, controls/render/storage/timers/reducedMotion injection, heroSeen gate, #bTour + ESC + skip wiring"
      contains: "createHeroController"
  key_links:
    - from: "js/main.js"
      to: "js/ui/narrative.js"
      via: "import { buildNarrative, createHeroController }"
      pattern: "buildNarrative.*createHeroController|narrative\\.js"
    - from: "js/main.js"
      to: "safeReadFlag/safeWriteFlag"
      via: "storage injection + first-visit gate on heroSeen"
      pattern: "heroSeen"
    - from: "index.html #bTour"
      to: "controller.play"
      via: "click handler wired in main.js"
      pattern: "bTour"
    - from: "js/main.js render callback"
      to: "#heroTitle/#heroCaption/#heroProgress/#heroOverlay"
      via: "textContent writes + overlay show/hide"
      pattern: "heroCaption|heroOverlay"
---

<objective>
Wire the hero tour into the live page: add the narration overlay + replay button to `index.html` (NEW IDs only — preserve all 89 existing IDs and the inline bootstrap so `index-ui-integrity` stays green, RESEARCH Pitfall 2), and connect the pure narrative engine in `js/main.js`.

Wiring (RESEARCH Module/Test Seam + Mechanics): export `openGlobal` from `js/ui/index.js` (it is defined but currently neither exported nor on window — required as an injected control); in `js/main.js` import `buildNarrative`/`createHeroController` and `safeReadFlag`/`safeWriteFlag`, plus `openGlobal`/`openProfile` (ui) and `highlightBy`/`resetHighlight` (viz). Build `controls = { openGlobal, openProfile, highlightBy, resetHighlight }`, a `render(step,index,total)` callback that writes captions via textContent (T-05-01 — no innerHTML) and toggles `#heroOverlay`, `storage = { read: safeReadFlag, write: safeWriteFlag }`, `reducedMotion = () => matchMedia('(prefers-reduced-motion: reduce)').matches` (RESEARCH canonical pattern), `timers = { setTimeout: window.setTimeout.bind(window), clearTimeout: window.clearTimeout.bind(window) }`. Gate first play on `heroSeen !== '1'` and suppress the Quick-Start during an active first-visit tour (RESEARCH OQ1: hero first, Quick-Start after — planner decision). Wire `#bTour` -> `controller.play()` (replay always allowed), `#heroNext/#heroPrev/#heroPause/#heroSkip` to the controller, and route ESC -> `skip()` (reuse the existing global keydown ESC switch pattern, ui/index.js).

Purpose: Deliver the user-facing "first 30 seconds" hero (STORY-02) driving the data narrative (STORY-04) and close the regression gate (STORY-05).
Output: updated `index.html`, `js/ui/index.js`, `js/main.js`; full suite green.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md
@.planning/phases/05-hero-moment-investor-narrative/05-01-SUMMARY.md
@.planning/phases/05-hero-moment-investor-narrative/05-02-SUMMARY.md

<interfaces>
<!-- Verified in-repo this session -->

js/ui/index.js:
  - openGlobal() defined at :249 but NOT in the export block at :1013-1018 and NOT on window — ADD it to the export list.
  - openProfile(symbol) IS exported. maybeShowOnboarding() exported. wireUI() at :795 holds onclick wiring + global keydown ESC switch (~:896-919).
  - Modal/focus/ESC infra (openModal/closeModal/trapFocus + global keydown) already present — reuse for ESC->skip.

js/viz/index.js exports (:741): highlightBy(fn), resetHighlight(), render, ... (matchMedia reduced-motion at :40-42).

js/state.js exports (:98): safeReadFlag(key), safeWriteFlag(key,value).

js/main.js init order (verified): import ui {...} (NOT openGlobal currently) -> wireUI() (:37) -> render() (:52) -> maybeShowOnboarding() (:53) -> updateStatusIndicator() -> renderTop10List(). DATA imported from ./data/index.js. window.SUPPLY_MAP_DATA holds runtime data; buildNarrative needs nodes/meta/layers (use DATA / window.SUPPLY_MAP_DATA).

index.html (verified): #bar at line 154; #bMethodology (:165) and #bHelp (:166) are the last toolbar buttons — append #bTour adjacent. Modals (#onboardingPanel :24, #helpModal :37) use role="dialog" aria-modal="true". Inline bootstrap <script> at :308-310 (a comment-only block asserted by index-ui-integrity) and `<script type="module" src="js/main.js">` at :307 — DO NOT modify either.

index-ui-integrity asserts (verified): one inline script block exists + parses (node --check on extracted block); regex assert.match for specific IDs (#helpModal/#compareModal with role+aria-modal, #fatalError, #onboardingPanel, #cardRatings, #cardOverlap, #cardTimeline, #provenanceDrawer, #searchSuggest, #mobileToggle, #mobileSheet, etc.). Appending new markup + new IDs does NOT break these regex/syntax checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hero overlay markup + #bTour to index.html (new IDs only)</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 24-50 (#onboardingPanel/#helpModal dialog markup pattern), 154-166 (#bar toolbar + #bMethodology/#bHelp), 305-311 (module script + inline bootstrap — must stay untouched)
    - tests/index-ui-integrity.test.mjs (which IDs/markup it regex-asserts)
    - tests/hero-wiring.test.mjs (the overlay/#bTour IDs it requires)
  </read_first>
  <action>
    Append a #bTour button inside #bar adjacent to #bHelp: a labelled button (e.g. "Take the tour") with aria-label, using the same button styling conventions as neighbours. Do NOT rename or move #bHelp/#bMethodology or any existing toolbar ID.

    Add a #heroOverlay caption card near the modal markup (after #helpModal/#compareModal region, before the inline scripts) with role="dialog" aria-modal="true" aria-labelledby="heroTitle" tabindex="-1", initially hidden. Inside it add #heroTitle, #heroCaption, #heroProgress (the "Step n/N" indicator), and control buttons #heroPrev, #heroNext, #heroPause, #heroSkip with aria-labels. Use ONLY these new IDs — introduce NO collision with the 89 existing IDs (RESEARCH Markup section lists them). Leave the `<script type="module" src="js/main.js">` tag and the inline bootstrap comment <script> at the end of the file exactly as-is (index-ui-integrity asserts they exist + parse).

    Add minimal overlay styling reusing existing design tokens (var(--...)) consistent with the modal/onboarding look; no Phase-4-level polish required (visual refinement is acceptable but not gated). Do not introduce inline onclick handlers — wiring happens in main.js (Task 3).
  </action>
  <verify>
    <automated>node --test tests/index-ui-integrity.test.mjs && node --test tests/hero-wiring.test.mjs 2>&1 | grep -q heroOverlay; node -e "const h=require('fs').readFileSync('index.html','utf8');for(const id of ['heroOverlay','heroTitle','heroCaption','heroProgress','heroNext','heroPrev','heroPause','heroSkip','bTour'])if(!h.includes('id=\"'+id+'\"'))throw new Error('missing '+id);if(!/id=\"heroOverlay\"[^>]*role=\"dialog\"[^>]*aria-modal=\"true\"/.test(h))throw new Error('heroOverlay role/aria');if(!/<script[^>]*src=\"js\/main\.js\"/.test(h))throw new Error('module script removed')"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 new IDs present; #heroOverlay has role="dialog" aria-modal="true"
    - index-ui-integrity green (inline bootstrap + all existing IDs intact)
    - module script tag preserved; no inline onclick added
  </acceptance_criteria>
  <done>Overlay + replay button added with new IDs only; existing IDs and bootstrap preserved; integrity test green.</done>
</task>

<task type="auto">
  <name>Task 2: Export openGlobal from js/ui/index.js</name>
  <files>js/ui/index.js</files>
  <read_first>
    - js/ui/index.js lines 249 (openGlobal definition) and 1013-1018 (export block)
  </read_first>
  <action>
    Add `openGlobal` to the existing export block at the end of js/ui/index.js (it is defined at :249 but currently not exported — main.js needs it as an injected control). Make no other change; do not touch internal usages (#bBack onclick, keydown 'g', wireMobile) which reference the in-module function directly.
  </action>
  <verify>
    <automated>node --check js/ui/index.js && node -e "const s=require('fs').readFileSync('js/ui/index.js','utf8');const m=s.match(/export\s*\{[\s\S]*?\}/);if(!m||!/\bopenGlobal\b/.test(m[0]))throw new Error('openGlobal not exported')"</automated>
  </verify>
  <acceptance_criteria>
    - openGlobal appears in the js/ui/index.js export block
    - File passes node --check; no other export removed
  </acceptance_criteria>
  <done>openGlobal is an importable named export ready for injection into the hero controls.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the hero controller in js/main.js + close the full suite</name>
  <files>js/main.js</files>
  <read_first>
    - js/main.js (full — import blocks :5-13, init order :36-56)
    - js/ui/narrative.js (buildNarrative + createHeroController signatures from Plans 01/02)
    - .planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md (Module/Test Seam wiring + Mechanics 1-5 + OQ1 precedence)
    - tests/hero-wiring.test.mjs (the main.js wiring strings it asserts: heroSeen via safeReadFlag/safeWriteFlag, #bTour)
  </read_first>
  <action>
    In js/main.js: add imports for buildNarrative + createHeroController from ./ui/narrative.js; add safeReadFlag + safeWriteFlag from ./state.js; add openGlobal (now exported) + openProfile to the ./ui/index.js import; add highlightBy + resetHighlight from ./viz/index.js.

    After render()/wireUI(), build the hero: data = window.SUPPLY_MAP_DATA || DATA; steps = buildNarrative(data); controls = { openGlobal, openProfile, highlightBy, resetHighlight }; storage = { read: safeReadFlag, write: safeWriteFlag }; reducedMotion = () => (typeof window!=='undefined' && window.matchMedia) ? window.matchMedia('(prefers-reduced-motion: reduce)').matches : false (RESEARCH canonical pattern); timers = { setTimeout: window.setTimeout.bind(window), clearTimeout: window.clearTimeout.bind(window) }; render = (step,index,total) => { const o=document.getElementById('heroOverlay'); if(!step){ o.hidden=true; return;} document.getElementById('heroTitle').textContent=step.title; document.getElementById('heroCaption').textContent=step.caption; document.getElementById('heroProgress').textContent='Step '+(index+1)+'/'+total; o.hidden=false; } — use textContent only (T-05-01, no innerHTML). controller = createHeroController({ steps, controls, storage, reducedMotion, timers, render }).

    First-visit gate: if safeReadFlag('heroSeen') !== '1' call controller.play() and SUPPRESS the Quick-Start during the tour (RESEARCH OQ1 — hero first; only call maybeShowOnboarding() when not auto-playing the hero, e.g. guard the existing maybeShowOnboarding() call so it does not run simultaneously with a fresh-visit auto tour). Wire #bTour onclick -> controller.play() (replay always allowed, independent of heroSeen). Wire #heroNext->next, #heroPrev->prev, #heroPause->pause, #heroSkip->skip. Route ESC to controller.skip() while the overlay is visible — reuse the existing global keydown handling pattern rather than adding a competing listener where practical; a scoped keydown that calls skip() when #heroOverlay is visible is acceptable.

    Keep the init order otherwise unchanged; do not alter the existing window error shims.
  </action>
  <verify>
    <automated>node --check js/main.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - npm test fully green INCLUDING tests/narrative.test.mjs and tests/hero-wiring.test.mjs (hero-wiring no longer RED)
    - main.js references heroSeen via safeReadFlag/safeWriteFlag and wires #bTour, #heroNext/#heroPrev/#heroPause/#heroSkip, and ESC->skip
    - render callback writes captions with textContent (no innerHTML); overlay hidden when render(null)
    - Quick-Start does not double-overlay during a first-visit auto tour
  </acceptance_criteria>
  <done>Hero plays on first visit, is skippable (ESC + Skip) and replayable (#bTour), honors reduced motion, drives the real data narrative; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data -> DOM caption render | Company names from frozen data rendered into overlay text |
| localStorage <-> first-visit gate | heroSeen read/written to decide autoplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/Info-disclosure | #heroCaption/#heroTitle render | mitigate | render callback uses textContent only (never innerHTML) for title/caption/progress (RESEARCH Security V5). |
| T-05-02 | Tampering | npm installs | accept | No packages installed; package.json dependencies unchanged. |
| T-05-05 | Spoofing/Tampering | index-ui-integrity regression | mitigate | New IDs only; inline bootstrap + module script + 89 existing IDs preserved; verified by integrity test in CI gate. |
</threat_model>

<verification>
- `npm test` fully green: narrative + controller + hero-wiring + all 16 prior suites (index-ui-integrity preserved).
- index.html exposes #heroOverlay (dialog) + #bTour + the 8 hero control/label IDs; inline bootstrap intact.
- Manual/Playwright smoke (per VALIDATION, pre-verification): first load autoplays the 4-step tour; Skip + ESC exit and leave the map un-dimmed; #bTour replays; reduced-motion shows captions with no auto-advance.
</verification>

<success_criteria>
- Hero auto-plays on first visit, skippable (ESC/Skip) and replayable (#bTour), reduced-motion aware.
- Steps drive openGlobal/highlightBy/openProfile with real data captions.
- All 89 IDs + inline bootstrap preserved; full suite green including the two new files.
- No new dependencies; buildless static deploy preserved.
</success_criteria>

<output>
Create `.planning/phases/05-hero-moment-investor-narrative/05-03-SUMMARY.md` when done
</output>
