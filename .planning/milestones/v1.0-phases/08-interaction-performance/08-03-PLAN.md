---
phase: 08-interaction-performance
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/perf/_interaction-bench.cjs
  - docs/perf/interaction-2026-06-21.md
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "A Node micro-benchmark measures cold-cache vs warm-cache cost of the memoized analytics over N profile opens + repeated criticality/scenario calls"
    - "The recorded warm-cache timing is measurably faster than cold, demonstrating PERF-01 SC2 against the Phase-1 baseline context"
    - "The latency record is committed under docs/perf/ with honest numbers and the documented local-no-paint caveat"
  artifacts:
    - path: "docs/perf/_interaction-bench.cjs"
      provides: "cold-vs-warm micro-benchmark of buildSupplierFanIn/companyConcentration/supplierCriticality/runScenario using performance.now(); NOT registered in scripts.test"
      contains: "performance.now"
    - path: "docs/perf/interaction-2026-06-21.md"
      provides: "recorded before/after interaction-cost numbers + method + caveat (PERF-01 SC2)"
      min_lines: 20
  key_links:
    - from: "docs/perf/_interaction-bench.cjs"
      to: "js/analytics/index.js (__resetAnalyticsCache)"
      via: "import + cold run after reset, then warm run, both timed"
      pattern: "__resetAnalyticsCache"
---

<objective>
Record an honest, environment-robust latency improvement for PERF-01 SC2. The page does not paint
locally (stale data snapshot → NO_FCP, documented in the Phase-1 baseline), so the authoritative
measurement is a Node micro-benchmark of the pure analytics: time the cold path (cache cleared, fan-in
rebuilt every call) vs the warm path (memoized) over N profile opens plus repeated criticality and
scenario calls, using performance.now(). Record the delta in docs/perf/.

Purpose: PERF-01 SC2 — measurable interaction-latency improvement vs the Phase-1 baseline, recorded.
Output: docs/perf/_interaction-bench.cjs (the script, kept OUT of scripts.test) + docs/perf/interaction-2026-06-21.md (the record).
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-interaction-performance/08-RESEARCH.md

<read_first>
- 08-RESEARCH.md Pitfall 4 (page doesn't paint locally → prefer a Node micro-bench of the pure functions;
  cold vs warm, N iterations, performance.now()) and Open Questions #2 (record the Node micro-bench as
  the authoritative SC2 number; in-page capture optional/best-effort).
- docs/perf/baseline-2026-06-20.md — the Phase-1 baseline doc + its documented NO_FCP / "page does not
  paint locally" condition to cite as the caveat.
- docs/perf/_perf-capture.cjs — the existing Playwright harness shape (navigation/paint timing only);
  reference it as the supplementary best-effort path, do not depend on it painting.
- js/analytics/index.js — after Plan 01: buildSupplierFanIn, companyConcentration, supplierCriticality,
  runScenario, SCENARIO_PRESETS, __resetAnalyticsCache, __memoStats (the cold/warm toggle the bench drives).
- data/top100-map.json — the real fixture (profiles + nodes) the bench loads.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/perf/_interaction-bench.cjs — cold-vs-warm micro-benchmark of the memoized analytics</name>
  <files>docs/perf/_interaction-bench.cjs</files>
  <read_first>js/analytics/index.js exports (after Plan 01) + data/top100-map.json shape (profiles keyed by symbol, nodes array with marketcap). The file is run with `node docs/perf/_interaction-bench.cjs`; import the ESM analytics via dynamic import() since the file is CommonJS.</read_first>
  <action>Create docs/perf/_interaction-bench.cjs. Dynamically import("../../js/analytics/index.js") (resolve a file URL from __dirname via pathToFileURL) to get the named exports; JSON.parse(fs.readFileSync) data/top100-map.json for profiles + nodes. Build a representative workload: open every profile (companyConcentration(symbol,{profiles}) for each symbol), call supplierCriticality({profiles,limit:8}) about 50 times, and runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption,{profiles,nodes}) about 50 times — mirroring real interaction (profile opens + chokepoints render + scenario runs). COLD run: call __resetAnalyticsCache() before EACH companyConcentration/criticality/scenario call so the fan-in rebuilds every time (simulates the pre-memo eager default-arg behavior); time the whole workload with performance.now(). WARM run: __resetAnalyticsCache() ONCE, then run the same workload (memoized); time it. Use a small warmup pass + median-of-5 for each to reduce noise. console.log a JSON block: { profiles, coldMs, warmMs, speedup: coldMs/warmMs, fanInBuilds_warm: __memoStats().fanIn.builds, anchors: { GILD: companyConcentration("GILD",{profiles}).score, NVDA: companyConcentration("NVDA",{profiles}).score, taiwan: runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption,{profiles,nodes}).impactedCompanies.length } } so the recorded numbers also re-prove the anchors live. Keep this file OUT of package.json scripts.test (it is tooling, not a unit test).</action>
  <acceptance_criteria>
    - `node docs/perf/_interaction-bench.cjs` runs and prints a JSON block with coldMs, warmMs, speedup.
    - warmMs < coldMs (warm cache measurably faster); speedup > 1.
    - The block re-prints the live anchors (GILD=36, NVDA=12, Taiwan=7) confirming values unchanged.
    - File is NOT added to package.json scripts.test.
  </acceptance_criteria>
  <verify>
    <automated>node docs/perf/_interaction-bench.cjs | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s.slice(s.indexOf('{')));if(j.warmMs>=j.coldMs)process.exit(1);if(j.anchors.GILD!==36||j.anchors.NVDA!==12||j.anchors.taiwan!==7)process.exit(1);console.log('OK speedup='+j.speedup.toFixed(2))})"</automated>
  </verify>
  <done>Bench runs, warm faster than cold, anchors live-confirmed, not registered as a test.</done>
</task>

<task type="auto">
  <name>Task 2: Record the latency result in docs/perf/interaction-2026-06-21.md (PERF-01 SC2)</name>
  <files>docs/perf/interaction-2026-06-21.md</files>
  <read_first>The JSON output from Task 1's bench run; docs/perf/baseline-2026-06-20.md for the Phase-1 baseline framing + the NO_FCP local-no-paint caveat to cite.</read_first>
  <action>Run `node docs/perf/_interaction-bench.cjs`, capture the JSON, and write docs/perf/interaction-2026-06-21.md recording: (1) the method — Node micro-benchmark of the pure analytics, cold (cache reset each call → fan-in rebuilt) vs warm (memoized), median-of-5, N = number of profiles opened + 50 criticality + 50 scenario calls; (2) the measured numbers — coldMs, warmMs, speedup, warm fan-in builds; (3) the SC2 conclusion — warm-cache interaction cost is measurably lower, the memoization win; (4) the honest CAVEAT — the in-browser FCP/paint number is unmeasurable locally (committed data snapshot lacks nodes/links/profiles → NO_FCP, documented in baseline-2026-06-20.md), so the Node micro-bench is the authoritative SC2 measurement per 08-RESEARCH (Pitfall 4 / OQ2); the Playwright in-page capture (_perf-capture.cjs) remains a best-effort supplement that requires synthetic-data injection to paint. Cite real numbers from the bench — do not fabricate. No fenced JS; a results table + prose is fine.</action>
  <acceptance_criteria>
    - docs/perf/interaction-2026-06-21.md exists with the method, the real coldMs/warmMs/speedup numbers, the SC2 conclusion, and the local-no-paint caveat citing baseline-2026-06-20.md.
    - Numbers match a real bench run (warm < cold); no fabricated figures.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/perf/interaction-2026-06-21.md','utf8');const ok=/warm/i.test(t)&&/cold/i.test(t)&&/speedup|faster/i.test(t)&&/NO_FCP|paint|baseline-2026-06-20/i.test(t);process.exit(ok?0:1)"</automated>
  </verify>
  <done>Latency record committed under docs/perf/ with real numbers + honest caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded numbers → repo claim | A fabricated or unmeasured figure would misrepresent PERF-01 SC2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Repudiation | latency doc figures not backed by a real run | mitigate | Doc numbers must come from a live `node docs/perf/_interaction-bench.cjs` run; the bench re-prints live anchors so the record is self-verifying; caveat cites the documented local NO_FCP condition. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no packages installed; Node built-ins (performance.now, fs) + already-installed tooling only. |
</threat_model>

<verification>
- `node docs/perf/_interaction-bench.cjs` prints coldMs/warmMs/speedup with warm < cold and live anchors GILD=36/NVDA=12/Taiwan=7.
- docs/perf/interaction-2026-06-21.md records the real numbers + method + local-no-paint caveat.
</verification>

<success_criteria>
- Node micro-bench measures cold vs warm analytics cost; warm measurably faster (speedup > 1).
- Latency improvement recorded under docs/perf/ with honest numbers and the documented caveat (PERF-01 SC2).
- Bench file kept out of scripts.test (tooling, not a unit test).
</success_criteria>

<output>
Create `.planning/phases/08-interaction-performance/08-03-SUMMARY.md` when done.
</output>
