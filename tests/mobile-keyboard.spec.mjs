import test from "node:test";

// Placeholder registered in package.json scripts.test (Phase 09-01).
// The full Playwright 390x844 mobile + keyboard-only journey is authored in
// Phase 09-03. Until then this file is a registered no-op so the suite count
// stays stable and `npm test` remains green. See 09-RESEARCH.md "Wave 0 Gaps".
test("mobile-keyboard Playwright smoke (deferred to 09-03)", { skip: true }, () => {});
