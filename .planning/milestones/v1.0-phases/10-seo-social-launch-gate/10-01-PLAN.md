---
phase: 10-seo-social-launch-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/og-card.png
  - scripts/make-og-card.mjs
  - tests/seo-social.test.mjs
  - package.json
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "index.html <head> carries a meta description, canonical, full og:*, twitter:*, and a parseable JSON-LD block"
    - "A real 1200x630 PNG social card exists at assets/og-card.png"
    - "Sharing the Pages URL renders a large social card with a fetchable absolute og:image"
    - "tests/seo-social.test.mjs is registered in package.json scripts.test and the full suite stays green"
    - "index-ui-integrity stays green (inline bootstrap + asserted IDs untouched, JSON-LD is not a bare script)"
  artifacts:
    - path: "index.html"
      provides: "SEO/OG/Twitter/JSON-LD head tags"
      contains: "rel=\"canonical\""
    - path: "assets/og-card.png"
      provides: "1200x630 social share image"
    - path: "tests/seo-social.test.mjs"
      provides: "metadata + image + deploy assertions"
      min_lines: 30
    - path: "package.json"
      provides: "test registration"
      contains: "tests/seo-social.test.mjs"
  key_links:
    - from: "index.html"
      to: "https://akgularda.github.io/supplychain/assets/og-card.png"
      via: "og:image / twitter:image absolute URL"
      pattern: "og:image"
    - from: "tests/seo-social.test.mjs"
      to: "assets/og-card.png"
      via: "fs.existsSync + PNG header dimension read"
      pattern: "og-card\\.png"
    - from: "tests/seo-social.test.mjs"
      to: ".github/workflows/deploy-pages.yml"
      via: "asserts cp -R assets _site/"
      pattern: "cp -R assets"
---

## Phase Goal

**As a** prospective investor discovering or sharing the site, **I want to** see valid SEO metadata and a clean social share card when the URL is posted or crawled, **so that** the tool looks credible and discoverable before I even open it.

<objective>
Add the full SEO/social metadata tag set to `index.html <head>` (PERF-04), generate the real 1200x630 `assets/og-card.png`, and author + register `tests/seo-social.test.mjs` that pins every tag, the image, and the deploy path.

Purpose: The site must be discoverable by crawlers and render a proper large social card when shared (Slack/Twitter/iMessage), with honest copy and no false claims.
Output: Updated `index.html <head>`, `assets/og-card.png` (committed), `scripts/make-og-card.mjs` (generator), `tests/seo-social.test.mjs`, updated `package.json`.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-seo-social-launch-gate/10-RESEARCH.md

<interfaces>
<!-- Exact values the executor must use verbatim. From 10-RESEARCH.md (VERIFIED). -->

Canonical base: https://akgularda.github.io/supplychain/
og:image absolute: https://akgularda.github.io/supplychain/assets/og-card.png
JSON-LD logo absolute: https://akgularda.github.io/supplychain/assets/monarch-logo.png

index.html head (existing, KEEP untouched):
- <meta charset="UTF-8"> + <meta name="viewport" ...> (lines 4-5)
- <title>Monarch Castle Technologies | Market Intelligence</title> (line 6)
- <link rel="icon" type="image/svg+xml" href="favicon.svg"> (line 7)
- d3 CDN <script>, ./data/top100-map.js, ./data/credit-ratings.js, four stylesheet links (lines 8-14)

index-ui-integrity invariants (must stay green):
- Asserted body IDs (count 1 each): helpModal, compareModal, fatalError, onboardingPanel,
  cardRatings, cardOverlap, cardTimeline, provenanceDrawer, searchSuggest, mobileToggle, mobileSheet
- Inline-bootstrap regex /<script>([\s\S]*?)<\/script>\s*<\/body>/i matches the LAST attribute-less
  <script> before </body> (the line-373 bootstrap). JSON-LD MUST carry type="application/ld+json"
  and live in <head> so it does NOT match that pattern.
</interfaces>

The exact tag block, honest copy, and test code live in 10-RESEARCH.md (sections "Exact <head> Tag Block to Add" and "Code Examples"). Use them verbatim.
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Author and register the SEO/social validation test</name>
  <files>tests/seo-social.test.mjs, package.json</files>
  <read_first>
    - .planning/phases/10-seo-social-launch-gate/10-RESEARCH.md ("Code Examples" — the full tests/seo-social.test.mjs)
    - tests/index-ui-integrity.test.mjs (established string/regex-on-raw-file pattern to mirror)
    - package.json (scripts.test explicit file list — append, do not replace)
  </read_first>
  <acceptance_criteria>
    - tests/seo-social.test.mjs exists and uses node:test + node:assert/strict + node:fs (string/regex on raw index.html, no DOM dependency).
    - Asserts: substantive meta description; canonical == https://akgularda.github.io/supplychain/; all required og:* (og:type, og:site_name, og:title, og:description, og:url, og:image) present with og:image == the absolute Pages URL; twitter:card == summary_large_image plus twitter:title/description/image; a type="application/ld+json" block that JSON.parse-es, has @context == https://schema.org and url == canonical; assets/og-card.png exists with PNG-header width 1200 and height 630; .github/workflows/deploy-pages.yml still contains `cp -R assets _site/`.
    - package.json scripts.test has tests/seo-social.test.mjs appended to the explicit file list (suite remains a single node --test invocation).
  </acceptance_criteria>
  <action>Create tests/seo-social.test.mjs verbatim from the 10-RESEARCH.md "Code Examples" block (mirrors the index-ui-integrity string-assertion pattern; no new dependency). Then append tests/seo-social.test.mjs to the end of the existing package.json scripts.test file list — preserve every existing file in order. The test is expected to FAIL now (RED): index.html has no tags yet and assets/og-card.png does not exist. Do NOT weaken any assertion to make it pass; Tasks 1 and 2 satisfy it.</action>
  <verify>
    <automated>node --test tests/seo-social.test.mjs 2>&1 | grep -qE "fail|not ok" && echo "RED as expected" ; node -e "const p=require('./package.json'); process.exit(p.scripts.test.includes('tests/seo-social.test.mjs')?0:1)" && echo "REGISTERED"</automated>
  </verify>
  <done>tests/seo-social.test.mjs exists, fails for the right reasons (missing tags + missing image), and is registered in package.json scripts.test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add the SEO/OG/Twitter/JSON-LD tag block to index.html head</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/10-seo-social-launch-gate/10-RESEARCH.md ("Exact <head> Tag Block to Add" + "Honesty notes" + "Why this WON'T break index-ui-integrity")
    - index.html (lines 1-15 head region, and the trailing region before </body>)
  </read_first>
  <behavior>
    - meta description present, honest (no "platform"/"real-time"/hype), >=40 chars.
    - canonical, og:type=website, og:site_name, og:title, og:description, og:url, og:image (absolute), og:image:width=1200, og:image:height=630, og:image:alt all present.
    - twitter:card=summary_large_image, twitter:title, twitter:description, twitter:image present.
    - application/ld+json WebSite+Organization (Monarch Castle Technologies) parses; @context=https://schema.org; url=canonical.
    - index-ui-integrity stays green: no new attribute-less <script> before </body>; all asserted IDs intact.
  </behavior>
  <action>Insert the exact tag block from 10-RESEARCH.md immediately after the existing <title> (line 6) and before the stylesheet links, inside <head>. Use every URL verbatim with the /supplychain/ subpath. The JSON-LD MUST be <script type="application/ld+json"> (never a bare <script>) and stays in <head>. Use the research's honest copy verbatim — it is deliberately non-hype (visualization / market-intelligence map, source-linked provenance + confidence, no live-data claim). Do NOT add <meta name="keywords">. Do NOT remove or duplicate any existing charset/viewport/title/favicon/d3/data/stylesheet tag. Do NOT touch the trailing inline bootstrap or any body content. This implements PERF-04 (metadata half).</action>
  <verify>
    <automated>node --test tests/seo-social.test.mjs tests/index-ui-integrity.test.mjs 2>&1 | grep -qE "^# fail [1-9]" && exit 1 || echo "metadata + integrity green (image test may still fail until Task 2)"</automated>
  </verify>
  <done>All metadata assertions in seo-social.test.mjs pass and index-ui-integrity stays fully green; only the og-card.png image test remains red.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the 1200x630 og-card.png with installed Playwright</name>
  <files>assets/og-card.png, scripts/make-og-card.mjs</files>
  <read_first>
    - .planning/phases/10-seo-social-launch-gate/10-RESEARCH.md ("og:image Plan" + "Optional: generate the card with already-installed Playwright")
    - package.json (confirm playwright ^1.58.2 is the dependency — no new install)
  </read_first>
  <acceptance_criteria>
    - scripts/make-og-card.mjs renders a 1200x630 dark-theme HTML card (eyebrow "Monarch Castle Technologies", title "Market Intelligence", subtitle referencing top-100 supply chains + source-linked provenance) via the installed Playwright chromium at viewport 1200x630 and screenshots to assets/og-card.png.
    - assets/og-card.png is a committed real PNG whose header reports exactly width 1200, height 630.
    - No new package added to package.json dependencies or devDependencies (Playwright is already present).
  </acceptance_criteria>
  <action>Create scripts/make-og-card.mjs from the 10-RESEARCH.md Playwright generator (import { chromium } from "playwright"; setContent the inline card HTML; newPage viewport 1200x630; screenshot to assets/og-card.png). Run it once with node scripts/make-og-card.mjs to produce the committed PNG — the PNG is the deliverable, the script is the reproducible generator. If chromium is not provisioned, run npx playwright install chromium first; if Playwright rendering is unavailable in this environment, fall back to resizing docs/perf/site-screenshot.png to exactly 1200x630 and writing it to assets/og-card.png (do NOT add sharp to package.json — use a Playwright/canvas path or an already-available resizer). assets/ is already shipped by deploy-pages.yml (cp -R assets), so no workflow edit is needed. This completes PERF-04 (image half).</action>
  <verify>
    <automated>node -e "const b=require('fs').readFileSync('assets/og-card.png'); if(b.readUInt32BE(16)!==1200||b.readUInt32BE(20)!==630){console.error('bad dims',b.readUInt32BE(16),b.readUInt32BE(20));process.exit(1)} console.log('1200x630 OK')" && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>assets/og-card.png exists at exactly 1200x630, scripts/make-og-card.mjs reproduces it, and the FULL suite (npm test) is green including tests/seo-social.test.mjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site -> crawler/social-unfurl | Public static metadata + image fetched out-of-context by Slack/Twitter/Google |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | og:* / JSON-LD public metadata | mitigate | Honest, PII-free copy from research; no secrets/tokens in any tag (test asserts exact public strings) |
| T-10-02 | Tampering | index-ui-integrity invariant (inline bootstrap regex) | mitigate | JSON-LD uses type="application/ld+json" in <head>; no bare <script> before </body>; integrity test runs in verify |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Phase adds NO new packages (Playwright already vetted/installed); no slopcheck gate required per research Package Legitimacy Audit |
</threat_model>

<verification>
- `npm test` fully green (294 existing tests + tests/seo-social.test.mjs).
- `node --test tests/index-ui-integrity.test.mjs` green (no head/ID regression).
- `assets/og-card.png` PNG header == 1200x630.
- `tests/seo-social.test.mjs` present in `package.json scripts.test`.
</verification>

<success_criteria>
- All PERF-04 metadata tags present, well-formed, and honest in index.html <head>.
- Real 1200x630 og:image committed at assets/og-card.png and referenced by absolute Pages URL.
- New test registered and full suite green; index-ui-integrity unaffected.
</success_criteria>

<output>
Create `.planning/phases/10-seo-social-launch-gate/10-01-SUMMARY.md` when done.
</output>
