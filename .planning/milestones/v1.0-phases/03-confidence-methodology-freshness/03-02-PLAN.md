---
phase: 03-confidence-methodology-freshness
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - js/trust/index.js
  - js/data/index.js
  - js/viz/index.js
  - tests/confidence-score.test.mjs
  - tests/viz-confidence-wiring.test.mjs
autonomous: true
requirements: [TRUST-03, TRUST-05, TRUST-06]
must_haves:
  truths:
    - "Each figure that shows a provenance badge also shows a 0–100 integer confidence score, weighted by source type and decayed by source age."
    - "observed scores higher than estimated, which scores higher than unknown; an older source yields a strictly lower score than the same figure with a current source (monotonic decay)."
    - "A figure with no resolvable provenance returns the explicit unknown floor — never a fabricated high score."
    - "A source with no parseable year incurs no decay (absence of a date is not staleness) and future-dated/spurious years never push the score above its base."
    - "Node and link tooltips render 'Confidence: NN%' next to the Phase-2 observed/estimated/unknown badge."
    - "The viz render() no longer writes #lastUpdated, so js/ui updateStatusIndicator becomes the sole owner (viz-side half of the dual-owner fix)."
  artifacts:
    - path: "js/trust/index.js"
      provides: "Pure, DOM-free confidenceScore(input, ctx) + SOURCE_WEIGHTS map, reusing provenanceFor"
      contains: "export function confidenceScore"
    - path: "js/data/index.js"
      provides: "sourceYear(source, nowYear) helper exported next to parseYearsFromSources"
      contains: "sourceYear"
    - path: "js/viz/index.js"
      provides: "confidenceScore wired into showTooltip + showLinkTooltip; duplicate #lastUpdated write removed"
      contains: "confidenceScore"
    - path: "tests/confidence-score.test.mjs"
      provides: "Pure unit tests: bounds, ordering, monotonic decay, unknown floor, no-year=>no-decay, future-year guard"
      min_lines: 40
    - path: "tests/viz-confidence-wiring.test.mjs"
      provides: "String-presence test that viz tooltips import + call confidenceScore and render Confidence:"
      min_lines: 20
  key_links:
    - from: "js/viz/index.js"
      to: "js/trust/index.js confidenceScore"
      via: "import + call in showTooltip/showLinkTooltip"
      pattern: "confidenceScore\\("
    - from: "js/trust/index.js confidenceScore"
      to: "js/trust/index.js provenanceFor"
      via: "internal reuse to obtain tag + resolved source"
      pattern: "provenanceFor\\("
    - from: "js/viz/index.js"
      to: "js/data/index.js sourceYear"
      via: "import + call to resolve a source's usable year"
      pattern: "sourceYear\\("
---

<objective>
Add the pure confidence-scoring core (TRUST-03) and wire it into the viz tooltips with full unit coverage (TRUST-06), and resolve the viz-side half of the freshness dual-owner hazard (TRUST-05).

`confidenceScore(input, ctx)` extends the Phase-2 trust core: it calls `provenanceFor` to get the tag + resolved source, maps the tag to a base weight (observed 90 / estimated 65 / unknown 25), and applies an exponential half-life age decay derived from the figure's source year. It is PURE/DOM-free and unit-tested for bounds, ordering, monotonic decay, the unknown floor, and the no-year/future-year guards. The viz node + link tooltips render "Confidence: NN%" next to the existing badge. Because Plan 02 owns `js/viz`, it also removes the duplicate `#lastUpdated` write from render() so js/ui becomes the single freshness owner (Plan 03 owns the ui-side confirmation + the freshness test) — this keeps Plan 03 free of `js/viz` so the two Wave-2 plans never share a file.

Purpose: Investors can quantify how much to trust each figure; and the freshness display stops flickering between two owners.
Output: confidenceScore in js/trust, a sourceYear helper in js/data, score wiring + the #lastUpdated removal in js/viz, and two registered tests.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-confidence-methodology-freshness/03-RESEARCH.md

<interfaces>
<!-- Verified this session. Use directly — no exploration. -->

js/trust/index.js (Phase 2 — extend, do not rewrite):
  export function provenanceFor(input, ctx={}) -> {tag:'observed'|'estimated'|'unknown', source?:{label,url}}
    - market-cap marker: input.marketcap===true
    - tag from String(input.confidence ?? input.cf).toLowerCase(): startsWith('high')=>observed, startsWith('medium')=>estimated, else unknown
    - FK = input.sourceId ?? input.sf; resolved via ctx.sourceIndex[fk]; dangling/empty => tag-only (no source key)
  export function badgeHtml(prov) -> string
  export function renderProvenanceBadge(el, prov)
  (module keeps a local escapeHtml; stays DOM-free except renderProvenanceBadge)

js/data/index.js:
  function parseYearsFromSources(profile)  (line ~66) — existing regex /(19|20)\d{2}/ style year extraction
  graphForMode() builds sourceIndex: Object.fromEntries((p.sources||[]).map(s => [s.id, s]))  (line ~153)
  export block (line ~223): escapeHtml, normalizeEntityLabel, buildSharedSupplierOverlapIndex, getTopOverlap, parseYearsFromSources, ...

js/viz/index.js:
  import { provenanceFor, renderProvenanceBadge, badgeHtml } from "../trust/index.js";  (line 8)
  import { DATA, asCountryName, graphForMode } from "../data/index.js";  (line 6)
  showTooltip(d, ev)  (line 185): prov = provenanceFor(d,{sourceIndex:STATE.sourceIndex, meta:DATA.meta});
     badge rendered at line 205: <div class="tf">${d.kind||'Node'} · ${badgeHtml(prov)}</div>
  showLinkTooltip(d, ev) (line 254): prov = provenanceFor(d,{...}); badge at line 263: <div class="tf">Relationship · ${badgeHtml(prov)}</div>
  render() ~409-413: the DUPLICATE #lastUpdated write to delete:
     // Update footer with last updated timestamp
     const footerEl = document.getElementById('lastUpdated');
     if (footerEl && DATA.meta?.lastUpdated) { footerEl.textContent = DATA.meta.lastUpdated; }
  d carries node FK as d.sourceId, link FK as d.sf; STATE.sourceIndex is the resolved {id:source} map; DATA.meta.generatedAt is ISO.

RESEARCH recommended formula (constants are Claude's discretion, must stay bounded 0–100, unknown→floor, no fabrication):
  SOURCE_WEIGHTS = { observed:90, estimated:65, unknown:25 }; HALF_LIFE_YEARS=4; MIN_AGE_MULT=0.5
  confidenceScore: base = SOURCE_WEIGHTS[prov.tag] ?? unknown; if tag==='unknown' || !prov.source return unknown floor;
    mult=1 if no finite ctx.sourceYear or no ctx.now; else ageYears=max(0, ctx.now - year); mult=max(MIN_AGE_MULT, 0.5^(ageYears/HALF_LIFE_YEARS));
    return clamp(round(base*mult),0,100).
  sourceYear(src, nowYear): match /(19|20)\d{2}/g over `${src.id} ${src.title} ${src.url}`, keep years in [1990, nowYear], return max or null.

Test convention (tests/viz-provenance-wiring.test.mjs): node:test + node:assert/strict; pure module imported directly for math; viz tested by readFileSync string asserts. Root package.json is type=commonjs; js/package.json is type=module so the trust ESM imports resolve under node:test.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED→GREEN confidenceScore in js/trust + sourceYear in js/data (pure, unit-tested)</name>
  <read_first>js/trust/index.js (full — provenanceFor signature + escapeHtml), js/data/index.js parseYearsFromSources ~66 + export block ~223, tests/provenance.test.mjs (import convention for the pure trust module), 03-RESEARCH.md §"Concrete unit assertions for confidence-score.test.mjs"</read_first>
  <files>tests/confidence-score.test.mjs, js/trust/index.js, js/data/index.js</files>
  <behavior>
    - confidenceScore({confidence:"high (company disclosure)", sourceId:"S1"}, {sourceIndex:{S1:{id:"S1",title:"2025 10-K",url:"https://x"}}, sourceYear: NOW, now: NOW}) is between 80 and 100.
    - Same input with sourceYear = NOW-8 returns strictly LESS than the NOW case, and still >= floor (monotonic decay, clamped at MIN_AGE_MULT).
    - confidenceScore({cf:"medium (source-backed)", sf:"S1"}, sameCtx) < the high-tier case (ordering observed>estimated).
    - confidenceScore({}, {}) === unknown floor (25); never higher.
    - For a fuzz of ages [-5..50]: every output is an integer and 0 <= score <= 100 (future years never exceed 100).
    - sourceYear:null (or no ctx.now) => score equals the undecayed base (absence of a date is not a penalty).
    - sourceYear(src, nowYear): "2024 Annual Report"=>2024; future "2044 PPA"=>filtered (returns null if no <=now year); no digits=>null.
  </behavior>
  <action>RED: write `tests/confidence-score.test.mjs` (replace the Plan-01 placeholder) encoding every behavior above, importing `confidenceScore` from `../js/trust/index.js` and `sourceYear` from `../js/data/index.js`; run it and confirm it FAILS. GREEN: add `confidenceScore(input, ctx={})` and a `SOURCE_WEIGHTS` const to `js/trust/index.js` per the RESEARCH formula — it must call the existing `provenanceFor(input, ctx)` to get {tag, source}, map tag→base weight, return the unknown floor directly when tag==='unknown' || !prov.source, apply the half-life decay only when ctx.sourceYear is a finite number AND ctx.now is set, clamp ageYears to >=0 (future-year guard) and the result to round/[0,100]; keep it DOM-free. Add `sourceYear(src, nowYear)` to `js/data/index.js` next to parseYearsFromSources and add it to the export block; it matches /(19|20)\d{2}/g over id+title+url, filters to [1990, nowYear], returns Math.max or null. Do NOT add a `low` weight tier (the data has none — only the unknown floor). Re-run until GREEN. REFACTOR only if needed; keep tests green.</action>
  <verify>
    <automated>node --test tests/confidence-score.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/confidence-score.test.mjs` passes; the test asserts bounds, observed>estimated>unknown ordering, monotonic age decay, unknown floor (25), no-year=>no-decay, and integer/[0,100] across a [-5..50] age fuzz.
    - `grep -n "export function confidenceScore" js/trust/index.js` matches; `confidenceScore` calls `provenanceFor` internally.
    - `sourceYear` is exported from js/data/index.js and ignores years > nowYear and years < 1990.
    - js/trust/index.js remains DOM-free (no document/window/innerHTML added outside the existing renderProvenanceBadge).
  </acceptance_criteria>
  <done>A pure, bounded, monotonic confidenceScore exists and is unit-proven; the FK→year resolver lives in js/data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire confidenceScore into viz tooltips (node + link) and prove the wiring</name>
  <read_first>js/viz/index.js lines 1-12 (imports), 185-240 (showTooltip), 254-270 (showLinkTooltip); js/data/index.js export block (confirm sourceYear exported in Task 1); tests/viz-provenance-wiring.test.mjs (string-presence convention)</read_first>
  <files>js/viz/index.js, tests/viz-confidence-wiring.test.mjs</files>
  <behavior>
    - viz imports confidenceScore from ../trust/index.js and sourceYear from ../data/index.js.
    - viz calls confidenceScore at least twice (node tooltip + link tooltip render paths).
    - both tooltip HTML strings render the literal label "Confidence:" followed by the score and a percent sign.
    - the score's nowYear is derived live from DATA.meta.generatedAt (not a hardcoded year).
  </behavior>
  <action>In `js/viz/index.js`: extend the line-8 trust import to also import `confidenceScore`, and add `sourceYear` to the line-6 data import. In `showTooltip` (after the existing `prov = provenanceFor(...)` at line 189), compute `const nowYear = new Date(DATA.meta?.generatedAt || Date.now()).getUTCFullYear();` then resolve the figure's source via `STATE.sourceIndex[d.sourceId ?? d.sf]`, compute `const score = confidenceScore(d, { sourceIndex: STATE.sourceIndex, meta: DATA.meta, sourceYear: src ? sourceYear(src, nowYear) : null, now: nowYear });` and append ` · Confidence: ${score}%` to the `.tf` line (line 205) right after `badgeHtml(prov)`. Repeat the same pattern in `showLinkTooltip` (.tf at line 263, FK is d.sf). Render the score as a numeric `${score}%` only — never interpolate any user string (V5 control). Then replace the Plan-01 placeholder `tests/viz-confidence-wiring.test.mjs` with string-presence assertions: viz imports confidenceScore, imports sourceYear, calls confidenceScore >= 2 times, the file contains the literal `Confidence:` and `%` adjacent to a score interpolation, and nowYear derives from `DATA.meta?.generatedAt`.</action>
  <verify>
    <automated>node --test tests/viz-confidence-wiring.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/viz-confidence-wiring.test.mjs` passes.
    - `grep -c "confidenceScore(" js/viz/index.js` >= 2.
    - viz imports confidenceScore from ../trust/index.js and sourceYear from ../data/index.js.
    - both showTooltip and showLinkTooltip .tf lines render `Confidence: NN%` next to the badge.
    - nowYear is computed from DATA.meta.generatedAt (no hardcoded year literal in the score path).
  </acceptance_criteria>
  <done>Every badged figure in a tooltip now shows a live, source-aged confidence percentage; the wiring is gated by a registered test.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the viz duplicate #lastUpdated write (viz-side half of the dual-owner fix)</name>
  <read_first>js/viz/index.js render() ~409-413 (the duplicate #lastUpdated write + its "Update footer with last updated timestamp" comment); 03-RESEARCH.md Pitfall 2 (dual-owner hazard)</read_first>
  <files>js/viz/index.js</files>
  <action>In `js/viz/index.js` render(), DELETE the 4-line block that writes #lastUpdated (the `// Update footer with last updated timestamp` comment plus `const footerEl = document.getElementById('lastUpdated'); if (footerEl && DATA.meta?.lastUpdated) { footerEl.textContent = DATA.meta.lastUpdated; }`). This makes js/ui updateStatusIndicator the SOLE owner of #lastUpdated (Plan 03 owns the ui-side confirmation + the freshness test). Do not touch anything else in render(). The deletion lives in Plan 02 because Plan 02 owns js/viz, keeping Plan 03 free of js/viz for parallel safety.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const v=fs.readFileSync('js/viz/index.js','utf8');const n=(v.match(/getElementById\(['\"]lastUpdated['\"]\)/g)||[]).length;if(n!==0){console.error('viz still writes #lastUpdated:',n);process.exit(1)}console.log('viz lastUpdated write removed')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getElementById(.lastUpdated.)" js/viz/index.js` == 0.
    - render() no longer contains the "Update footer with last updated timestamp" comment.
    - No other behavior in render() changed (only the 4-line freshness block removed).
  </acceptance_criteria>
  <done>The viz no longer competes for #lastUpdated; js/ui is the single live owner (Plan 03 verifies + tests this).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected data record → tooltip HTML | Node/link fields flow into innerHTML strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | confidenceScore output in tooltip HTML | mitigate | confidenceScore returns an integer; rendered as `${score}%` (numeric only) — no user string interpolated by this feature. |
| T-03-04 | Tampering | sourceYear over source id/title/url text | accept | Read-only regex extraction of a numeric year; no HTML emitted from sourceYear; titles/urls continue to flow through the existing escaped badgeHtml/View-Source path unchanged. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH: zero installs); nothing to slopcheck. |
</threat_model>

<verification>
- `node --test tests/confidence-score.test.mjs` and `tests/viz-confidence-wiring.test.mjs` both pass.
- `npm test` stays green (these files were registered in Plan 01).
- confidenceScore is pure; viz renders `Confidence: NN%` in both tooltips with a live nowYear; viz no longer writes #lastUpdated.
</verification>

<success_criteria>
A defensible, bounded, monotonic confidence score is computed per figure and displayed in node + link tooltips next to the Phase-2 badge, fully unit-tested; the viz-side freshness duplicate write is removed.
</success_criteria>

<output>
Create `.planning/phases/03-confidence-methodology-freshness/03-02-SUMMARY.md` when done.
</output>
