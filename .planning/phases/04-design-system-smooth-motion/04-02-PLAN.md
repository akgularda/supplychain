---
phase: 04-design-system-smooth-motion
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - styles/layout.css
  - styles/components.css
  - styles/theme.css
autonomous: true
requirements: [STORY-01, STORY-03]
must_haves:
  truths:
    - "layout.css, components.css, and theme.css consume var(--token) instead of hardcoded literals"
    - "The .confidence-high/medium/low rules keep their exact green/amber/neutral hues (trust color contract)"
    - "No markup class name or ID is renamed (provenance hooks, modal IDs, container IDs intact)"
    - "theme.css contains a @media (prefers-reduced-motion: reduce) block that zeroes transitions/animations"
    - "The site renders with no intended visual change vs before"
  artifacts:
    - path: "styles/layout.css"
      provides: "Token-consuming layout rules"
      contains: "var(--"
    - path: "styles/components.css"
      provides: "Token-consuming component rules with preserved trust hues"
      contains: "--color-observed"
    - path: "styles/theme.css"
      provides: "Token-consuming theme rules + reduced-motion media block"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "components.css .confidence-high/medium/low"
      to: "base.css --color-observed/estimated/unknown"
      via: "var() reference, identical hues"
      pattern: "--color-observed"
    - from: "theme.css @media (prefers-reduced-motion)"
      to: ".node/.link/#companyCard/.modal transitions"
      via: "transition:none / duration override"
      pattern: "prefers-reduced-motion"
---

## Phase Goal

**As an** investor, **I want to** view a visually coherent, world-class interface, **so that** the credibility of the data is reinforced by the quality of its presentation.

<objective>
Migrate the three consumer stylesheets (layout, components, theme) to consume the base.css design tokens, and add the CSS-side prefers-reduced-motion block, with zero intended visual change.

Purpose: complete the STORY-01 site-wide application of the token system. The trust color contract (.confidence-*) must keep its exact hues by mapping to the semantic trust tokens; no markup hook may be renamed (the 178-test suite string-matches markup classes/IDs and badge HTML). Adding the reduced-motion CSS block closes the CSS half of STORY-03 accessibility.

Output: token-consuming layout.css/components.css/theme.css and a reduced-motion @media block in theme.css. Implements STORY-01 (apply tokens) and the CSS portion of STORY-03 (reduced-motion).
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-design-system-smooth-motion/04-RESEARCH.md

<read_first>
- styles/base.css (now the token source from Plan 01; all --color-*/--fs-*/--space-*/--radius-*/--shadow-*/--dur-*/--ease-* available; 7 legacy aliases still resolve)
- styles/layout.css, styles/components.css, styles/theme.css (the literals to migrate)
- styles/components.css L49-54 (THE trust color contract; map .confidence-high color to --color-observed, .confidence-medium to --color-estimated, .confidence-low to --color-unknown; KEEP the rgba background/border literals byte-identical so tests/design-tokens still matches them)
- 04-RESEARCH.md Appendix Hardcoded-Value Catalog (literal-to-token mapping; A3 cross-check)
- 04-RESEARCH.md Code Examples (token-consuming #bar button example; trust color mapping example; reduced-motion CSS block for theme.css)
- 04-RESEARCH.md Pitfall 1 (do NOT collapse .confidence-* into a generic token; map to SEMANTIC trust tokens)
- 04-RESEARCH.md Anti-Patterns (do not rename any .confidence-*/.source-link/#id hook; do not introduce fresh literals; do not reorder the four CSS link tags)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout.css and components.css to consume tokens (preserve trust hues + all hooks)</name>
  <files>styles/layout.css, styles/components.css</files>
  <action>
Walk layout.css and components.css rule by rule and replace hardcoded color/size/spacing/radius/shadow/transition literals with the matching var(--token) from base.css, using the Hardcoded-Value Catalog as the literal-to-token map (A3: a wrong mapping is a visual regression, so each swap must resolve to the same computed value). For components.css L52-54 (the trust contract), map ONLY the color: literal to the semantic token: .confidence-high color to var(--color-observed), .confidence-medium to var(--color-estimated), .confidence-low to var(--color-unknown); LEAVE the background rgba(76,175,80,0.2)/rgba(255,193,7,0.2)/rgba(158,158,158,0.2) and border rgba(...,0.4) literals byte-identical (the design-tokens test matches those rgba strings and they are the contract). Do NOT rename any selector: .confidence-*, .confidence-badge, .source-link, .prov-badge, .verified-node, and every #id stay exactly as written. Do NOT introduce any new color/size literal; map only to tokens already defined in base.css (the catalog defines the full set). Keep selector text, property order, and declaration structure otherwise unchanged so the cascade is preserved. Target at least ~15 var(--) references in each file as migration evidence (the design-tokens test requires >=10 per file).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components.css still contains rgba(76,175,80, rgba(255,193,7, and rgba(158,158,158 (trust hues byte-identical) and now references --color-observed/--color-estimated/--color-unknown
    - All hook selectors (.confidence-high/medium/low, .confidence-badge, .source-link, .prov-badge, .verified-node) and every #id are unchanged
    - layout.css and components.css each have at least 10 occurrences of var(--
    - No new color/size literal introduced; structure/order preserved
  </acceptance_criteria>
  <done>layout.css and components.css consume tokens; trust color contract preserved exactly; no hook renamed; full suite still green except the theme.css reduced-motion assertion (closed in Task 2) and the viz-motion assertions (Plan 03).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate theme.css to tokens and add the prefers-reduced-motion block</name>
  <files>styles/theme.css</files>
  <action>
Migrate theme.css color/size/spacing/radius/shadow/transition literals to var(--token) per the Hardcoded-Value Catalog, the same way as Task 1, preserving the existing @media (max-width:768px) and @media print blocks and all selectors/IDs. Use the motion tokens (--dur-*, --ease-standard, --ease-emphasized) for any transition declarations. Then APPEND a new @media (prefers-reduced-motion: reduce) block (theme.css loads last, so it wins the cascade) exactly per 04-RESEARCH Code Examples: a universal rule zeroing animation-duration/iteration-count/transition-duration with !important, plus an explicit rule setting transition:none!important;animation:none!important on .node, .link, #companyCard, #tt, .modal. Do not alter the existing media-query selectors or remove the page fade behavior; only ADD the reduced-motion block. Reach at least ~10 var(--) references in theme.css as migration evidence.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - theme.css contains a @media (prefers-reduced-motion: reduce) block zeroing transition/animation durations and targeting .node/.link/#companyCard/#tt/.modal
    - theme.css has at least 10 occurrences of var(--; existing @media (max-width:768px) and @media print blocks preserved
    - design-tokens.test.mjs migration-evidence assertions now PASS for all of layout/components/theme
    - The viz-motion theme.css reduced-motion assertion (/prefers-reduced-motion/) now passes; the JS-side viz assertions remain RED until Plan 03
  </acceptance_criteria>
  <done>theme.css consumes tokens and honors prefers-reduced-motion; STORY-01 token application is complete site-wide and the CSS half of STORY-03 accessibility is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored CSS to browser | Static authored tokens; no runtime user input flows into stylesheets |
| restyle vs markup-string tests | 178-test suite string-matches markup class/ID/badge HTML; renames would break it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | .confidence-* trust color contract | mitigate | Map only color: to semantic token; keep rgba background/border literals byte-identical; design-tokens test asserts the rgba strings persist |
| T-04-04 | Tampering | markup hook names (.source-link, #ids) | mitigate | No selector renamed; full suite (markup string-match) run after each task |
| T-04-05 | Denial of Service (a11y) | motion-sensitive users | mitigate | @media (prefers-reduced-motion) block zeroes transitions/animations site-wide |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- npm test green for all design-tokens assertions (token presence + migration evidence + trust hues + hooks)
- theme.css contains the prefers-reduced-motion block
- Trust rgba hue literals still present in components.css
- No selector/ID renamed across the three files
</verification>

<success_criteria>
- layout/components/theme all consume tokens (>=10 var(--) each) with zero intended visual change (STORY-01 applied site-wide)
- Trust color contract preserved exactly; reduced-motion CSS block present (STORY-03 CSS half)
- design-tokens.test.mjs fully GREEN; only the JS viz-motion assertions remain RED (closed by Plan 03)
</success_criteria>

<output>
Create `.planning/phases/04-design-system-smooth-motion/04-02-SUMMARY.md` when done
</output>
