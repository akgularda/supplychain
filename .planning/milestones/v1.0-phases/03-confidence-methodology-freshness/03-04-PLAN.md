---
phase: 03-confidence-methodology-freshness
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - docs/perf/_render-smoke-0304.cjs
autonomous: false
requirements: [TRUST-03, TRUST-04, TRUST-05, TRUST-06]
must_haves:
  truths:
    - "The full registered suite (`npm test`) is green with all Phase-3 tests included (>= 144 + the new confidence/viz-wiring/methodology/freshness/data-shape tests)."
    - "Served over http-server with the rich data file, the app PAINTS with real data: nodes/links render, tooltips show the Phase-2 badge AND 'Confidence: NN%', the Methodology modal opens (focus trap + ESC), and the freshness indicator shows a real date from a single owner."
    - "Zero console errors during the render smoke."
  artifacts:
    - path: "docs/perf/_render-smoke-0304.cjs"
      provides: "Playwright render-smoke harness (http-server + real served data) confirming paint + score + modal + freshness"
      min_lines: 30
  key_links:
    - from: "docs/perf/_render-smoke-0304.cjs"
      to: "index.html + data/top100-map.js"
      via: "http-server served page load, assert paint + Confidence% + methodology modal + freshness date"
      pattern: "Confidence"
---

<objective>
Final phase gate: prove the whole Phase-3 surface works end to end and pause for a human visual confirmation.

This plan does NOT add features. It (1) runs the full registered suite green, (2) runs a render smoke over http-server against the now-rich served data to confirm the app actually paints with real data and shows the confidence score, methodology modal, and freshness date, and (3) pauses at a human-verify checkpoint for the visual confirmation that automated string/render checks cannot fully cover.

Purpose: Catch any cross-plan regression and confirm the credibility surface is real in a browser before /gsd:verify-work.
Output: a reusable render-smoke harness and a green full suite + human sign-off.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-confidence-methodology-freshness/03-RESEARCH.md

<interfaces>
<!-- Verified this session. Use directly — no exploration. -->

Tooling already in repo:
  devDependencies: http-server ^14.1.1 ; dependencies: playwright ^1.58.2 (root package.json).
  Phase 02 left a reusable parity harness at docs/perf/_parity-verify-0204.cjs — mirror its structure (boot http-server, launch chromium, load http://127.0.0.1:PORT/, assert, capture console errors). STATE.md records this harness + that the served snapshot could not paint locally ONLY because the committed data was the flat shape — Plan 01 fixes exactly that, so the render smoke should now PAINT.

Render-smoke assertions to make:
  - page loads with zero console errors (collect page.on('console', ...) and page.on('pageerror', ...)).
  - SVG nodes rendered (e.g. selector for the D3 node group has > 0 elements).
  - hovering/triggering a node tooltip (#tt) shows both a provenance badge (.prov-badge) and the literal "Confidence:" with a % .
  - clicking #bMethodology opens #methodologyModal (display flex); ESC (or close) hides it; focus returns.
  - footer #lastUpdated shows a non-"--", non-empty date string (single owner from js/ui).

Run model: this environment may block external CDNs (Google Fonts / d3 CDN). If d3 is CDN-loaded and unreachable in-sandbox, the smoke may not paint — in that case fall back to the Phase-02 synthetic-dataset approach used in _parity-verify-0204.cjs and document the limitation in the SUMMARY, then rely on the human-verify checkpoint for the true visual pass (mirrors the plan-sanctioned fallback recorded in STATE.md for Phase 1/2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green gate + render smoke over http-server with real data</name>
  <read_first>package.json scripts.test (all Phase-3 files registered by Plan 01); docs/perf/_parity-verify-0204.cjs (Phase-2 harness to mirror); data/top100-map.js (rich shape after Plan 01); index.html (#bMethodology, #methodologyModal, #lastUpdated, #tt)</read_first>
  <files>docs/perf/_render-smoke-0304.cjs</files>
  <action>Run `npm test` and confirm it exits 0 with all Phase-3 tests included (confidence-score, viz-confidence-wiring, methodology-wiring, freshness-wiring, data-shape) — if anything fails, STOP and report the failing assertion (do not paper over it). Then create `docs/perf/_render-smoke-0304.cjs` mirroring `docs/perf/_parity-verify-0204.cjs`: boot http-server on a free port serving the repo root, launch chromium, load the page, collect console + pageerror events, and assert: (1) zero console/page errors, (2) D3 node elements rendered (> 0), (3) a node tooltip (#tt) contains a `.prov-badge` and the literal `Confidence:` with `%`, (4) clicking `#bMethodology` opens `#methodologyModal` and ESC/close hides it, (5) `#lastUpdated` text is non-empty and not `--`. If the in-sandbox environment blocks the d3/font CDN so the real page cannot paint, fall back to the Phase-02 synthetic-dataset injection pattern from the parity harness to exercise the same code paths, and clearly note the fallback + its limitation in the SUMMARY. Capture the harness output (pass/fail per assertion).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with all Phase-3 test files included (suite count rises from 144 by the new cases).
    - `docs/perf/_render-smoke-0304.cjs` exists and, when run, reports zero console errors and asserts paint + Confidence% + methodology modal open/close + non-empty freshness date (or documents the CDN-blocked fallback path it used).
  </acceptance_criteria>
  <done>The full suite is green and an automated render smoke confirms the Phase-3 surface paints with real data (or documents the sandbox fallback).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of the Phase-3 trust surface</name>
  <action>Pause for the user to visually confirm the integrated Phase-3 surface in a browser after the automated gate + render smoke pass. Do not proceed until the user signals approval.</action>
  <what-built>Phase 3 trust surface: per-figure confidence score in node + link tooltips, an accessible Methodology modal with honest real-data facts, a single-owner live freshness indicator, and a restored rich served data file so the app paints with real data.</what-built>
  <how-to-verify>
    1. From the repo root run: `npx http-server -p 8088 -c-1 .`
    2. Open `http://127.0.0.1:8088/` in a browser. Confirm the map PAINTS with real company nodes/links (not the fatal-error guard).
    3. Hover a company node — the tooltip shows the Observed/Estimated/Unknown badge AND `Confidence: NN%` next to it. Hover a link/relationship — same (Confidence% on the relationship tooltip).
    4. Click the `Methodology` button (near the `?` help button). The modal opens, is keyboard-navigable (Tab cycles within it), ESC closes it, and the copy states the real facts (407 sources, 120 high / 3,447 medium qualifiers, 75 dangling references, weighting + age-decay explanation, observed/estimated/unknown meaning, honest limits).
    5. In the footer, `Data updated:` shows a real date (e.g. a Feb 2026 date), not `--`, and it does not flicker/change format on re-render.
    6. Open DevTools console — confirm no errors.
  </how-to-verify>
  <verify>
    <human-check>User confirms score%, methodology modal (focus trap + ESC), live freshness date, and real-data paint all work with zero console errors.</human-check>
  </verify>
  <resume-signal>Type "approved" if the score, methodology modal, freshness date, and real-data paint all work; otherwise describe what is wrong (e.g. "no Confidence% on link tooltip", "modal not keyboard-trapped", "footer shows --").</resume-signal>
  <done>User has confirmed the browser experience of the Phase-3 trust surface, or filed specific issues to address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| served page → browser | The integrated render path is exercised end to end; no new untrusted input introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | integrated render of data-derived score/modal/freshness | mitigate | Render smoke asserts zero console errors; all string interpolation already escaped (Plans 02/03 threat models). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH: zero installs); http-server + playwright already in package.json. |
</threat_model>

<verification>
- `npm test` exits 0 with all Phase-3 tests included.
- Render smoke confirms paint + Confidence% + methodology modal + live freshness (or documents sandbox fallback).
- Human checkpoint approves the visual + interaction surface.
</verification>

<success_criteria>
The complete Phase-3 trust surface (confidence score, methodology view, freshness) works end to end on real data, the full suite is green, and a human has confirmed the browser experience.
</success_criteria>

<output>
Create `.planning/phases/03-confidence-methodology-freshness/03-04-SUMMARY.md` when done.
</output>
