---
phase: 02-provenance-source-linking
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - tests/trust-wiring.test.mjs
  - package.json
autonomous: false
requirements: [TRUST-01, TRUST-02]
must_haves:
  truths:
    - "A registered string-presence test asserts js/viz/index.js and js/ui/index.js both import from ../trust/index.js and reference provenanceFor"
    - "The test asserts js/trust/index.js exports provenanceFor, badgeHtml, and renderProvenanceBadge"
    - "The full suite (116 existing + provenance.test.mjs + trust-wiring.test.mjs) is green"
    - "An investor can hover a node/link and click a real source link, and the $cap + profile figures show observed/estimated/unknown badges (human-verified in browser)"
  artifacts:
    - path: "tests/trust-wiring.test.mjs"
      provides: "String-presence wiring assertions for viz/ui/trust"
      contains: "provenanceFor"
    - path: "package.json"
      provides: "scripts.test includes tests/trust-wiring.test.mjs"
      contains: "tests/trust-wiring.test.mjs"
  key_links:
    - from: "package.json scripts.test"
      to: "tests/trust-wiring.test.mjs"
      via: "node --test file list"
      pattern: "tests/trust-wiring\\.test\\.mjs"
    - from: "tests/trust-wiring.test.mjs"
      to: "js/viz/index.js + js/ui/index.js + js/trust/index.js"
      via: "fs.readFileSync + regex assertions"
      pattern: "trust/index\\.js"
---

<objective>
Close Phase 2: add the wiring string-presence test (now that both viz and ui are wired by plans 02-02
and 02-03), register it in `package.json scripts.test` (the second half of the GATE fix), confirm the
full suite is green, and run a human-verify checkpoint in the browser to confirm badges render and
source links resolve.

This test is intentionally added LAST so every prior wave's `npm test` stayed green — it can only pass
once both viz and ui call the trust API.

Purpose: lock in TRUST-01/TRUST-02 wiring and give the developer a real-browser confirmation before
`/gsd:verify-work`.
Output: `tests/trust-wiring.test.mjs`, its package.json registration, a green full suite, and a signed-off
visual check. This plan owns the test file + package.json exclusively (no source-file overlap with 02/03).

User story: As an investor, I want to actually see the observed/estimated/unknown badges and click a
real SEC/IR source link in the live page.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provenance-source-linking/02-RESEARCH.md
@.planning/phases/02-provenance-source-linking/02-VALIDATION.md
@.planning/phases/02-provenance-source-linking/02-02-PLAN.md
@.planning/phases/02-provenance-source-linking/02-03-PLAN.md

<interfaces>
<!-- String-test convention: read source as a string and regex-assert. Mirror tests/index-ui-integrity.test.mjs. -->
Assertions to make (read each file with fs.readFileSync):
  - js/trust/index.js  : exports provenanceFor, badgeHtml, renderProvenanceBadge (match /export function provenanceFor/ etc.)
  - js/viz/index.js    : imports from "../trust/index.js" AND references provenanceFor (>=1)
  - js/ui/index.js     : imports from "../trust/index.js" AND references provenanceFor (>=1)

package.json scripts.test currently (after plan 02-01) ends with:
  ... tests/auto-update-script.test.mjs tests/provenance.test.mjs
Append: tests/trust-wiring.test.mjs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the trust-wiring string test and register it</name>
  <files>tests/trust-wiring.test.mjs, package.json</files>
  <read_first>
    - tests/index-ui-integrity.test.mjs (the read-source-as-string + regex convention to copy)
    - js/trust/index.js, js/viz/index.js, js/ui/index.js (confirm the export + import + call sites exist after plans 01-03)
    - .planning/phases/02-provenance-source-linking/02-RESEARCH.md "Validation Architecture" (Wave 0 gaps: trust-wiring.test.mjs) + the GATE caveat
    - package.json scripts.test (the list to extend)
  </read_first>
  <action>
    Create tests/trust-wiring.test.mjs using `import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs";`. Read js/trust/index.js, js/viz/index.js, js/ui/index.js via fs.readFileSync. Assert: trust exports all three functions (regex /export function provenanceFor/, /badgeHtml/, /renderProvenanceBadge/); viz contains both `from "../trust/index.js"` and `provenanceFor`; ui contains both `from "../trust/index.js"` and `provenanceFor`. Then EDIT package.json to append `tests/trust-wiring.test.mjs` to the scripts.test file list (after tests/provenance.test.mjs). This is the second half of the GATE LANDMINE fix — without registration the wiring assertion never runs.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `node --check tests/trust-wiring.test.mjs` passes
    - `grep -q "tests/trust-wiring.test.mjs" package.json` (registered)
    - `npm test` exits 0 with the full suite: 116 existing + provenance.test.mjs + trust-wiring.test.mjs all green
    - The wiring test fails if either viz or ui drops the trust import/call (it is a real guard, not a no-op)
  </acceptance_criteria>
  <done>Wiring test registered and green; the full phase suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Provenance badges (observed/estimated/unknown) on the node tooltip, relationship tooltip, the $cap
    stat bar figure, the company card anchor, and the compare grid; reachable source links; and the
    fixed source drawer reading real titles. All driven by the data-derived trust module.
  </what-built>
  <how-to-verify>
    1. Serve the site: `npx http-server -p 8080 .` (or the repo's usual static serve) and open http://localhost:8080 .
       NOTE: per the carried-forward Phase-1 condition, the committed data/top100-map.js snapshot lacks
       nodes/profiles, so locally figures may degrade to the honest "Unknown" badge with NO source link —
       that is the CORRECT graceful-degradation behavior. To see observed/estimated badges + real links,
       load a build/deploy that serves the rich dataset (or the live Pages deploy), OR temporarily point
       window.SUPPLY_MAP_DATA at data/top100-map.json.
    2. Hover a node: confirm a provenance pill (Observed/Estimated/Unknown) appears and, when sourced, a
       "source ↗" link opens a real SEC/IR URL in a new tab.
    3. Hover a relationship line: confirm the pill reflects cf and links via sf where present.
    4. Check the top stat bar: confirm the $cap figure shows an Observed badge linking to companiesmarketcap.
    5. Open a company profile and the Sources drawer: confirm rows show real human TITLES (not source IDs)
       and links resolve.
    6. Confirm NO figure shows a fabricated source — unsourced/dangling figures show Unknown with no link.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> source files | read-only string assertions; no execution of page code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | unregistered test silently not running (the GATE LANDMINE) | mitigate | explicitly append tests/trust-wiring.test.mjs to package.json scripts.test; acceptance grep confirms |
| T-02-SC | Tampering | npm installs | accept | No packages this phase (buildless) |
</threat_model>

<verification>
- `npm test` exits 0 with 116 + provenance.test.mjs + trust-wiring.test.mjs.
- Both new test files registered in package.json scripts.test.
- Human-verify checkpoint signed off.
</verification>

<success_criteria>
- Phase 2 complete: every major figure carries a data-derived observed/estimated/unknown badge with a reachable source where sourced and an honest unknown state where not; the wiring is test-locked and the full suite is green; a human has confirmed the rendered behavior.
</success_criteria>

<output>
Create `.planning/phases/02-provenance-source-linking/02-04-SUMMARY.md` when done.
</output>
