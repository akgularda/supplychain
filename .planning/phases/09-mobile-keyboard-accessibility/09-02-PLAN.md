---
phase: 09-mobile-keyboard-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - js/main.js
  - js/ui/narrative.js
  - js/ui/index.js
  - tests/mobile-keyboard-a11y.test.mjs
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "When the hero overlay opens, keyboard focus moves into it (#heroSkip), Tab is trapped inside it, and on close focus is restored to the prior element"
    - "Escape on the hero overlay is handled by the central ESC switch (no double-binding) and closes/skips it"
    - "The keyboard-only journey search -> filter -> select -> reset is fully operable with no pointer"
    - "Every new control (incl. the 4 mobile-sheet buttons) shows a visible :focus ring via existing tokens and has an accessible name"
    - "macro-site-accessibility.test.mjs stays green; the a11y test gains keyboard/ARIA assertions"
  artifacts:
    - path: "js/main.js"
      provides: "hero overlay routed through openModal/closeModal/trapFocus + central ESC registration"
      contains: "heroOverlay"
    - path: "js/ui/index.js"
      provides: "central ESC switch + activeModal/trapFocus handling extended to the hero overlay"
      contains: "activeModal"
    - path: "tests/mobile-keyboard-a11y.test.mjs"
      provides: "keyboard/ARIA assertions for hero trap wiring + focus ring on new controls"
      contains: "heroOverlay"
  key_links:
    - from: "js/main.js heroRender"
      to: "js/ui/index.js openModal/closeModal"
      via: "open on render(step), close on render(null) — sets/clears activeModal"
      pattern: "(openModal|closeModal|activeModal)"
    - from: "js/ui/index.js keydown switch"
      to: "trapFocus(e, activeModal)"
      via: "Tab trap + Escape routing including heroOverlay"
      pattern: "trapFocus"
---

<objective>
Fix keyboard accessibility (PERF-03): route #heroOverlay through the existing openModal/closeModal/trapFocus
machinery and register it in the central ESC switch so focus moves in on open, is trapped while open, and is
restored on close (folding away main.js's scoped ESC->skip handler to avoid double-binding). Then confirm the
full keyboard-only journey (search -> filter -> select -> reset) is operable, all new controls (including the
4 mobile-sheet buttons from 09-01) show the visible token :focus ring and have accessible names, and extend
the Node a11y test with keyboard/ARIA assertions while keeping macro-site-accessibility.test.mjs green.

Purpose: The hero overlay declares role=dialog aria-modal=true but is toggled via hidden only, so it never
traps focus — Tab leaks to background controls. This is the core PERF-03 keyboard defect.
Output: A trapping/restoring hero overlay, a verified keyboard journey, extended a11y assertions.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-keyboard-accessibility/09-RESEARCH.md
@.planning/phases/09-mobile-keyboard-accessibility/09-01-SUMMARY.md

<interfaces>
<!-- Existing machinery to REUSE — verified in repo. Do not build a new trap loop. -->
- js/ui/index.js exposes the modal machinery: openModal / closeModal / trapFocus and an activeModal pointer.
  openModal moves focus to the first focusable element and stores the previously-focused element; closeModal
  restores it. The central keydown switch already calls trapFocus(e, activeModal) on Tab and routes Escape,
  plus shortcuts g/l/f/b/'/'/e/h and Ctrl+S.
- js/main.js currently shows/hides #heroOverlay via o.hidden=false/true in hero rendering and has a SCOPED
  ESC->skip handler that must be folded into the central ESC switch (avoid double ESC binding).
- js/ui/narrative.js holds hero render/step logic (render(step) opens, render(null) closes).
- Hero overlay controls in DOM order: #heroPrev, #heroPause, #heroNext, #heroSkip (all labeled <button>).
  Place initial focus on #heroSkip so escape-without-mouse is one key away (RESEARCH Pattern 3 / OQ2).
- The hero auto-advances ~5.5s and respects reducedMotion(); trapping focus must not change autoplay timing.
- macro-site-accessibility.test.mjs is GREEN today and is NOT in scripts.test's orphaned set — extend coverage
  in tests/mobile-keyboard-a11y.test.mjs (registered in 09-01), do not resurrect orphaned macro-site files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task A: Make the hero overlay a real trapped, focus-restoring modal</name>
  <files>js/main.js, js/ui/narrative.js, js/ui/index.js</files>
  <behavior>
    - On hero open (render(step)): activeModal becomes #heroOverlay and focus moves to #heroSkip
    - While open: Tab/Shift+Tab stay within #heroOverlay (no leak to #bReset or other background controls)
    - On hero close (render(null) or skip): activeModal cleared and focus restored to the pre-open element
    - Escape is handled once by the central switch (no double-binding) and closes/skips the hero
    - simulation.restart() is NOT called (PERF-01 invariant preserved)
  </behavior>
  <read_first>
    js/main.js (hero wiring: bTour/heroNext/Prev/Pause/Skip, the o.hidden toggling, the scoped ESC->skip handler)
    js/ui/narrative.js (hero render(step)/render(null) entry points)
    js/ui/index.js (openModal/closeModal/trapFocus signatures, the activeModal pointer, the central keydown ESC switch)
    tests/no-restart-invariant.test.mjs (the no-restart invariant to not regress)
  </read_first>
  <action>
    Route #heroOverlay through the existing modal machinery instead of bare hidden toggling. When the hero
    opens (render(step) shows the overlay), set it as activeModal and move focus to #heroSkip using the same
    openModal-equivalent path that other dialogs use (reuse openModal/trapFocus from js/ui/index.js — do NOT
    write a new trap loop, per Don't Hand-Roll). When the hero closes (render(null)/skip), clear activeModal
    and restore focus via the closeModal-equivalent. Register #heroOverlay in the central ESC switch in
    js/ui/index.js so a single Escape closes/skips it, and REMOVE main.js's separate scoped ESC->skip handler
    so ESC is bound exactly once. Keep the existing show/hide visual state and the ~5.5s autoplay + reducedMotion
    behavior unchanged; ensure focus trapping does not retrigger any data load and never calls simulation.restart()
    (per D PERF-03 + PERF-01 invariant). If wiring through ui/index.js requires a small exported helper, add the
    minimal export rather than duplicating trap logic.
  </action>
  <verify>
    <automated>node --test tests/no-restart-invariant.test.mjs tests/hero-wiring.test.mjs tests/narrative.test.mjs</automated>
  </verify>
  <done>Hero overlay sets/clears activeModal, focuses #heroSkip on open, traps Tab, restores focus on close, ESC bound once; hero/narrative/no-restart tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task B: Verify keyboard journey + extend a11y assertions</name>
  <files>tests/mobile-keyboard-a11y.test.mjs, js/ui/index.js</files>
  <behavior>
    - search (/) -> type/select -> filter/select node -> reset (Escape) is fully keyboard-operable
    - Every new control (#bMethodology,#bTour,#bChokepoints,#bScenarioTaiwan,#bScenarioReset,#scenarioChokepointSelect,#heroSkip and the 4 mobile-sheet buttons) is focusable and has an accessible name
    - The visible :focus ring (layout.css :focus-visible token rule) still applies to all controls
    - macro-site-accessibility.test.mjs stays green
  </behavior>
  <read_first>
    tests/mobile-keyboard-a11y.test.mjs (the file created in 09-01 — extend it)
    tests/macro-site-accessibility.test.mjs (existing ARIA baseline to keep green / mirror)
    js/ui/index.js (the keydown switch implementing the search/filter/select/reset shortcuts)
    index.html (confirm tab order = DOM order for the new controls + mobile-sheet buttons)
  </read_first>
  <action>
    Extend tests/mobile-keyboard-a11y.test.mjs with static assertions that: (1) js/ui/index.js references the
    hero overlay through the modal machinery (e.g. matches activeModal/openModal/closeModal alongside heroOverlay)
    so the trap wiring is provably present; (2) every new control id plus the 4 mobile-sheet button ids appear as
    focusable elements with accessible names (aria-label on the toolbar/select/hero controls; visible textContent
    on the sheet buttons); (3) layout.css still carries the :focus-visible ... outline ring (no outline:none
    reintroduced). Confirm via reading js/ui/index.js that the keyboard shortcuts for search('/'), filter, select,
    and reset(Escape) remain wired (DOM order = logical tab order for the new controls) — if a shortcut path is
    missing, fix the wiring in js/ui/index.js minimally (per D PERF-03). Do NOT modify macro-site-accessibility.test.mjs
    except to keep it green; do NOT add orphaned macro-site files to scripts.test.
  </action>
  <verify>
    <automated>node --test tests/mobile-keyboard-a11y.test.mjs tests/macro-site-accessibility.test.mjs</automated>
  </verify>
  <done>a11y test asserts hero-trap wiring + accessible names + focus ring on all new controls; macro-site-accessibility stays green; keyboard journey shortcuts confirmed wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/focus events -> DOM | Focus-trap logic governs which controls receive focus; no new data crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | hero overlay focus-trap wiring (js/main.js, js/ui/index.js) | mitigate | Reuse the tested openModal/closeModal/trapFocus machinery; bind ESC exactly once; no new event-injection surface |
| T-09-04 | Denial of service | focus trap + 5.5s autoplay interaction | accept | Autoplay timing unchanged; #heroSkip focused first so a single Escape always exits; reducedMotion respected |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH audit: none); no install task |
</threat_model>

<verification>
- node --test tests/mobile-keyboard-a11y.test.mjs tests/macro-site-accessibility.test.mjs passes
- node --test tests/no-restart-invariant.test.mjs tests/hero-wiring.test.mjs passes (no PERF-01 regression)
- npm test reports 0 failures
</verification>

<success_criteria>
- Hero overlay focuses #heroSkip on open, traps Tab, restores focus on close, ESC bound once
- Keyboard-only search -> filter -> select -> reset journey operable; new controls focusable + named with visible ring
- macro-site-accessibility.test.mjs green; a11y test extended; npm test green
</success_criteria>

<output>
Create `.planning/phases/09-mobile-keyboard-accessibility/09-02-SUMMARY.md` when done
</output>
