---
phase: 07-scenario-stress-tests
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - docs/perf/_scenario-smoke-0703.cjs
autonomous: false
requirements: [DEPTH-03, DEPTH-04]
user_setup: []

must_haves:
  truths:
    - "The full suite (242 prior + scenario unit + scenario-wiring) is green"
    - "Over http-server, running the Taiwan preset shows '7 companies · $11.36T exposed' + impact list + graph highlight"
    - "The scenario output renders a Derived badge (never Observed) reachable to the Methodology view"
  artifacts:
    - path: "docs/perf/_scenario-smoke-0703.cjs"
      provides: "Playwright integration smoke driving the Taiwan preset over http-server"
      contains: "scenarioPanel"
  key_links:
    - from: "Playwright smoke"
      to: "#bScenarioTaiwan click"
      via: "real DOM run over http-server"
      pattern: "bScenarioTaiwan"
---

<objective>
Final Phase 7 gate (DEPTH-03 + DEPTH-04): prove the full suite is green and that the scenario stress-test actually works end-to-end in a real browser — run the Taiwan preset over http-server, confirm the impact panel shows the live "7 companies · $11.36T exposed" headline + impact list + Derived badge + graph highlight, with zero console errors. Then a human-verify checkpoint confirms the stress-test reads clearly and the impact is obvious.

Purpose: Close the phase with a regression gate + the real-render proof the unit/string tests cannot provide (runtime wiring + visual honesty). Mirrors the Phase-3/6 render-smoke gate pattern.
Output: docs/perf/_scenario-smoke-0703.cjs + a green full suite + human sign-off.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scenario-stress-tests/07-RESEARCH.md
@.planning/phases/07-scenario-stress-tests/07-01-SUMMARY.md
@.planning/phases/07-scenario-stress-tests/07-02-SUMMARY.md

<interfaces>
<!-- Reuse the Phase-3/6 render-smoke pattern (boots http-server on a free port, real repo root, http:// not file://, drives a real interaction). -->

From js/ui (Plan 02): #bScenarioTaiwan click → renderScenario + highlightImpacted
Expected runtime headline: "7 companies impacted · $11.36T market cap exposed" (or .toFixed(2) form)
Derived badge: span.prov-badge text "Derived" inside #scenarioProv; never "Observed"
Note: STATE.md records the live data/top100-map.js snapshot paints (Phase-3 smoke confirmed 100 nodes); reuse the same boot harness. If the d3 CDN is unreachable this run, fall back to the synthetic-dataset approach used by docs/perf render smokes (plan-sanctioned).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Full-suite green gate</name>
  <files>docs/perf/_scenario-smoke-0703.cjs</files>
  <read_first>package.json scripts.test (both scenario files registered), STATE.md test baseline, 07-01-SUMMARY.md + 07-02-SUMMARY.md (counts + any deviations)</read_first>
  <action>Run `npm test` and confirm the full suite is green: 242 prior + the scenario unit tests (Plan 01) + the scenario-wiring tests (Plan 02), 0 fail. If any prior test regressed, STOP and report (do not patch around a regression — it signals the additive contract was violated). Record the exact final count for the SUMMARY. No source edits in this task.</action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>`npm test` exits 0; final count = 242 prior + scenario unit + scenario-wiring, 0 fail; both new test files confirmed in the run output.</done>
</task>

<task type="auto">
  <name>Task 1: Playwright scenario integration smoke over http-server</name>
  <files>docs/perf/_scenario-smoke-0703.cjs</files>
  <read_first>an existing docs/perf render-smoke .cjs (Phase-3 _render-smoke-0304.cjs pattern: free-port http-server boot, real repo root, http:// URL, console-error capture, real interaction), 07-RESEARCH.md "UI Wiring" (global-mode highlight note)</read_first>
  <action>Create docs/perf/_scenario-smoke-0703.cjs mirroring the Phase-3 render-smoke harness: boot http-server on a free port at the real repo root, navigate via http:// (not file://), wait for the global map to paint, capture console/page errors. Drive the real scenario: ensure global mode, click #bScenarioTaiwan, then assert (a) #scenarioSummary text matches /7 companies/ AND /\$11\.36T|\$11\.36 ?T|11\.36/ (live-derived headline), (b) #scenarioImpactList contains 7 .cItem rows, (c) #scenarioProv contains a .prov-badge whose text is "Derived" and is NOT "Observed", (d) clicking #bScenarioTaiwan applied a graph highlight (impacted company nodes dimmed-vs-highlighted; assert highlightBy took effect by checking opacity classes/attrs on the expected impacted symbols, mirroring how the chokepoints smoke would), (e) zero console errors. If the d3 CDN is unreachable this run, fall back to the synthetic-dataset injection used by prior docs/perf smokes (plan-sanctioned) and assert the same DOM outcomes. Print a single PASS/FAIL line + the captured headline for the SUMMARY.</action>
  <verify>
    <automated>node docs/perf/_scenario-smoke-0703.cjs</automated>
  </verify>
  <done>The smoke boots the real page over http-server, runs the Taiwan preset, and confirms the live "7 companies · $11.36T exposed" headline + 7 impact rows + a "Derived" (never "Observed") badge + graph highlight, with zero console errors (synthetic fallback only if the CDN is down).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full scenario stress-test slice: a #scenarioPanel with a "Run: Taiwan semiconductor disruption" button + chokepoint select, an impact panel ("7 companies impacted · $11.36T market cap exposed" + per-firm list with HHI 0.20→0.25), a Derived provenance badge linking to Methodology, and a graph highlight of the 7 impacted companies. The full suite is green and the Playwright smoke passed.</what-built>
  <how-to-verify>
    1. From the repo root run: `npx http-server -p 8099 -c-1` and open http://localhost:8099 in a browser.
    2. Wait for the global map to render, then locate the Scenario Stress-Test panel (sibling of the Critical Chokepoints panel).
    3. Click "Run: Taiwan semiconductor disruption". Expected: the summary reads "7 companies impacted · $11.36T market cap exposed"; the list shows NVDA, AAPL, AVGO, SK Hynix (000660.KS), AMD, AMAT, KLA (KLAC) each with "1 supplier" and "0.20→0.25 HHI"; the 7 companies are highlighted in the graph.
    4. Confirm the panel shows a "Derived" badge (NOT "Observed") and the Methodology link opens the scenario-model section (single-hop, HHI, "exposure not loss").
    5. Click "Reset" and confirm the highlight clears and the panel empties. Optionally pick a supplier from the chokepoint select and confirm it runs a disruption.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (numbers, highlight, badge, copy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived analytic → investor (runtime) | the rendered headline/badge must match the real engine output and be honestly tagged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information disclosure | runtime headline drifting from real data or mis-tagged | mitigate | smoke asserts /7 companies/ + /11.36/ derived at runtime + "Derived" badge present, "Observed" absent |
| T-07-07 | Denial of service | console errors / broken render under real boot | mitigate | smoke captures console/page errors and fails on any; reuses the proven Phase-3 boot harness |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (playwright/http-server already present; audit N/A) |
</threat_model>

<verification>
- `npm test` green (242 prior + scenario unit + scenario-wiring, 0 fail)
- `node docs/perf/_scenario-smoke-0703.cjs` → PASS (7 companies / $11.36T / Derived badge / highlight / 0 console errors)
- human-verify checkpoint approved
</verification>

<success_criteria>
- Full suite green including both new scenario test files
- Real-render smoke confirms the live Taiwan headline + impact list + Derived badge + graph highlight, 0 console errors
- Human confirms the stress-test reads clearly and the impact is obvious
</success_criteria>

<output>
Create `.planning/phases/07-scenario-stress-tests/07-03-SUMMARY.md` when done
</output>
