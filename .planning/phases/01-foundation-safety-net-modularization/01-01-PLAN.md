---
phase: 01-foundation-safety-net-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/perf/baseline-2026-06-20.md
  - docs/perf/lighthouse-2026-06-20.report.html
  - docs/perf/lighthouse-2026-06-20.report.json
autonomous: false
requirements: [FOUND-04]
user_setup: []

must_haves:
  truths:
    - "A pre-extraction Lighthouse + perf baseline exists in the repo for later comparison"
    - "The local static server serves index.html with module-compatible MIME so later extraction can be verified"
    - "The pre-extraction green test state (npm test = 0 fail) is recorded as the regression anchor"
  artifacts:
    - path: "docs/perf/baseline-2026-06-20.md"
      provides: "Recorded Lighthouse scores (performance/accessibility/best-practices/seo) + key web vitals (FCP, LCP, TBT, CLS) captured BEFORE extraction"
      min_lines: 20
      contains: "Lighthouse"
  key_links:
    - from: "docs/perf/baseline-2026-06-20.md"
      to: "npx lighthouse against http://localhost:8080/index.html"
      via: "lighthouse CLI run recorded as the pre-extraction baseline"
      pattern: "FCP|LCP|TBT|CLS"
---

<objective>
Capture the pre-extraction performance + Lighthouse baseline (FOUND-04) BEFORE any file is moved, and confirm the local serving + tooling chain works. This plan must run first: a baseline taken after extraction cannot prove "no regression." It also materializes `http-server` in node_modules and verifies module-compatible MIME so the extraction plans can be verified locally.

Purpose: Establish the objective evidence ("nothing got slower / less accessible") that the whole behavior-preserving refactor will be measured against.
Output: `docs/perf/baseline-2026-06-20.md` (+ raw Lighthouse json/html), recorded pre-extraction `npm test` green state.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md
@.planning/phases/01-foundation-safety-net-modularization/01-VALIDATION.md

# Tooling facts (from RESEARCH.md, verified):
# - Chrome installed at: C:\Program Files\Google\Chrome\Application\chrome.exe
# - http-server ^14.1.1 is a devDependency (run via npx); not yet in node_modules — run `npm install` first
# - lighthouse CLI is NOT installed; use `npx lighthouse` (fallback: Chrome DevTools Lighthouse panel, record numbers manually)
# - ES modules must be served over http:// (http-server), NEVER file:// (CORS)
# - Authoritative gate command: `npm test` (5 files; green = 0 fail). Do NOT run `node --test tests/` over all files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize tooling and record the pre-extraction green test anchor</name>
  <files>(no source files modified — tooling + recorded state only)</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/package.json (confirm test script = 5 files; http-server devDep)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Environment Availability + Lighthouse capture section)
  </read_first>
  <action>
    Run `npm install` to materialize `http-server` (and playwright) into node_modules. Then run the authoritative gate `npm test` and record the exact pass/fail counts — this is the pre-extraction regression anchor that every later extraction step must keep equal. Start `npx http-server . -p 8080 -c-1` (background) and do a single smoke fetch of `http://localhost:8080/index.html` plus one `.js`-style path to confirm the server returns a module-compatible `text/javascript` MIME (resolves assumption A1). If the MIME is wrong, note the `--mimetypes` fallback. Do NOT modify index.html, styles, or js in this task — this is read-only baseline tooling setup.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` completes; node_modules/http-server exists.
    - `npm test` exits 0 with 0 failures (record the assertion count, expected 116).
    - `http-server` serves http://localhost:8080/index.html with HTTP 200.
    - A served `.js` path returns Content-Type beginning with `text/javascript` (or the `--mimetypes` fallback is documented in the SUMMARY).
  </acceptance_criteria>
  <done>Tooling installed, server confirmed serving module-MIME, and the pre-extraction `npm test` green count is recorded in the plan SUMMARY as the regression anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and record the pre-extraction Lighthouse + perf baseline</name>
  <files>docs/perf/baseline-2026-06-20.md, docs/perf/lighthouse-2026-06-20.report.json, docs/perf/lighthouse-2026-06-20.report.html</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Lighthouse baseline capture (Windows + Node) code example)
  </read_first>
  <action>
    Create `docs/perf/`. With `http-server` running on port 8080, run `npx lighthouse http://localhost:8080/index.html --output=json --output=html --output-path=docs/perf/lighthouse-2026-06-20 --chrome-path="C:\Program Files\Google\Chrome\Application\chrome.exe" --only-categories=performance,accessibility,best-practices,seo`. Then author `docs/perf/baseline-2026-06-20.md` summarizing the four category scores and the key web vitals (FCP, LCP, TBT, CLS, Speed Index) extracted from the JSON, labeled clearly as the PRE-EXTRACTION baseline dated 2026-06-20, with a one-line note that the post-extraction run (in plan 03) must be equal-or-better. If `npx lighthouse` cannot run headless on this machine, fall back to the Chrome DevTools Lighthouse panel against the served URL and transcribe the same metrics into the markdown — record which method was used.
  </action>
  <verify>
    <automated>test -f docs/perf/baseline-2026-06-20.md && grep -Eqi "FCP|LCP|TBT|CLS" docs/perf/baseline-2026-06-20.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/perf/baseline-2026-06-20.md` exists, >= 20 lines, contains the four Lighthouse category scores and FCP/LCP/TBT/CLS values.
    - The markdown explicitly labels itself the PRE-EXTRACTION baseline and names the capture method (lighthouse CLI or DevTools panel).
    - Raw Lighthouse output committed (json + html) OR, if DevTools fallback used, the SUMMARY states why no raw file exists.
    - index.html / styles / js are unchanged by this plan (still the monolith).
  </acceptance_criteria>
  <done>A dated pre-extraction baseline file with scores and web vitals exists in the repo and is the comparison anchor for FOUND-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A pre-extraction Lighthouse/perf baseline (`docs/perf/baseline-2026-06-20.md`) captured against the still-monolithic site, plus confirmed local module-MIME serving.</what-built>
  <how-to-verify>
    1. Open `docs/perf/baseline-2026-06-20.md` — confirm it shows performance/accessibility/best-practices/seo scores and FCP/LCP/TBT/CLS.
    2. Confirm the file is clearly dated and labeled "pre-extraction" so the post-extraction comparison in plan 03 is meaningful.
    3. Confirm `index.html` is still the original monolith (extraction has NOT started yet).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to extraction, or describe what the baseline is missing.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` green (0 fail) — recorded as the pre-extraction anchor.
- `docs/perf/baseline-2026-06-20.md` exists with Lighthouse scores + web vitals, labeled pre-extraction.
- http-server serves index.html and `.js` with module-compatible MIME (A1 resolved).
- No source file (index.html/styles/js) changed in this plan.
</verification>

<success_criteria>
FOUND-04 baseline artifact captured BEFORE extraction and committed; tooling chain (http-server + lighthouse-or-DevTools) confirmed working; pre-extraction green test count recorded as the regression anchor for plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-safety-net-modularization/01-01-SUMMARY.md` when done. Record: pre-extraction `npm test` assertion/pass count, Lighthouse scores + web vitals, capture method used, and the confirmed module MIME result.
</output>
