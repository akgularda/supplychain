---
phase: 11-multi-hop-scenario-cascade
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - index.html
  - js/ui/index.js
  - tests/scenario-wiring.test.mjs
autonomous: false
requirements: [CASC-02, CASC-03]
must_haves:
  truths:
    - "Running Taiwan in the live page shows 8 companies / $13.28T derived live (no hardcoded 8 or 13.28 / $11.36T literal)"
    - "The scenario panel shows a direct-vs-indirect hop split (N companies across H hops · $X.XT exposed)"
    - "runTaiwanScenario and runChokepointScenario opt into maxHops:3"
    - "The Derived provenance badge still renders on scenario output"
    - "Methodology copy describes the bounded multi-hop model + termination bound + assumptions and KEEPS the literal phrase 'direct dependents'"
  artifacts:
    - path: "js/ui/index.js"
      provides: "renderScenario hop breakdown + maxHops:3 call sites + live headline"
      contains: "maxHops"
    - path: "index.html"
      provides: "#scenarioHopBreakdown panel id + multi-hop methodology copy"
      contains: "scenarioHopBreakdown"
    - path: "tests/scenario-wiring.test.mjs"
      provides: "hop-breakdown id + multi-hop methodology wiring assertions"
      contains: "scenarioHopBreakdown"
  key_links:
    - from: "runTaiwanScenario"
      to: "runScenario"
      via: "maxHops:3 disruption"
      pattern: "maxHops"
    - from: "renderScenario"
      to: "#scenarioHopBreakdown"
      via: "byHop[1] direct vs indirect split"
      pattern: "byHop"
---

<objective>
Surface the multi-hop cascade in the scenario panel: render a direct-vs-indirect hop breakdown with a
live-derived headline ("N companies across H hops · $X.XT exposed"), opt the Taiwan + chokepoint call
sites into `maxHops:3`, keep the honest Derived badge, and update the Methodology copy to describe the
bounded multi-hop model while keeping the literal phrase "direct dependents" so the methodology-wiring
test stays green. Extend the wiring tests for the new id + copy, then gate with the full suite and a
Playwright smoke (Taiwan -> 8 firms / $13.28T + hop split).

Purpose: CASC-02 (panel hop breakdown; impacted count + cap derived live from the multi-hop result) and
CASC-03 (keep Derived badge; Methodology explains the multi-hop model, bound, assumptions).
Output: edited index.html (#scenarioPanel + methodology), edited js/ui/index.js (renderScenario +
call sites), extended tests/scenario-wiring.test.mjs.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-hop-scenario-cascade/11-RESEARCH.md
@.planning/phases/11-multi-hop-scenario-cascade/11-01-SUMMARY.md

@js/ui/index.js
@index.html
@tests/scenario-wiring.test.mjs

<interfaces>
<!-- Anchors extracted from source — use directly. -->

Multi-hop runScenario result shape (delivered by 11-01):
- `{ impactedCompanies:[{symbol, company, marketcap, lostSuppliers, suppliersBefore, suppliersAfter, concentrationBefore, concentrationAfter, severity, hop}], byHop:{1:[sym...],2:[sym...]}, maxHopReached, totalMarketCapExposed, supplierCount, disabled }`

js/ui/index.js (current scenario slice ~lines 733-788):
- `function renderScenario(result)` writes scenarioSummaryEl.textContent (headline) + scenarioImpactListEl.innerHTML (per-company rows, every label via escapeHtml) + scenarioProvEl.innerHTML (badgeHtml(provenanceFor({derived:true, n})))
- `function runTaiwanScenario()` calls `runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption, {profiles, nodes})`
- `function runChokepointScenario(label)` calls `runScenario({ disableSupplier: label }, {profiles, nodes})`
- escapeHtml is required on every label before innerHTML (T-07-03 contract)

index.html anchors:
- #scenarioPanel (line ~330): #scenarioProv, subtitle line ~332 ("direct downstream impact (single-hop)"), #scenarioSummary (~333), #scenarioImpactList (~334)
- Methodology modal (~lines 157-164): the `<li><strong>Single-hop only.</strong> ... direct dependents ...</li>` bullet (~160), plus exposure-not-loss (~162) and TSMC (~163) bullets that must remain

tests/scenario-wiring.test.mjs constraints (must stay green / extend):
- asserts the 7 existing #scenarioPanel ids; adding a new id is allowed (not forbidden)
- asserts `/single-hop|direct dependents/i`, `/HHI/`, `/exposure/i`, `/not a loss|exposure, not/i`, `/tsmc|taiwan/i`
- forbids `11.36` and `7 companies impacted` literals in UI; headline must derive from impactedCompanies.length + totalMarketCapExposed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel hop breakdown + maxHops:3 call sites + live headline + multi-hop methodology copy</name>
  <read_first>
    - js/ui/index.js lines 725-797 (renderScenario, runTaiwanScenario, runChokepointScenario, resetScenario)
    - index.html lines 157-164 (methodology scenario bullets) and 330-342 (#scenarioPanel)
    - tests/scenario-wiring.test.mjs (the id list, the methodology regexes, the forbidden-literal asserts)
    - 11-RESEARCH.md §UI Design (headline formula, direct/indirect split, methodology copy keeping "direct dependents")
  </read_first>
  <action>
    index.html #scenarioPanel: add a new breakdown container right after #scenarioSummary:
    `&lt;div class="cMuted" id="scenarioHopBreakdown" style="margin-bottom:6px"&gt;&lt;/div&gt;` (additive id, mirrors the
    existing additive-id pattern). Update the subtitle at line ~332 from "direct downstream impact (single-hop)" to a
    multi-hop phrasing, e.g. "multi-hop downstream cascade (bounded, cycle-safe)".

    index.html methodology (~line 160): replace the `<li><strong>Single-hop only.</strong> ...</li>` bullet with a
    bounded multi-hop bullet, e.g. titled "Bounded multi-hop cascade", that: (a) starts from the disabled suppliers'
    *direct dependents* (hop 1) — KEEP the literal phrase "direct dependents" so the `/single-hop|direct dependents/i`
    regex stays satisfied; (b) propagates to companies depending on an impacted firm that is itself a supplier (hop 2+);
    (c) states traversal is cycle-safe (visited set guarantees termination) and bounded (at most N hops); (d) notes real
    second-order edges are sparse so multi-hop often equals the direct-dependent set and no edges are fabricated. Do NOT
    remove the exposure-not-loss (~162), HHI, or TSMC (~163) bullets — the wiring regexes still require exposure / "not a
    loss" / HHI / tsmc|taiwan.

    js/ui/index.js renderScenario: change the headline to derive live from the multi-hop result —
    `${result.impactedCompanies.length} companies impacted across ${result.maxHopReached} hop(s) · $${(result.totalMarketCapExposed/1e12).toFixed(2)}T exposed`
    (no 11.36, no "7 companies impacted" literal; count from .length, hops from maxHopReached, cap from totalMarketCapExposed).
    Populate #scenarioHopBreakdown (capture the element near the other scenario element refs) with a direct/indirect split:
    `const direct = result.byHop?.[1]?.length || 0; const indirect = result.impactedCompanies.length - direct;` render
    e.g. "{direct} direct · {indirect} indirect". Optionally tag each impact row with `hop ${c.hop}` — if so, keep
    escapeHtml on every company/symbol label (T-07-03). Keep the `provenanceFor({derived:true, n})` -> badgeHtml ->
    #scenarioProv path UNCHANGED. In resetScenario, also clear #scenarioHopBreakdown.

    Call sites: runTaiwanScenario -> pass `{ ...SCENARIO_PRESETS.TAIWAN_SEMI.disruption, maxHops: 3 }` (or add maxHops:3
    to the ctx) so the live page opts into multi-hop; runChokepointScenario -> `{ disableSupplier: label, maxHops: 3 }`.
    Add the #scenarioHopBreakdown element lookup wherever the other scenario element refs are resolved. NO fenced code in this action.
  </action>
  <acceptance_criteria>
    - `grep -F 'id="scenarioHopBreakdown"' index.html` matches; subtitle no longer reads "(single-hop)".
    - `grep -F 'maxHops' js/ui/index.js` matches in BOTH runTaiwanScenario and runChokepointScenario (maxHops:3).
    - renderScenario headline uses impactedCompanies.length + maxHopReached + totalMarketCapExposed; no `11.36` and no `7 companies impacted` literal in js/ui/index.js.
    - Methodology still contains the literal "direct dependents" AND describes a bounded, cycle-safe multi-hop model; exposure-not-loss / HHI / TSMC bullets remain.
    - `provenanceFor({ derived: true ... })` -> badgeHtml -> #scenarioProv path is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/scenario-wiring.test.mjs</automated>
  </verify>
  <done>Panel shows a live-derived multi-hop headline + direct/indirect split via #scenarioHopBreakdown; both call sites pass maxHops:3; Derived badge kept; methodology describes multi-hop while keeping "direct dependents".</done>
</task>

<task type="auto">
  <name>Task 2: Extend scenario-wiring tests for the hop-breakdown id + multi-hop methodology phrasing; full suite gate</name>
  <read_first>
    - tests/scenario-wiring.test.mjs (existing id list + methodology + forbidden-literal asserts)
    - js/ui/index.js + index.html (post-Task-1 state)
    - 11-VALIDATION.md §Sampling Rate (per-task npm test; full suite green before verification)
  </read_first>
  <action>
    Extend tests/scenario-wiring.test.mjs (do not weaken existing asserts): (a) add `scenarioHopBreakdown` to the
    panel-id assertion (or a new test asserting `id="scenarioHopBreakdown"` is present in index.html); (b) add an
    assertion that js/ui/index.js references `maxHops` AND `byHop` AND `maxHopReached` (the headline + split derive from
    the multi-hop result); (c) add an assertion that index.html methodology contains a multi-hop signal (e.g.
    `/multi-hop|hop 2|second-order|cycle-safe/i`) WHILE the existing `/single-hop|direct dependents/i` regex still
    passes (the literal "direct dependents" remains). Keep the existing forbidden-literal asserts (no 11.36, no "7
    companies impacted"). Then run the full registered suite via `npm test` and confirm exit 0 (301 baseline + the
    11-01 cascade tests + the extended wiring tests).
  </action>
  <acceptance_criteria>
    - `node --test tests/scenario-wiring.test.mjs` exits 0 with the new hop-breakdown id + multi-hop methodology + maxHops/byHop assertions present.
    - `npm test` exits 0 (full suite green: baseline + cascade + wiring).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>scenario-wiring tests assert the hop-breakdown id, the maxHops/byHop/maxHopReached wiring, and the multi-hop methodology copy (with "direct dependents" preserved); the full registered suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright smoke + visual verification of the multi-hop scenario panel</name>
  <what-built>
    The scenario panel now renders the multi-hop cascade live: running the Taiwan preset (maxHops:3) shows
    8 companies / $13.28T exposed with a direct-vs-indirect hop split (7 direct · 1 indirect) and the Derived
    badge; the Methodology view describes the bounded, cycle-safe multi-hop model.
  </what-built>
  <how-to-verify>
    1. Start the static server: `npx http-server -p 8080 -c-1 .` (buildless; no build step).
    2. Open http://localhost:8080 and locate the Scenario Stress-Test panel.
    3. Click "Run: Taiwan semiconductor disruption". Confirm:
       - Headline reads "8 companies impacted across 2 hop(s) · $13.28T exposed" (derived, NOT a hardcoded 7 / $11.36T).
       - The hop breakdown shows "7 direct · 1 indirect" (TSM is the indirect hop-2 firm).
       - The Derived badge is present (never "Observed").
    4. Open the Methodology view; confirm it describes the bounded multi-hop cascade, its termination bound, and assumptions, and still reads "direct dependents".
    5. (Optional) A Playwright smoke asserting the 8 / $13.28T headline + hop split may be run if a spec is added; otherwise visual confirmation suffices.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (wrong count, missing hop split, missing badge, methodology copy).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine result -> DOM | supplier/company labels from the result are rendered into #scenarioPanel via innerHTML (untrusted-ish strings) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | XSS via supplier/company label in the hop-breakdown rows | mitigate | apply existing `escapeHtml(...)` to every company/symbol label before innerHTML in renderScenario (T-07-03 contract); no raw label interpolation |
| T-11-04 | Information disclosure | hardcoded headline misrepresenting the multi-hop result | mitigate | headline derives from impactedCompanies.length + maxHopReached + totalMarketCapExposed; wiring test forbids 11.36 / "7 companies impacted" literals |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (playwright/http-server already present); RESEARCH §Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `npm test` exits 0 (full suite incl. extended scenario-wiring + 11-01 cascade tests).
- Live Taiwan run shows 8 / $13.28T + "7 direct · 1 indirect" + Derived badge; methodology keeps "direct dependents".
- Both UI call sites pass maxHops:3; #scenarioHopBreakdown id present.
</verification>

<success_criteria>
- Scenario panel shows a hop breakdown; impacted count + market-cap exposed derived live from the multi-hop result — CASC-02.
- Derived badge kept; Methodology explains the multi-hop model + bound + assumptions (keeping "direct dependents") — CASC-03.
- Full registered suite green; human-verified live behavior.
</success_criteria>

<output>
Create `.planning/phases/11-multi-hop-scenario-cascade/11-02-SUMMARY.md` when done.
</output>
