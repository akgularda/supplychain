---
phase: 05-hero-moment-investor-narrative
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/narrative.test.mjs
  - tests/hero-wiring.test.mjs
  - js/ui/narrative.js
autonomous: true
requirements: [STORY-04, STORY-05]

must_haves:
  truths:
    - "buildNarrative(data) returns exactly 4 steps ordered market -> concentration -> risk -> opportunity"
    - "Each step exposes id, non-empty title, non-empty caption, and a function apply(controls)"
    - "Each step.apply calls the correct injected control (market->openGlobal, concentration->highlightBy, risk->highlightBy, opportunity->openProfile)"
    - "Caption numbers are computed from the passed data fixture, not hardcoded literals"
    - "Both new test files run under npm test (registered in package.json scripts.test)"
  artifacts:
    - path: "js/ui/narrative.js"
      provides: "Pure buildNarrative(data) export (DOM-free, no js/ui/index.js import)"
      exports: ["buildNarrative"]
      contains: "export function buildNarrative"
    - path: "tests/narrative.test.mjs"
      provides: "Pure step-list assertions (order/titles/captions/apply targets, non-fabricated numbers)"
      min_lines: 40
    - path: "tests/hero-wiring.test.mjs"
      provides: "String-presence assertions for markup + module/main wiring (RED until Plans 02/03)"
      min_lines: 20
    - path: "package.json"
      provides: "scripts.test registers tests/narrative.test.mjs and tests/hero-wiring.test.mjs"
      contains: "tests/narrative.test.mjs"
  key_links:
    - from: "tests/narrative.test.mjs"
      to: "js/ui/narrative.js"
      via: "import { buildNarrative }"
      pattern: "import.*buildNarrative.*narrative\\.js"
    - from: "package.json"
      to: "tests/narrative.test.mjs"
      via: "node --test args in scripts.test"
      pattern: "tests/narrative\\.test\\.mjs"
---

<objective>
Establish the Wave 0 test gate for Phase 5 and build the pure, DOM-free narrative step list.

Create `js/ui/narrative.js` exporting `buildNarrative(data)` — an ordered 4-step array `[market, concentration, risk, opportunity]` where each step is `{ id, title, caption, apply(controls) }`. Every caption number is computed at runtime from the passed `data` object (combined market cap, dominant-layer count + name, bottleneck count, top-cap bottleneck symbol) so captions never go stale and never contain fabricated literals (RESEARCH Pitfall 1). The module imports NO DOM and NO `js/ui/index.js` (RESEARCH Pitfall 5) so it unit-tests in Node.

Register both new test files in `package.json scripts.test` up front (Wave 0 — RESEARCH Section 5 / VALIDATION). The narrative assertions in `tests/narrative.test.mjs` go GREEN this plan; the controller + markup/wiring assertions in `tests/hero-wiring.test.mjs` are INTENDED Wave 0 RED (closed by Plans 02 and 03) — this matches the repo's established Wave 0 pattern (STATE 04-01).

Purpose: Lock the test contract before implementation and deliver the data engine (STORY-04) every later step renders.
Output: `js/ui/narrative.js`, two registered test files, updated `package.json`.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md
@.planning/phases/05-hero-moment-investor-narrative/05-VALIDATION.md

<interfaces>
<!-- Verified in-repo this session. Use directly — no exploration needed. -->

package.json scripts.test currently lists 16 .mjs files via a single `node --test ...` string. Append the two new files to that same string.

Real node fields used by buildNarrative (data/top100-map.json, verified):
- data.nodes[]: { symbol, company, marketcap (number), rank, bn (bool), y (layer index) }
- data.meta: { count, source } (count = 100, source = "companiesmarketcap.com")
- data.layers: { "<index>": "<human name>" } map

Verified real figures at current data (compute, never hardcode): count 100, combined cap ~$55.8T (Σ marketcap/1e12 = 55.77), 19 bn nodes, top-cap bottleneck = NVDA rank #1 ~$4.62T.

Injected controls shape (spied in tests, real in Plan 03 main.js):
  controls = { openGlobal(), openProfile(symbol), highlightBy(fn), resetHighlight() }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Register both test files and write the test specs (Wave 0)</name>
  <files>package.json, tests/narrative.test.mjs, tests/hero-wiring.test.mjs</files>
  <read_first>
    - package.json (scripts.test single-string format, 16 files)
    - tests/index-ui-integrity.test.mjs (string-presence + inline-script syntax-check convention)
    - tests/viz-motion.test.mjs (matchMedia / reduced-motion string-presence convention)
    - .planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map)
  </read_first>
  <action>
    Edit package.json scripts.test: append ` tests/narrative.test.mjs tests/hero-wiring.test.mjs` to the existing `node --test ...` string (do not reorder or drop any of the 16 existing files). This MUST happen first so the new tests actually run under npm test (RESEARCH GATE LANDMINE).

    Write tests/narrative.test.mjs (node:test + node:assert/strict, imports only buildNarrative from ../js/ui/narrative.js): build a tiny fake data fixture with a handful of nodes carrying marketcap/bn/y plus a meta.count and a layers map. Assert: (1) buildNarrative(fixture) returns length 4 with ids in order market, concentration, risk, opportunity; (2) every step has a string title and string caption both non-empty and a typeof-function apply; (3) using a spy controls object, market.apply calls openGlobal, concentration.apply calls highlightBy, risk.apply calls highlightBy, opportunity.apply calls openProfile (capture the openProfile symbol arg and assert it equals the highest-marketcap-and-bn node symbol from the fixture); (4) non-fabricated proof: run buildNarrative twice with DIFFERENT fixtures (different cap totals / bn counts) and assert the market and risk captions differ, and that the market caption contains the fixture-derived combined-cap number and meta.count — proving captions are computed from data, not literals.

    Write tests/hero-wiring.test.mjs (string-presence, reads source files via fs): assert index.html contains a #heroOverlay with role="dialog" and aria-modal="true", plus #heroTitle/#heroCaption/#heroProgress/#heroNext/#heroPrev/#heroPause/#heroSkip/#bTour; assert js/ui/narrative.js source contains createHeroController and the matchMedia('(prefers-reduced-motion reduce string; assert js/main.js wires heroSeen via safeReadFlag/safeWriteFlag and references #bTour. These assertions are INTENDED RED until Plans 02 (controller) and 03 (markup + main wiring) land — document that in a top-of-file comment so the executor does not treat the RED as a defect.
  </action>
  <verify>
    <automated>node --check tests/narrative.test.mjs && node --check tests/hero-wiring.test.mjs && node -e "const p=require('./package.json');if(!/tests\/narrative\.test\.mjs/.test(p.scripts.test)||!/tests\/hero-wiring\.test\.mjs/.test(p.scripts.test))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.test contains both new file paths and still lists all 16 prior files
    - tests/narrative.test.mjs imports buildNarrative and asserts 4 ordered ids, apply-target spies, and non-fabricated captions (two-fixture diff)
    - tests/hero-wiring.test.mjs asserts overlay IDs + #bTour + module/main wiring strings (intended RED noted in comment)
    - both test files pass `node --check`
  </acceptance_criteria>
  <done>Both test files registered and syntactically valid; narrative spec encodes order, apply targets, and the non-hardcoded-caption contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure buildNarrative(data)</name>
  <files>js/ui/narrative.js</files>
  <behavior>
    - buildNarrative(data) returns [market, concentration, risk, opportunity] in that order
    - market.apply(c) => c.openGlobal(); caption cites data.meta.count and Σ(marketcap)/1e12 (toFixed 1) + source companiesmarketcap.com
    - concentration.apply(c) => c.highlightBy(d => String(d.y) === String(dominantLayerIdx)); caption cites dominant-layer node count + data.layers name
    - risk.apply(c) => c.highlightBy(d => d.bn); caption cites bn count of total node count
    - opportunity.apply(c) => c.openProfile(topSymbol) where topSymbol = highest-marketcap node that is also bn; caption cites company, symbol, rank, marketcap/1e12 (toFixed 2)
    - Changing the input data changes the captions (no hardcoded numeric literals matching live data)
  </behavior>
  <read_first>
    - .planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md (Pattern 1 buildNarrative reference + "The Concrete 4-Step Narrative" table + Pitfall 1)
    - js/data/index.js (graphForMode sets layer = n.y; formatMarketCap) — for field semantics only, DO NOT import it
  </read_first>
  <action>
    Create js/ui/narrative.js with NO top-level DOM access and NO import of js/ui/index.js (RESEARCH Pitfall 5 — keeps the module Node-importable). Export `buildNarrative(data)`.

    Inside, derive from data (guard for missing fields): nodes = data.nodes || []; totalCapT = sum of n.marketcap (filter falsy) / 1e12; bnCount = count of n.bn; layerCounts keyed by n.y; dominantLayerIdx = entry with max count; dominantLayerName = (data.layers||{})[dominantLayerIdx] || fallback; dominantLayerN = that count; topByCap = nodes with marketcap sorted desc, preferring those also flagged bn for the opportunity payoff (highest-marketcap bn node; if none flagged, fall back to highest-marketcap overall); topSymbol = topByCap.symbol.

    Return the 4 step objects per the RESEARCH Concrete-4-Step table. Each caption is a template string built ONLY from the derived runtime values (per STORY-04 honest captions; market-cap framed as observed from meta source, bn/layer framed as "flagged"/"of the top N" derived aggregates — do NOT introduce a new sourced claim). apply functions reference the injected controls argument exactly: openGlobal, highlightBy with the predicates above, openProfile(topSymbol). No fenced numeric literals matching live data anywhere in the file.
  </action>
  <verify>
    <automated>node --test tests/narrative.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/narrative.test.mjs passes (4 ordered ids, apply-target spies, two-fixture caption diff)
    - js/ui/narrative.js imports neither a DOM global at top level nor js/ui/index.js
    - opportunity.apply passes the computed top-cap bottleneck symbol (not a string literal)
    - No numeric literal equal to a live data figure (55.x, 19, 100, 4.62) appears as a caption constant
  </acceptance_criteria>
  <done>buildNarrative returns the four data-driven steps; narrative test suite green; module is DOM-free and import-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data -> caption string | Frozen first-party data flows into caption text that Plan 03 will render to the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/Info-disclosure | caption text containing company name | mitigate | Captions are plain strings; the rendering plan (03) MUST use textContent (not innerHTML). buildNarrative itself produces no HTML. |
| T-05-02 | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: Package Legitimacy Gate not applicable); package.json dependencies unchanged. |
</threat_model>

<verification>
- `npm test` runs: narrative assertions GREEN; hero-wiring assertions INTENDED RED (closed by Plans 02/03).
- `node --test tests/narrative.test.mjs` fully green.
- buildNarrative is importable in Node without a DOM (no ReferenceError: document).
</verification>

<success_criteria>
- js/ui/narrative.js exports a pure buildNarrative(data) producing 4 ordered, data-driven steps.
- tests/narrative.test.mjs + tests/hero-wiring.test.mjs registered in scripts.test.
- Narrative test green; non-fabricated-caption contract enforced by two-fixture diff.
- No new dependencies; buildless preserved.
</success_criteria>

<output>
Create `.planning/phases/05-hero-moment-investor-narrative/05-01-SUMMARY.md` when done
</output>
