---
phase: 10-seo-social-launch-gate
plan: 01
subsystem: seo-social-metadata
tags: [seo, open-graph, twitter-cards, json-ld, social-card, static-site]
requires:
  - index.html (existing head, inline bootstrap, asserted IDs)
  - .github/workflows/deploy-pages.yml (cp -R assets _site/)
  - playwright ^1.58.2 (already installed)
provides:
  - SEO/OG/Twitter/JSON-LD head tags in index.html
  - assets/og-card.png (1200x630 social card)
  - tests/seo-social.test.mjs (registered in npm test)
affects:
  - crawler/social-unfurl preview when the Pages URL is shared
tech-stack:
  added: []
  patterns:
    - string/regex assertions on raw index.html (mirrors index-ui-integrity)
    - Playwright chromium screenshot at fixed 1200x630 viewport for og:image
    - absolute OG/Twitter/canonical URLs rooted at the Pages subpath
key-files:
  created:
    - tests/seo-social.test.mjs
    - scripts/make-og-card.mjs
    - assets/og-card.png
  modified:
    - index.html
    - package.json
decisions:
  - Used already-installed Playwright (no new dependency) to render a bespoke 1200x630 card rather than resizing the 1440x900 screenshot or adding sharp.
  - JSON-LD placed in <head> with type="application/ld+json" so it never matches the attribute-less inline-bootstrap regex that index-ui-integrity asserts on.
  - Honest, non-hype copy: "market-intelligence map / visualization", source-linked provenance + confidence, no live-data claim.
metrics:
  duration: ~6 min
  completed: 2026-06-21
  tasks: 3
  files: 5
---

# Phase 10 Plan 01: SEO, Social Cards & Metadata Summary

PERF-04 delivered: full SEO/OG/Twitter/JSON-LD `<head>` tag set in `index.html`, a real 1200×630 `assets/og-card.png` generated by the already-installed Playwright, and a registered `tests/seo-social.test.mjs` pinning every tag, the image dimensions, and the deploy path — full suite green at 301/301, index-ui-integrity untouched.

## What Was Built

- **`index.html <head>` metadata** — inserted after `<title>`/favicon, before the d3 script: honest `meta description`, `link rel="canonical"` to `https://akgularda.github.io/supplychain/`, the full Open Graph set (`og:type=website`, `og:site_name`, `og:title`, `og:description`, `og:url`, absolute `og:image`, `og:image:width=1200`, `og:image:height=630`, `og:image:alt`), Twitter `summary_large_image` set (`twitter:card/title/description/image`), and a WebSite+Organization (Monarch Castle Technologies) `application/ld+json` block. All URLs use the verbatim `/supplychain/` subpath. No `meta keywords` added. No bare `<script>` introduced.
- **`assets/og-card.png`** — a real 1200×630 dark-theme PNG (eyebrow "Monarch Castle Technologies", title "Market Intelligence", subtitle referencing top-100 supply chains + source-linked provenance, plus a decorative supply-chain network motif), rendered by `scripts/make-og-card.mjs` via Playwright chromium at a 1200×630 viewport. PNG header reports exactly width 1200, height 630.
- **`scripts/make-og-card.mjs`** — reproducible generator (the committed PNG is the deliverable).
- **`tests/seo-social.test.mjs`** — 7 string/regex + PNG-header + workflow assertions, registered at the end of `package.json scripts.test` (single `node --test` invocation preserved).

## Task Commits

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 0 | Author + register SEO/social test (RED) | d33ff2a | tests/seo-social.test.mjs, package.json |
| 1 | Add SEO/OG/Twitter/JSON-LD head tags | af8342d | index.html |
| 2 | Generate 1200×630 og-card.png via Playwright | efc6e36 | assets/og-card.png, scripts/make-og-card.mjs |

## Verification

- `npm test` → **301 tests, 301 pass, 0 fail** (294 prior + 7 new).
- `node --test tests/index-ui-integrity.test.mjs` → 5/5 pass (no head/ID regression; inline bootstrap is still the last attribute-less `<script>` before `</body>`).
- `assets/og-card.png` PNG header == 1200×630 (verified via `readUInt32BE(16)`/`(20)`).
- `tests/seo-social.test.mjs` present in `package.json scripts.test`.
- TDD gate sequence honored: `test(...)` RED (d33ff2a) → `feat(...)` GREEN (af8342d, efc6e36).

## Deviations from Plan

None — plan executed exactly as written. The exact research tag block, test code, and Playwright generator were used verbatim (the generator additionally includes a decorative SVG supply-chain motif, consistent with the research's "supply-chain graph motif" guidance).

## Threat Model Notes

- **T-10-01 (Info Disclosure):** all OG/JSON-LD copy is honest and PII-free; no secrets/tokens in any tag (test asserts the exact public strings). Mitigated.
- **T-10-02 (Tampering / integrity invariant):** JSON-LD carries `type="application/ld+json"` in `<head>`; no bare `<script>` added before `</body>`; index-ui-integrity ran green in verification. Mitigated.
- **T-10-SC (package installs):** no new packages added — Playwright was already a dependency. Accepted per research.

## Self-Check: PASSED

- FOUND: tests/seo-social.test.mjs
- FOUND: scripts/make-og-card.mjs
- FOUND: assets/og-card.png
- FOUND: commit d33ff2a
- FOUND: commit af8342d
- FOUND: commit efc6e36
