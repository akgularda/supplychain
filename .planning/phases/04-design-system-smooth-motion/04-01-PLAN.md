---
phase: 04-design-system-smooth-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/base.css
  - package.json
  - tests/design-tokens.test.mjs
  - tests/viz-motion.test.mjs
autonomous: true
requirements: [STORY-01, STORY-03]
must_haves:
  truths:
    - "base.css :root defines a full design-token set (color, type, spacing, radii, elevation, motion)"
    - "Semantic trust tokens (--color-observed/estimated/unknown) exist and match the .confidence-* hues"
    - "The 7 legacy vars (--bg --text --dim --acc --blue --green --purple) remain as aliases so existing rules keep resolving"
    - "tests/design-tokens.test.mjs and tests/viz-motion.test.mjs are registered in package.json scripts.test"
    - "npm test runs the two new files (no GATE LANDMINE)"
  artifacts:
    - path: "styles/base.css"
      provides: "Design-token :root block + legacy aliases"
      contains: "--color-observed"
    - path: "tests/design-tokens.test.mjs"
      provides: "Token-presence + trust-hook preservation assertions"
      min_lines: 30
    - path: "tests/viz-motion.test.mjs"
      provides: "Build-once / reduced-motion structure assertions (target files refactored in Plans 02/03)"
      min_lines: 25
    - path: "package.json"
      provides: "scripts.test registering both new files"
      contains: "design-tokens.test.mjs"
  key_links:
    - from: "styles/base.css :root"
      to: "components.css .confidence-*"
      via: "semantic trust tokens with identical hues"
      pattern: "--color-observed"
    - from: "package.json scripts.test"
      to: "tests/design-tokens.test.mjs"
      via: "node --test command line"
      pattern: "design-tokens.test.mjs"
---

## Phase Goal

**As an** investor, **I want to** view a visually coherent, world-class interface, **so that** the credibility of the data is reinforced by the quality of its presentation.

<objective>
Establish the design-token single source of truth in styles/base.css :root and stand up the Wave 0 test scaffolding (the failing structure tests plus their registration) that gate the entire phase.

Purpose: base.css loads first, so every consumer rule (layout/components/theme) resolves the tokens defined here. This plan defines the contract Plans 02 and 03 implement against, and registers the two new test files so they actually run (the documented GATE LANDMINE: unregistered files silently never run).

Output: a populated :root token block in base.css, two new test files, and updated package.json scripts.test. Implements STORY-01 (token foundation) and registers the STORY-03 motion test.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-system-smooth-motion/04-RESEARCH.md
@.planning/phases/04-design-system-smooth-motion/04-VALIDATION.md

<read_first>
- styles/base.css (current :root has only 7 vars on L4; @import fonts on L1; pageFadeIn keyframe)
- styles/components.css L49-54 (THE trust color contract: .source-link, .confidence-badge, .confidence-high/medium/low; hues rgba(76,175,80) green / rgba(255,193,7) amber / rgba(158,158,158) neutral)
- package.json scripts.test (current 14-file command line; append, do not replace)
- tests/methodology-wiring.test.mjs (convention: import test from node:test, assert from node:assert/strict, readFileSync + regex, resolve paths relative to import.meta.url)
- 04-RESEARCH.md Appendix Hardcoded-Value Catalog (authoritative token values; A3: cross-check each value)
- 04-RESEARCH.md Pattern 1 (the exact :root token block with legacy aliases)
- 04-RESEARCH.md "Recommended new test assertions" (concrete regexes for both test files)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write failing token + motion structure tests and register them</name>
  <files>tests/design-tokens.test.mjs, tests/viz-motion.test.mjs, package.json</files>
  <action>
Create tests/design-tokens.test.mjs following the node:test + node:assert/strict + readFileSync convention used by tests/methodology-wiring.test.mjs (resolve paths from import.meta.url, read target files as UTF-8 text, assert with regex; NO browser, NO jsdom). Assert against styles/base.css, styles/components.css, index.html, and js/trust/index.js per 04-RESEARCH "Recommended new test assertions": (a) base.css :root contains one token per family matching /--color-bg\b/, /--fs-base\b/, /--space-4\b/, /--radius-md\b/, /--shadow-md\b/, /--dur-base\b/, /--ease-standard\b/; (b) semantic trust tokens /--color-observed/, /--color-estimated/, /--color-unknown/; (c) legacy aliases retained /--bg\b/, /--text\b/, /--dim\b/, /--acc\b/, /--blue\b/, /--green\b/, /--purple\b/; (d) trust color contract preserved in components.css matching /rgba\(76,175,80/, /rgba\(255,193,7/, /rgba\(158,158,158/ plus literal .confidence-high/.confidence-medium/.confidence-low; (e) trust hooks unchanged in markup: index.html contains id="methodologyModal", id="provenanceDrawer", id="companyCard", and js/trust/index.js still emits the literal "prov-badge confidence-badge"; (f) migration evidence (asserted now, satisfied by Plan 02): layout.css, components.css, theme.css EACH contain at least 10 occurrences of "var(--" via a count assertion so it FAILS until Plan 02 migrates (RED is the intended Wave 0 state).

Create tests/viz-motion.test.mjs reading js/viz/index.js and styles/theme.css as text and asserting (per 04-RESEARCH): exactly one /d3\.forceSimulation\(/g occurrence (build-once); presence of /function buildSimulation/ and /function updateGraph/; re-bind present via a simulation /\.nodes\(/ call AND /force\(["']link["']\)\.links\(/; gentle reheat /alphaTarget\(/ AND assert.doesNotMatch(VIZ, /\balpha\(1\)/); mental-map carry /prev\.get\(/ or /\.x\s*=\s*p\.x/; reduced-motion /matchMedia\(['"]\(prefers-reduced-motion/ in viz AND /prefers-reduced-motion/ in theme.css; defensive trust-wiring preserved /provenanceFor\(/, /confidenceScore\(/, and the literal /Confidence:\s*\$\{\s*score\s*\}\s*%/. These WILL fail until Plan 02 (theme.css reduced-motion) and Plan 03 (viz refactor) land; that is the intended Wave 0 RED.

Append BOTH new filenames to the END of the existing package.json scripts.test "node --test ..." command line, preserving every one of the current 14 files exactly. Do not reorder or drop existing entries.
  </action>
  <verify>
    <automated>node --test tests/design-tokens.test.mjs tests/viz-motion.test.mjs; echo "exit=$?"; grep -o "[a-z-]*\.test\.mjs" package.json | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - tests/design-tokens.test.mjs and tests/viz-motion.test.mjs exist and are valid ESM node:test files (run without syntax/import errors)
    - package.json scripts.test includes both filenames at the end and retains all 14 prior files (16 unique .test.mjs references total)
    - Current failures are assertion failures only (the wired RED gate), not runtime errors
    - No existing source file (CSS/JS/markup) modified by this task
  </acceptance_criteria>
  <done>Both test files exist, run, and are registered; their failures are the expected Wave 0 RED state.</done>
</task>

<task type="auto">
  <name>Task 1: Define the full design-token :root block in base.css with legacy aliases</name>
  <files>styles/base.css</files>
  <action>
Replace the 7-var :root block (base.css L3-5) with the full token set from 04-RESEARCH Pattern 1, cross-checking each value against the Hardcoded-Value Catalog appendix (A3: a mistyped value is a visual regression). Define inside the SAME :root: color surfaces (--color-bg:#0a0a0a, --color-surface, --color-surface-raised, --color-border, --color-border-subtle); color text (--color-text:#d4d4d4, --color-text-bright, --color-text-muted, --color-text-dim, --color-text-faint); brand/accent (--color-accent:#e8453c, --color-blue, --color-link, --color-purple); status plus SEMANTIC TRUST tokens that EXACTLY match the .confidence-* hues: --color-observed:#66bb6a green, --color-estimated:#ffb300 amber, --color-unknown:#9e9e9e neutral, plus --color-success, --color-warning, --color-danger; type scale (--fs-2xs..--fs-2xl per the catalog 6px-20px steps, --fw-light..--fw-bold 300..700, --lh-tight/base/relaxed, --font-mono/--font-display/--font-ui); spacing (--space-1:2px..--space-8:24px); radii (--radius-xs:2px..--radius-2xl:12px); elevation (--shadow-sm/md/lg/up from the catalog box-shadow combos); motion (--dur-fastest/fast/base/slow, --ease-standard:cubic-bezier(0.4,0,0.2,1), --ease-emphasized:cubic-bezier(0.2,0,0,1)). Then KEEP the 7 legacy aliases pointing at new tokens: --bg:var(--color-bg); --text:var(--color-text); --dim:var(--color-text-dim); --acc:var(--color-accent); --blue:var(--color-blue); --green:var(--color-success); --purple:var(--color-purple). Leave the @import fonts (L1), the universal reset, the html/body rule, and the pageFadeIn keyframe untouched. Do NOT change any value the legacy aliases resolve to (zero visual change this plan; consumers migrate in Plan 02).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/base.css','utf8'); const need=['--color-bg','--color-observed','--color-estimated','--color-unknown','--fs-base','--space-4','--radius-md','--shadow-md','--dur-base','--ease-standard','--bg:var(--color-bg)','--acc:var(--color-accent)']; const miss=need.filter(t=>!c.includes(t)); console.log(miss.length?'MISSING '+miss.join(','):'ALL TOKENS PRESENT')"</automated>
  </verify>
  <acceptance_criteria>
    - base.css :root contains every token family token listed plus the 3 semantic trust tokens
    - All 7 legacy aliases present and pointing at new tokens (verify prints ALL TOKENS PRESENT)
    - design-tokens.test.mjs token/alias/trust-hook assertions now pass (migration-evidence assertion still RED until Plan 02)
    - @import, reset, html/body, and pageFadeIn untouched; no consumer rule edited
  </acceptance_criteria>
  <done>base.css :root is the token single source of truth with legacy aliases; token-presence tests pass; visual output unchanged (only base.css aliases resolve, same values as before).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored CSS to browser | Static authored tokens; no runtime user input flows into :root |
| test command line to runner | Unregistered test files silently never run (GATE LANDMINE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | base.css :root token values | mitigate | A3 cross-check each value against the RESEARCH catalog; legacy aliases keep resolved values identical (zero visual change) |
| T-04-02 | Repudiation | package.json scripts.test registration | mitigate | Task 0 appends both files; verify asserts 16 unique .test.mjs references so the gate cannot silently skip them |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH Package Legitimacy Audit: none); npm install unchanged |
</threat_model>

<verification>
- node --test tests/design-tokens.test.mjs tests/viz-motion.test.mjs runs both files (RED expected for migration/motion assertions until Plans 02/03)
- package.json scripts.test lists 16 unique .test.mjs files
- base.css token-presence verify prints ALL TOKENS PRESENT
- No existing CSS consumer rule, JS, or markup modified in this plan
</verification>

<success_criteria>
- Full token set defined in base.css :root with 7 legacy aliases (STORY-01 foundation)
- Both new test files exist and are registered (STORY-01 + STORY-03 gates wired)
- design-tokens token/alias/trust-hook assertions GREEN; migration-evidence + viz-motion assertions intentionally RED (closed by Plans 02/03)
</success_criteria>

<output>
Create `.planning/phases/04-design-system-smooth-motion/04-01-SUMMARY.md` when done
</output>
