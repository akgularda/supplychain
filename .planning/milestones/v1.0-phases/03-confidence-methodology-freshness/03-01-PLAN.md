---
phase: 03-confidence-methodology-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/top100-map.js
  - scripts/update-marketcap-data.mjs
  - package.json
  - tests/data-shape.test.mjs
autonomous: true
requirements: [TRUST-05, TRUST-06]
must_haves:
  truths:
    - "The served data/top100-map.js assigns window.SUPPLY_MAP_DATA the FULL rich shape (meta + nodes + links + profiles), so the live app paints with real data."
    - "The weekly auto-update can no longer overwrite the served file with a meta-less flat {companies} shape."
    - "A data-shape guard test fails loudly if meta.generatedAt, meta.lastUpdated, nodes, links, or profiles are missing from the served file."
    - "Every Phase-3 test filename is registered in package.json scripts.test before any of those tests are authored, closing the GATE LANDMINE."
  artifacts:
    - path: "data/top100-map.js"
      provides: "Served browser data in the rich shape (window.SUPPLY_MAP_DATA = {meta, layers, countries, nodes, links, profiles})"
      contains: "\"profiles\""
    - path: "tests/data-shape.test.mjs"
      provides: "Guard that the served .js has meta.generatedAt/lastUpdated + nodes + links + profiles"
      min_lines: 25
    - path: "scripts/update-marketcap-data.mjs"
      provides: "Quarantined/fixed weekly updater that never strips meta/profiles/nodes/links"
    - path: "package.json"
      provides: "scripts.test registering all Phase-3 .mjs test files"
      contains: "confidence-score.test.mjs"
  key_links:
    - from: "index.html"
      to: "data/top100-map.js"
      via: "<script src> assigning window.SUPPLY_MAP_DATA"
      pattern: "window\\.SUPPLY_MAP_DATA\\s*="
    - from: "tests/data-shape.test.mjs"
      to: "data/top100-map.js"
      via: "read served file, assert rich-shape keys"
      pattern: "profiles"
---

<objective>
Fix the BLOCKING served-file integrity defect and lay the Wave-0 test scaffolding for the whole phase.

The committed `data/top100-map.js` is currently the FLAT `{last_auto_update, companies:[...]}` shape with NO `meta`, `profiles`, `nodes`, or `links` — which breaks the live app (DATA.profiles undefined, tooltips throw, freshness shows "Unknown"). The rich source of truth is `data/top100-map.json` (`{meta, layers, countries, nodes, links, profiles}`). This plan restores the served `.js` to the rich shape, stops the weekly updater from re-breaking it, guards it with a test, and registers every Phase-3 test file in `package.json` up front so later parallel plans never touch `package.json`.

Purpose: Without the rich served shape, nothing else in Phase 3 can render or be verified (confidence tooltips, methodology modal, freshness all read `DATA.meta`/`DATA.profiles`). This is the gate for the entire phase.
Output: A rich-shape `data/top100-map.js`, a quarantined `update-marketcap-data.mjs`, a `tests/data-shape.test.mjs` guard, and a `package.json` scripts.test that lists all Phase-3 test files.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-confidence-methodology-freshness/03-RESEARCH.md

<interfaces>
<!-- Verified this session. Use directly — no exploration. -->

Served file `data/top100-map.js` (BROKEN, flat shape — current head):
  // Auto-generated market cap data - 2026-06-15T...
  window.SUPPLY_MAP_DATA = { "last_auto_update": "...", "update_source": "...", "snapshot_date": "...", "top100_source_url": "...", "companies": [ ... ] };
  // NO meta, NO profiles, NO nodes, NO links.

Source of truth `data/top100-map.json` (RICH shape — verified keys):
  { "meta": {"generatedAt":"2026-02-22T19:21:43.758Z","lastUpdated":"Feb 22, 2026, 10:21 PM","source":"...","count":100,"profileCount":100},
    "layers": {...}, "countries": {...}, "nodes": [100], "links": [186], "profiles": {100 symbols} }

scripts/update-marketcap-data.mjs (the CULPRIT — writeDataFiles at ~line 256-266):
  writes data/top100-map.js as `window.SUPPLY_MAP_DATA = ${JSON.stringify(flatData)}` where flatData = {snapshot_date, last_auto_update, update_source, companies:[...]}.
  Reads its prior state from data/top100-map-updated.json (flat). This codepath NEVER carried meta/profiles/nodes/links.

scripts/generate-top100-data.mjs (the CORRECT generator) writes BOTH:
  OUTPUT_JSON_PATH = data/top100-map.json  and  OUTPUT_JS_PATH = data/top100-map.js
  from the same rich `payload` (main() at ~line 3373; fetches CSV over network at line 3375 — requires network, may be unavailable in sandbox).

package.json scripts.test (current — 144 pass) lists exactly:
  tests/index-ui-integrity.test.mjs tests/no-xx-country-codes.test.mjs tests/profile-link-metadata.test.mjs
  tests/supply-chain-research-quality.test.mjs tests/auto-update-script.test.mjs tests/provenance.test.mjs
  tests/viz-provenance-wiring.test.mjs tests/ui-provenance-wiring.test.mjs tests/trust-wiring.test.mjs

Existing test convention (tests/viz-provenance-wiring.test.mjs): node:test + node:assert/strict, read file as string via
  readFileSync(join(__dirname,"..","js","viz","index.js"),"utf8"); assert.match(...). Node root package.json is type=commonjs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Restore the served data/top100-map.js to the rich shape</name>
  <read_first>data/top100-map.js (confirm flat shape), data/top100-map.json (confirm rich shape), scripts/generate-top100-data.mjs main()/writeFile lines ~3373-3458, index.html script tag that loads ./data/top100-map.js</read_first>
  <files>data/top100-map.js</files>
  <action>Make `data/top100-map.js` assign `window.SUPPLY_MAP_DATA` the FULL rich object (meta + layers + countries + nodes + links + profiles). PREFER regenerating from `scripts/generate-top100-data.mjs` — run `node scripts/generate-top100-data.mjs` and confirm it rewrote BOTH data/top100-map.json AND data/top100-map.js with the rich payload. If that script needs network and the fetch fails/unavailable in this environment, FALL BACK to deriving the served .js from the committed rich `data/top100-map.json`: read the JSON, and write `data/top100-map.js` as a one-line-header comment plus `window.SUPPLY_MAP_DATA = ` followed by the JSON text and a trailing semicolon (mirror the exact emit format generate-top100-data.mjs uses at OUTPUT_JS_PATH: `window.SUPPLY_MAP_DATA = ${JSON.stringify(payload, null, 2)};\n`). Do NOT hand-edit company numbers — the .json is the frozen source of truth (data contract). Do NOT change data/top100-map.json. After writing, the served .js must NOT contain `last_auto_update` or a top-level `companies` key, and MUST contain `meta`, `profiles`, `nodes`, `links`.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('data/top100-map.js','utf8');const ok=s.includes('SUPPLY_MAP_DATA')&&/[\"]meta[\"]\s*:/.test(s)&&/[\"]profiles[\"]\s*:/.test(s)&&/[\"]nodes[\"]\s*:/.test(s)&&/[\"]links[\"]\s*:/.test(s)&&!s.includes('last_auto_update');if(!ok){console.error('rich-shape check FAILED');process.exit(1);}console.log('rich shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/top100-map.js` contains `window.SUPPLY_MAP_DATA = {...}` with top-level keys meta, layers, countries, nodes, links, profiles.
    - The served `.js` no longer contains `last_auto_update` or a top-level `companies` array.
    - `data/top100-map.json` is byte-unchanged (the data contract is preserved).
    - meta carries `generatedAt` (ISO) and `lastUpdated` (display string) exactly as in the .json.
  </acceptance_criteria>
  <done>The browser-loaded data file carries the full rich shape; the app can resolve DATA.meta/DATA.profiles/DATA.nodes/DATA.links.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Quarantine the weekly updater so it can never strip meta/profiles, and register all Phase-3 tests</name>
  <read_first>scripts/update-marketcap-data.mjs (writeDataFiles ~256-266, mergeData/createFreshDataset ~116-223), .github/workflows/auto-update-data.yml (runs node scripts/update-marketcap-data.mjs at ~line 37), package.json scripts.test, tests/auto-update-script.test.mjs (so the quarantine does not break the existing registered test)</read_first>
  <files>scripts/update-marketcap-data.mjs, package.json</files>
  <action>(a) Fix `scripts/update-marketcap-data.mjs` so a weekly run can NEVER overwrite `data/top100-map.js` with a meta/profiles-less shape. Make `writeDataFiles` stop emitting the flat shape to `data/top100-map.js`: either redirect its market-cap output to a side file (e.g. keep writing `data/top100-map-updated.json` only) and DELETE the `top100-map.js` write entirely, OR convert `writeDataFiles` to load the rich `data/top100-map.json`, splice ONLY the refreshed rank/marketcap values into the existing rich nodes/profiles, and re-emit the rich shape — choose the smaller, safer change that keeps the existing `tests/auto-update-script.test.mjs` green. Add a top-of-function guard comment referencing RESEARCH Pitfall 1. Confirm `tests/auto-update-script.test.mjs` still passes against the modified script. (b) In `package.json`, append ALL Phase-3 test filenames to scripts.test in one edit so downstream parallel plans never touch package.json: `tests/confidence-score.test.mjs tests/viz-confidence-wiring.test.mjs tests/methodology-wiring.test.mjs tests/freshness-wiring.test.mjs tests/data-shape.test.mjs`. (These files are authored in Tasks below and in plans 02/03; an empty test file is created in Task 3 and as stubs so `npm test` stays green from this point — see Task 3.)</action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8')).scripts.test;const need=['confidence-score','viz-confidence-wiring','methodology-wiring','freshness-wiring','data-shape'];const miss=need.filter(n=>!p.includes(n+'.test.mjs'));if(miss.length){console.error('unregistered:',miss);process.exit(1)}const u=fs.readFileSync('scripts/update-marketcap-data.mjs','utf8');if(/writeFileSync\([^)]*top100-map\.js/.test(u)){console.error('updater still writes top100-map.js flat — quarantine incomplete');process.exit(1)}console.log('registration + quarantine OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` scripts.test lists confidence-score, viz-confidence-wiring, methodology-wiring, freshness-wiring, and data-shape test files (in addition to the existing 9).
    - `scripts/update-marketcap-data.mjs` no longer calls `writeFileSync(... top100-map.js ...)` with the flat shape (verified by the grep gate above).
    - `tests/auto-update-script.test.mjs` still passes against the modified updater.
  </acceptance_criteria>
  <done>The weekly pipeline cannot regress the served file; the GATE LANDMINE is closed (all Phase-3 test files registered up front).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the data-shape guard test (and create empty stubs for the still-unwritten Phase-3 test files)</name>
  <read_first>tests/viz-provenance-wiring.test.mjs (convention: node:test, node:assert/strict, readFileSync string asserts), data/top100-map.js (post-Task-1 rich shape), package.json scripts.test (post-Task-2 — references the stub files)</read_first>
  <files>tests/data-shape.test.mjs</files>
  <behavior>
    - Reading data/top100-map.js as a string: it assigns window.SUPPLY_MAP_DATA and contains the keys meta, nodes, links, profiles.
    - Evaluated/loaded shape: window.SUPPLY_MAP_DATA.meta.generatedAt is a parseable ISO date; meta.lastUpdated is a non-empty string; nodes and links are non-empty arrays; profiles has >= 1 key.
    - It does NOT contain a top-level `companies` array nor `last_auto_update` (regression guard for the flat shape).
  </behavior>
  <action>Create `tests/data-shape.test.mjs` following the repo's string-presence + light-eval convention. Read `data/top100-map.js`, strip the leading comment + the `window.SUPPLY_MAP_DATA = ` prefix and trailing `;` to obtain the JSON object (or define a minimal `window` shim and evaluate the assignment), then assert: meta.generatedAt parses via `new Date(...).getTime()` (Number.isFinite), meta.lastUpdated is a non-empty string, Array.isArray(nodes) && nodes.length>0, Array.isArray(links) && links.length>0, Object.keys(profiles).length>0, and that the raw string does NOT match /last_auto_update/ nor a top-level "companies" key. Then create EMPTY placeholder files `tests/confidence-score.test.mjs`, `tests/viz-confidence-wiring.test.mjs`, `tests/methodology-wiring.test.mjs`, `tests/freshness-wiring.test.mjs` each containing only a single trivially-passing test (e.g. `import {test} from "node:test"; import assert from "node:assert/strict"; test("placeholder — implemented in plan 02/03", () => assert.ok(true));`) so that `npm test` stays GREEN now that Task 2 registered them. Plans 02 and 03 replace these stubs with real assertions.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/data-shape.test.mjs` passes against the rich served file and would FAIL if the flat shape were restored (manually reason: the meta/profiles asserts would throw).
    - The four placeholder test files exist and each contains exactly one passing test.
    - `npm test` exits 0 with at least 145 passing (144 prior + new data-shape suite; placeholders add their trivial cases).
  </acceptance_criteria>
  <done>Served-file integrity is guarded by an automated test in the registered suite; all Phase-3 test slots exist and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weekly CI updater → committed served data file | An automated job rewrites the file the browser loads; a wrong shape silently breaks the whole app. |
| data file → browser runtime | Injected `window.SUPPLY_MAP_DATA` is consumed without validation by viz/ui/data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | scripts/update-marketcap-data.mjs writing data/top100-map.js | mitigate | Quarantine the flat-shape write (Task 2); grep gate asserts the updater no longer writes top100-map.js flat. |
| T-03-02 | Denial of Service (app-break) | data/top100-map.js wrong shape at load | mitigate | Restore rich shape (Task 1) + data-shape guard test (Task 3) that fails CI if meta/profiles/nodes/links missing. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH: zero installs). No registry interaction; nothing to slopcheck. |
</threat_model>

<verification>
- `npm test` exits 0 (>= 145 passing) with all Phase-3 test files registered.
- `data/top100-map.js` carries the rich shape; `data/top100-map.json` unchanged.
- `scripts/update-marketcap-data.mjs` cannot rewrite the served file with the flat shape.
</verification>

<success_criteria>
The live app can load real data (meta + nodes + links + profiles); the weekly pipeline cannot regress it; a registered guard test enforces the shape; all Phase-3 test filenames are registered so no later plan touches package.json.
</success_criteria>

<output>
Create `.planning/phases/03-confidence-methodology-freshness/03-01-SUMMARY.md` when done.
</output>
