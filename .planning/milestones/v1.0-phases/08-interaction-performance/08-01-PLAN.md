---
phase: 08-interaction-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/analytics/index.js
  - tests/analytics-memo.test.mjs
  - package.json
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "Repeated companyConcentration/supplierCriticality/runScenario calls return byte-identical values to a fresh compute (anchors hold)"
    - "A second identical call is served from cache without rebuilding the fan-in map (build count stays 1, hit count increments)"
    - "__resetAnalyticsCache() re-arms the cache and a post-reset recompute returns the same values"
    - "buildSupplierFanIn is built once per session and shared by all four hotspots via memoization"
  artifacts:
    - path: "js/analytics/index.js"
      provides: "_memo Map cache + __resetAnalyticsCache() + __memoStats() seams + memoized buildSupplierFanIn/companyConcentration/supplierCriticality/runScenario"
      contains: "__resetAnalyticsCache"
    - path: "tests/analytics-memo.test.mjs"
      provides: "value-equality + cache-hit + reset-seam unit tests"
      contains: "__resetAnalyticsCache"
    - path: "package.json"
      provides: "scripts.test registers tests/analytics-memo.test.mjs and tests/no-restart-invariant.test.mjs"
      contains: "analytics-memo.test.mjs"
  key_links:
    - from: "companyConcentration / supplierCriticality / runScenario"
      to: "buildSupplierFanIn"
      via: "memoized shared dependency (default-arg path is O(1) after first build)"
      pattern: "_memo\\(\"fanIn\""
    - from: "companyConcentration cache key"
      to: "excludeSuppliers + opts weights"
      via: "stable string signature including the sorted exclude set"
      pattern: "excludeSuppliers"
---

<objective>
Make repeated analytics interactions O(1) without changing a single computed value. Add a tiny
per-session Map-based memo to js/analytics, wrap the four recompute hotspots (buildSupplierFanIn FIRST
— the shared hot dependency — then companyConcentration, supplierCriticality, runScenario), and expose
test seams (__resetAnalyticsCache + __memoStats). This is Wave 0 for the phase: it also REGISTERS both
new phase test files in package.json scripts.test so the gate command runs them.

Purpose: PERF-01 — memoized filter/style/analytics interactions. Caching changes COST, never VALUE;
the real anchors (GILD=36, NVDA=12, top chokepoint fan-in=4, Taiwan 7 firms / $11,360,589,871,184)
stay identical because DATA is frozen per session (no in-session writer => no invalidation).
Output: memoized js/analytics, tests/analytics-memo.test.mjs, updated package.json scripts.test.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interaction-performance/08-RESEARCH.md

<read_first>
- js/analytics/index.js — the pure engine. The memoization targets and their PARAM-DEFAULT lines:
  buildSupplierFanIn(profiles = DATA.profiles) at L34; companyConcentration opts default
  fanIn = buildSupplierFanIn(profiles) at L52; supplierCriticality opts default at L137;
  runScenario ctx fanIn default + internal fan = fanIn || buildSupplierFanIn(profiles) at L158-159.
  companyConcentration value-affecting opts: wHHI(0.6), wShared(0.4), sharedThreshold(1), profiles,
  fanIn, excludeSuppliers (Set|string[]|undefined). runScenario disruption accepts BOTH
  disableSuppliers[] and disableSupplier (normalized to a label set at L161-169).
- data/top100-map.json — the real frozen fixture the test loads (has nodes + profiles).
- package.json — scripts.test is an explicit space-separated file list (currently 22 files ending
  ...scenario.test.mjs scenario-wiring.test.mjs). Append the two new files.
- tests/concentration.test.mjs, tests/scenario.test.mjs — the existing anchor assertions
  (GILD=36, NVDA=12, fan-in top "credit and risk data inputs"=4, Taiwan 7/$11360589871184) that
  MUST keep passing unchanged.
</read_first>

<interfaces>
<!-- Exports the memo must preserve byte-for-byte (signatures unchanged; only internals + new seams). -->
From js/analytics/index.js:
  export function buildSupplierFanIn(profiles = DATA.profiles): Map<supplierLabel, Set<symbol>>
  export function companyConcentration(symbol, opts={}): {symbol,suppliers,hhi,sharedCount,sharedFrac,score,n} | null
  export function supplierCriticality(opts={}): {supplier,fanIn}[]
  export function runScenario(disruption={}, ctx={}): {impactedCompanies,totalMarketCapExposed,supplierCount,disabled}
  export const SCENARIO_PRESETS
  // NEW seams to add:
  export function __resetAnalyticsCache(): void
  export function __memoStats(): { <bucket>: { builds:number, hits:number } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Register both phase test files in package.json scripts.test (Wave 0 gate)</name>
  <files>package.json</files>
  <read_first>package.json — the single-line scripts.test value, a space-separated `node --test tests/...` list ending in `tests/scenario-wiring.test.mjs`.</read_first>
  <action>Append `tests/analytics-memo.test.mjs` and `tests/no-restart-invariant.test.mjs` to the END of the scripts.test command string (after `tests/scenario-wiring.test.mjs`), preserving the existing 22 entries and the leading `node --test`. Register BOTH now even though no-restart-invariant.test.mjs is authored in Plan 02 — registering the not-yet-created file is the Wave-0 gate pattern (an unregistered test silently never runs). Do not reorder or remove any existing entry. No other key changes.</action>
  <acceptance_criteria>
    - scripts.test contains both `tests/analytics-memo.test.mjs` and `tests/no-restart-invariant.test.mjs`.
    - All 22 prior test files still present in the command.
    - package.json remains valid JSON (parses).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('./package.json').scripts.test; if(!t.includes('analytics-memo.test.mjs')||!t.includes('no-restart-invariant.test.mjs')) process.exit(1); if((t.match(/tests\//g)||[]).length!==24) process.exit(1); console.log('OK 24 test files registered')"</automated>
  </verify>
  <done>Both new test files registered in scripts.test; 24 total entries; valid JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: RED — write tests/analytics-memo.test.mjs (value-equality, cache-hit via __memoStats, reset seam)</name>
  <files>tests/analytics-memo.test.mjs</files>
  <read_first>08-RESEARCH.md "Memoization unit test skeleton" (the import list, fixture load from data/top100-map.json, and the four test bodies) and "Open Questions #1" (expose __memoStats returning build/hit counts rather than ESM mocking).</read_first>
  <behavior>
    - companyConcentration("GILD",{profiles}) twice => deepEqual; .score === 36; companyConcentration("NVDA",{profiles}).score === 12.
    - After __resetAnalyticsCache(): two supplierCriticality({profiles}) calls => __memoStats().fanIn.builds === 1 and .hits >= 1 (second call served from cache, fan-in not rebuilt).
    - supplierCriticality({profiles,limit:3}) before vs after __resetAnalyticsCache() => deepEqual; after[0].supplier === "credit and risk data inputs".
    - runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption,{profiles,nodes}) => impactedCompanies.length === 7 and totalMarketCapExposed === 11360589871184; a second call with the equivalent single-vs-array disruption shape hits the same cache entry (normalized sorted disabled set as key).
  </behavior>
  <action>Create tests/analytics-memo.test.mjs importing companyConcentration, supplierCriticality, runScenario, buildSupplierFanIn, SCENARIO_PRESETS, __resetAnalyticsCache, __memoStats from ../js/analytics/index.js; load data via JSON.parse(fs.readFileSync("data/top100-map.json","utf8")) and destructure profiles + nodes. Write the four tests from the behavior block. Call __resetAnalyticsCache() at the start of each test for isolation. For the cache-hit test assert __memoStats().fanIn.builds === 1 after two supplierCriticality calls AND __memoStats().fanIn.hits >= 1. For the runScenario shape-equivalence assertion, run once with disableSuppliers:["tsmc"] then once with disableSupplier:"tsmc" and assert __memoStats().scenario.builds === 1 (same normalized key). This task is RED: __memoStats/__resetAnalyticsCache and the memo do not exist yet, so the import or the build-count assertions fail. Run the file and CONFIRM failure before Task 2. NO fenced code in this plan — follow the RESEARCH skeleton.</action>
  <acceptance_criteria>
    - File exists, imports the seven named exports.
    - Running it FAILS now (RED) due to missing __memoStats/__resetAnalyticsCache or unmemoized build counts.
    - Asserts all four anchors literally (36, 12, "credit and risk data inputs", 7, 11360589871184).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/analytics-memo.test.mjs; test $? -ne 0 && echo "RED confirmed" || (echo "expected RED" && exit 1)</automated>
  </verify>
  <done>RED test file committed; fails for the right reason (no memo/seams yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add _memo + seams and memoize buildSupplierFanIn → companyConcentration → supplierCriticality → runScenario</name>
  <files>js/analytics/index.js</files>
  <read_first>08-RESEARCH.md Pattern 1 (_caches/_bucket/_memo helper, ~12 lines), Pattern 2 (wrap buildSupplierFanIn FIRST; key "default" when profiles===DATA.profiles), and the Anti-Patterns/Pitfall-1 notes (key companyConcentration on wHHI+wShared+sharedThreshold+profiles-identity-tag+SORTED excludeSuppliers signature; key runScenario on the NORMALIZED SORTED disabled label set — never the raw arg).</read_first>
  <action>Add a module-private memo layer near the top of js/analytics/index.js: a _caches Map<bucketName, Map<key,value>>, a _bucket(name) accessor, and _memo(name,key,compute) that returns cached on hit (incrementing a per-bucket hit counter) else computes+stores (incrementing a per-bucket build counter). Track counts in a parallel _stats object so __memoStats() returns {fanIn:{builds,hits}, concentration:{...}, criticality:{...}, scenario:{...}}. Export __resetAnalyticsCache() that clears _caches AND zeroes _stats, and __memoStats(). Rename the current buildSupplierFanIn body to an internal _buildSupplierFanInUncached(profiles) and make buildSupplierFanIn(profiles=DATA.profiles) return _memo("fanIn", profiles===DATA.profiles?"default":_sig(profiles), ()=>_buildSupplierFanInUncached(profiles)) — this fixes the eager default-arg rebuild at L52/137/159. Wrap companyConcentration result in _memo("concentration", key, ...) where key = `${symbol}|${wHHI}|${wShared}|${sharedThreshold}|${profiles===DATA.profiles?"D":"X"}|${[...excludeSet].sort().join(",")}` (excludeSet derived from the same normalization the fn already does); include excludeSuppliers in the key — this is the #1 correctness risk (runScenario reads excluded scores). Wrap supplierCriticality in _memo("criticality", `${profiles-tag}|${limit ?? "all"}`, ...). Wrap runScenario in _memo("scenario", `${[...disabled].sort().join(",")}|${profiles-tag}`, ...) where disabled is the existing normalized set (compute the key AFTER normalization so disableSuppliers:["tsmc"] and disableSupplier:"tsmc" collide). Do NOT change any formula, weight, clamp, sort, or return shape. Keep the additive excludeSuppliers behavior byte-identical (undefined => legacy output). Run the memo test GREEN, then run the full suite to prove anchors unchanged.</action>
  <acceptance_criteria>
    - __resetAnalyticsCache() and __memoStats() exported; tests/analytics-memo.test.mjs passes (GREEN).
    - __memoStats().fanIn.builds === 1 after two supplierCriticality calls (fan-in built once).
    - companyConcentration cache key includes the sorted excludeSuppliers set (scenario reads correct excluded score).
    - runScenario single-vs-array disruption shapes hit the same scenario cache entry.
    - Full suite green: `npm test` exit 0 with anchors GILD=36, NVDA=12, fan-in top=4, Taiwan 7/$11360589871184 unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/analytics-memo.test.mjs && npm test</automated>
  </verify>
  <done>Memo + seams implemented; memo test GREEN; full suite (257 + new) green; zero value changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → cached analytics return | A caller could mutate a shared cached object, poisoning later reads |
| cache key derivation | A key that omits a value-affecting input would return a wrong-but-consistent value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | cached companyConcentration/runScenario object reused by reference | accept | Callers in js/ui/js/viz only READ returns (verified: values rendered, never mutated); cache lifetime is per-session, frozen data. No freeze added to avoid behavioral change. |
| T-08-02 | Tampering/Info | cache key collision (omitting excludeSuppliers / un-normalized disruption shape) | mitigate | Key companyConcentration on sorted excludeSuppliers + all opts; key runScenario on the NORMALIZED sorted disabled set; existing concentration/scenario anchor tests catch a too-coarse key. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan installs NO packages (Node built-ins + already-installed tooling per 08-RESEARCH Package Legitimacy Audit). No slopcheck surface. |
</threat_model>

<verification>
- `node --test tests/analytics-memo.test.mjs` GREEN (value-equality + cache-hit + reset seam).
- `npm test` exit 0; anchors unchanged (GILD=36, NVDA=12, fan-in top "credit and risk data inputs"=4, Taiwan 7 / $11,360,589,871,184).
- scripts.test now lists 24 test files (both phase files registered).
</verification>

<success_criteria>
- buildSupplierFanIn memoized FIRST (built once per session; default-arg path O(1) thereafter).
- companyConcentration, supplierCriticality, runScenario served from per-session caches keyed on stable
  signatures (excludeSuppliers in the concentration key; normalized sorted disabled set in the scenario key).
- __resetAnalyticsCache() + __memoStats() test seams exported; cache-hit proven (build=1, hit>=1).
- Zero value changes; full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-interaction-performance/08-01-SUMMARY.md` when done.
</output>
