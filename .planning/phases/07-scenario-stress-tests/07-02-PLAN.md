---
phase: 07-scenario-stress-tests
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - index.html
  - js/ui/index.js
  - tests/scenario-wiring.test.mjs
  - package.json
autonomous: true
requirements: [DEPTH-03, DEPTH-04]
user_setup: []

must_haves:
  truths:
    - "A #scenarioPanel renders a Taiwan-preset button + chokepoint <select> + reset, mirroring #chokepointsPanel"
    - "Running the Taiwan preset shows '7 companies impacted · $11.36T market cap exposed' (derived live, never hardcoded)"
    - "Impacted companies are highlighted in the global graph via highlightBy(n => impacted.has(n.symbol))"
    - "Scenario output renders the derived provenance ('Derived' badge + 'computed from N relationships' + Methodology link), NEVER 'Observed'"
    - "Methodology modal explains the scenario model: single-hop, HHI delta, 'exposure not loss', TSMC label set"
  artifacts:
    - path: "index.html"
      provides: "#scenarioPanel + #scenarioSummary/#scenarioImpactList/#scenarioProv + #bScenarioTaiwan/#scenarioChokepointSelect/#bScenarioReset + methodology scenario block"
      contains: "scenarioPanel"
    - path: "js/ui/index.js"
      provides: "runTaiwanScenario / renderScenario / highlightImpacted + bootstrap wiring + analytics import"
      contains: "runScenario"
    - path: "tests/scenario-wiring.test.mjs"
      provides: "panel/import/highlight + derived-provenance string + methodology copy tests"
      contains: "scenarioPanel"
  key_links:
    - from: "js/ui/index.js renderScenario"
      to: "badgeHtml(provenanceFor({derived:true,n}))"
      via: "#scenarioProv innerHTML"
      pattern: "derived: true"
    - from: "js/ui/index.js highlightImpacted"
      to: "highlightBy"
      via: "impacted symbol set predicate"
      pattern: "highlightBy\\("
    - from: "index.html #bScenarioTaiwan"
      to: "runTaiwanScenario"
      via: "addEventListener in wireUI"
      pattern: "bScenarioTaiwan"
---

<objective>
Ship the scenario UI slice (DEPTH-03 + DEPTH-04): a `#scenarioPanel` mirroring `#chokepointsPanel` with a Taiwan-preset button, a generalized chokepoint `<select>` (populated from `supplierCriticality`), an impact summary + list, a graph highlight of impacted companies, the honest `derived` provenance badge, and the Methodology copy explaining the scenario model. After this plan a real user can run a what-if disruption and see live downstream impact with full provenance.

Purpose: This is the user-facing payoff of the engine from Plan 01 — the differentiator becomes interactive. Per locked decisions: mirror #chokepointsPanel, NEW IDs only, preserve the inline bootstrap + all prior IDs.
Output: index.html panel + methodology block, js/ui render/highlight/wiring, and tests/scenario-wiring.test.mjs registered + green.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scenario-stress-tests/07-RESEARCH.md
@.planning/phases/07-scenario-stress-tests/07-01-SUMMARY.md

@js/ui/index.js
@index.html

<interfaces>
<!-- Contracts the UI consumes. Extracted from the codebase — use directly, no exploration. -->

From js/analytics/index.js (Plan 01):
- export function runScenario(disruption, ctx) -> { impactedCompanies:[{symbol,company,marketcap,lostSuppliers,suppliersBefore,suppliersAfter,concentrationBefore,concentrationAfter,severity}], totalMarketCapExposed, supplierCount, disabled }
- export const SCENARIO_PRESETS  (TAIWAN_SEMI.disruption.disableSuppliers = 5 TSMC labels)
- export function supplierCriticality({profiles, limit}) -> [{supplier, fanIn}]

From js/trust/index.js:
- export function provenanceFor({derived:true, n}, {methodologyUrl}) -> {tag:'derived', note:'computed from N relationships', source}
- export function badgeHtml(prov) -> emits "Derived" (confidence-medium), NEVER "Observed"

From js/viz/index.js (already imported by js/ui line 12):
- export function highlightBy(fn)   // global-mode company nodes carry .symbol
- export function resetHighlight()

From js/ui/index.js (mirror these):
- chokepoints trio renderChokepoints/highlightChokepoints (lines 626-660), DOM refs (lines 39-42), bootstrap wiring (lines 869-872), escapeHtml import (line 8), analytics import (line 16)

From index.html:
- #chokepointsPanel block lines 274-282 (copy structure for #scenarioPanel; insert as its sibling, before #tt at line 284)
- methodology modal: chokepoints <h3> at line 117, "Known limits" <h3> at line 120 (insert scenario block between them)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Register scenario-wiring test + write the failing wiring/provenance spec (RED)</name>
  <read_first>package.json scripts.test, tests/criticality-wiring.test.mjs (string/wiring assertion style: readFileSync over index.html + js/ui/index.js), 07-RESEARCH.md "Phase Requirements → Test Map" rows for scenario-wiring</read_first>
  <action>Append `tests/scenario-wiring.test.mjs` to package.json scripts.test (after tests/scenario.test.mjs). Create tests/scenario-wiring.test.mjs as string/wiring tests reading the source files (mirror tests/criticality-wiring.test.mjs's readFileSync approach — these assert wiring, not runtime). Assertions: (1) index.html contains id="scenarioPanel", id="scenarioSummary", id="scenarioImpactList", id="scenarioProv", id="bScenarioTaiwan", id="scenarioChokepointSelect", id="bScenarioReset"; (2) js/ui/index.js imports runScenario and SCENARIO_PRESETS from ../analytics/index.js; (3) js/ui/index.js calls highlightBy with an impacted-symbol predicate (matches /impacted.*\.symbol/ or a highlightImpacted fn referencing highlightBy); (4) js/ui/index.js renders the derived badge for scenario output (matches provenanceFor({ derived: true and a #scenarioProv assignment); (5) index.html methodology modal contains the scenario block text — assert it mentions "single-hop" (or "direct dependents"), "HHI", and "exposure" (not loss) and references the Taiwan/TSMC label set. These MUST FAIL now (RED) because none of the markup/wiring exists yet. Also assert that the scenario summary/list code does NOT contain a hardcoded "7" or "11.36" literal coupled to the headline (derive-live guard) — assert the UI string uses impactedCompanies.length and totalMarketCapExposed, not literals.</action>
  <verify>
    <automated>node --test tests/scenario-wiring.test.mjs 2>&1 | grep -q "fail" &amp;&amp; echo "RED as intended"</automated>
  </verify>
  <done>tests/scenario-wiring.test.mjs registered in package.json and present; running it FAILS (RED) because the panel/wiring/methodology copy do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 1: Add #scenarioPanel markup + methodology scenario block (index.html)</name>
  <files>index.html</files>
  <read_first>index.html #chokepointsPanel lines 274-282 (structure + classes cSecTitle/cMuted/cList/cActions), methodology modal lines 109-125 (chokepoints h3 at 117, Known limits h3 at 120)</read_first>
  <action>Insert a #scenarioPanel block as a sibling of #chokepointsPanel (after line 282, before #tt at line 284), copying the #chokepointsPanel structure exactly: role="region", a .cSecTitle "Scenario Stress-Test", a .cMuted intro ("Run a what-if disruption — direct downstream impact (single-hop)"), a #scenarioSummary line, a #scenarioProv span (derived-badge target) in the header area, a .cList #scenarioImpactList, and a .cActions row containing a #bScenarioTaiwan button ("Run: Taiwan semiconductor disruption"), a #scenarioChokepointSelect <select> (generalized chokepoint control, options populated at runtime by js/ui), and a #bScenarioReset button ("Reset"). NEW IDs only — do not rename or remove any existing ID; preserve the inline bootstrap script. Add aria-labels mirroring the chokepoint buttons. In the methodology modal, insert an <h3>Scenario stress-tests (derived)</h3> block between the chokepoints h3 (line 117 block) and the "Known limits" h3 (line 120), stating per 07-RESEARCH "Methodology copy additions": the model (disable one or more real suppliers → list companies that lose ≥1 supplier), single-hop / direct-dependents only (cascade out of scope for v1), concentration shift reported as HHI=1/k (rises on removal; composite not used because non-monotonic), "market cap exposed" is the combined market cap of impacted firms (exposure, NOT a loss estimate), and that the Taiwan preset disables the real TSMC supplier-label variants found in the data. Honest copy only — no fabricated cascade.</action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8');['scenarioPanel','scenarioSummary','scenarioImpactList','scenarioProv','bScenarioTaiwan','scenarioChokepointSelect','bScenarioReset'].forEach(id=>{if(!h.includes('id=\"'+id+'\"'))throw new Error('missing '+id)});if(!/single-hop|direct dependents/i.test(h)||!/exposure/i.test(h))throw new Error('methodology copy missing');console.log('html ok')"</automated>
  </verify>
  <done>index.html has #scenarioPanel (with all 7 new IDs) as a sibling of #chokepointsPanel; methodology modal has the scenario block with single-hop + HHI + exposure-not-loss copy; all prior IDs and the inline bootstrap intact (index-ui-integrity stays green).</done>
</task>

<task type="auto">
  <name>Task 2: Wire renderScenario/highlightImpacted/runTaiwanScenario + select + bootstrap (js/ui) and go GREEN</name>
  <files>js/ui/index.js</files>
  <read_first>js/ui/index.js: analytics import line 16, DOM refs lines 39-42, chokepoints trio lines 626-660, bootstrap wiring lines 869-872, escapeHtml import line 8, badgeHtml/provenanceFor import line 15; 07-RESEARCH.md "Code Examples" + "Graph highlight"</read_first>
  <action>Add runScenario, SCENARIO_PRESETS to the analytics import at line 16 (alongside companyConcentration, supplierCriticality). Add cached DOM refs for #scenarioPanel/#scenarioSummary/#scenarioImpactList/#scenarioProv/#scenarioChokepointSelect near the chokepoints refs (lines 39-42). Beside the chokepoints trio (after line 660), add three functions per 07-RESEARCH "Code Examples": (a) renderScenario(result) — write `${result.impactedCompanies.length} companies impacted · $${(result.totalMarketCapExposed/1e12).toFixed(2)}T market cap exposed` into #scenarioSummary (NO hardcoded 7/11.36 — derive from result), render each impacted firm into #scenarioImpactList as an escaped .cItem showing company (symbol), lostSuppliers count, and the HHI delta `${concentrationBefore.toFixed(2)}→${concentrationAfter.toFixed(2)} HHI` and "5→4 suppliers", and set #scenarioProv innerHTML to badgeHtml(provenanceFor({derived:true, n}, {methodologyUrl:"#methodology"})) where n = sum of lostSuppliers lengths; (b) highlightImpacted(result) — const impacted = new Set(result.impactedCompanies.map(c=>c.symbol)); highlightBy(n => impacted.has(n.symbol)); (c) runTaiwanScenario() — const result = runScenario(SCENARIO_PRESETS.TAIWAN_SEMI.disruption, {profiles:DATA.profiles, nodes:DATA.nodes}); renderScenario(result); highlightImpacted(result). Add a runChokepointScenario(label) helper that runs runScenario({disableSupplier:label}) for the generalized <select> control. In wireUI bootstrap (after line 872): populate #scenarioChokepointSelect options from supplierCriticality({profiles:DATA.profiles, limit:8}) (escaped supplier label as text + value), addEventListener("change") → runChokepointScenario(select.value) when set, addEventListener #bScenarioTaiwan → runTaiwanScenario, addEventListener #bScenarioReset → resetHighlight (and clear the summary/list/prov + reset the select). Escape every label via the existing escapeHtml import before innerHTML (T-07-03). Mirror the chokepoints bootstrap order exactly; append only — do not reorder existing wiring. Note in a comment that highlight only takes effect in global mode (mirrors chokepoints UX).</action>
  <verify>
    <automated>node --test tests/scenario-wiring.test.mjs</automated>
  </verify>
  <done>node --test tests/scenario-wiring.test.mjs passes (GREEN); js/ui imports runScenario/SCENARIO_PRESETS, renders the derived badge into #scenarioProv, highlights impacted symbols via highlightBy, and wires the Taiwan button + chokepoint select + reset; no headline number is hardcoded (derived from result).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived analytic → DOM | scenario summary/list/labels injected via innerHTML — must be escaped |
| derived number → investor | scenario output must badge "Derived", never "Observed"; copy says "exposed" not "lost" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering (XSS) | scenario impact list + chokepoint select innerHTML | mitigate | escapeHtml every supplier/company label before innerHTML (mirror renderChokepoints line 646); symbols/counts are numeric/known |
| T-07-04 | Information disclosure | scenario output mis-tagged Observed or "loss" copy | mitigate | reuse provenanceFor({derived:true}) → "Derived"; methodology + summary copy say "exposure"; scenario-wiring test asserts derived badge + copy |
| T-07-05 | Tampering | hardcoded 7/$11.36T headline drifting from real data | mitigate | UI derives from result.impactedCompanies.length + totalMarketCapExposed; scenario-wiring test forbids the literals |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (audit N/A) |
</threat_model>

<verification>
- `node --test tests/scenario-wiring.test.mjs` green
- index-ui-integrity test green (all prior IDs + inline bootstrap intact)
- grep confirms #scenarioPanel + 7 new IDs in index.html and runScenario import + highlightBy call in js/ui
- methodology modal contains single-hop + HHI + exposure copy
</verification>

<success_criteria>
- #scenarioPanel mirrors #chokepointsPanel; Taiwan button + chokepoint select + reset present and wired
- running Taiwan preset shows live "7 companies · $11.36T exposed" + impact list + graph highlight
- scenario output badged "Derived" (never Observed) with Methodology link
- methodology copy explains the honest scenario model; tests/scenario-wiring.test.mjs registered + green
</success_criteria>

<output>
Create `.planning/phases/07-scenario-stress-tests/07-02-SUMMARY.md` when done
</output>
