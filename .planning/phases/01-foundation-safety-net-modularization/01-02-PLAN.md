---
phase: 01-foundation-safety-net-modularization
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - index.html
  - styles/base.css
  - styles/layout.css
  - styles/components.css
  - styles/theme.css
  - js/main.js
  - js/state.js
  - js/data/index.js
  - js/viz/index.js
  - js/ui/index.js
  - js/trust/index.js
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "The rendered site is visually and behaviorally identical to the pre-extraction monolith"
    - "All CSS lives in styles/*.css linked from index.html in the original cascade order"
    - "All JS logic lives in js/* ES modules; index.html carries only references + a tiny inline bootstrap"
    - "All 7 inline-handler functions (incl. openCompanyProfile) resolve against window at click time"
    - "npm test passes UNCHANGED (0 fail) including index-ui-integrity.test.mjs"
  artifacts:
    - path: "styles/base.css"
      provides: "Reset, fonts (@import or <link> first), :root tokens, body/typography — extracted from index.html L11-317 leading rules"
    - path: "styles/layout.css"
      provides: "Grid/positioning, #loading, panels, responsive media queries"
    - path: "styles/components.css"
      provides: "Buttons, cards, modals, tooltips, badges, search popovers"
    - path: "styles/theme.css"
      provides: "Colors, depth/shadow tokens"
    - path: "js/main.js"
      provides: "Entry module: import order, init sequence (applyUrlState/render/maybeShowOnboarding/updateStatusIndicator/renderTop10List), and the window.* export shim for all 7 functions"
      contains: "window.openCompanyProfile"
    - path: "js/state.js"
      provides: "STATE object + syncUrlState/applyUrlState"
      contains: "STATE"
    - path: "js/data/index.js"
      provides: "Reads window.SUPPLY_MAP_DATA / window.CREDIT_RATINGS into DATA/CREDIT_RATINGS; normalization, overlap index, ratings helpers, escapeHtml"
    - path: "js/viz/index.js"
      provides: "D3 force sim, render(), tooltips, highlighting, motion"
    - path: "js/ui/index.js"
      provides: "Toolbar, filters, compare, modals, onboarding, search, bookmarks, top10 list, status indicator, company profile"
    - path: "js/trust/index.js"
      provides: "Thin placeholder stub for Phases 2-3 (exports nothing functional yet)"
    - path: "index.html"
      provides: "Semantic shell: head/meta, containers, <link> stylesheets, <script type=module src=js/main.js>, and a retained tiny inline <script>...</script> immediately before </body>"
      contains: "type=\"module\""
  key_links:
    - from: "index.html <head>"
      to: "styles/base.css, layout.css, components.css, theme.css"
      via: "<link rel=stylesheet> in original cascade order (base->layout->components->theme)"
      pattern: "styles/(base|layout|components|theme)\\.css"
    - from: "index.html before </body>"
      to: "js/main.js"
      via: "<script type=module src=...> plus a retained inline <script>...</script></body> bootstrap"
      pattern: "<script\\s+type=\"module\"[^>]*src=\"js/main\\.js\""
    - from: "js/main.js"
      to: "window.openCompanyProfile (and 6 siblings)"
      via: "Object.assign(window, {...}) export shim"
      pattern: "window\\.(openCompanyProfile|applyFilters|resetFilters|closeCompare|toggleHelp|loadView|deleteView)"
    - from: "js/data/index.js"
      to: "window.SUPPLY_MAP_DATA / window.CREDIT_RATINGS"
      via: "const DATA = window.SUPPLY_MAP_DATA (read, never mutate the data tags)"
      pattern: "window\\.SUPPLY_MAP_DATA"
---

<objective>
Extract the monolithic index.html into linked CSS files and ES modules with ZERO user-visible change, keeping `npm test` green UNCHANGED after every step. Three incremental, individually-verified extractions: (1) CSS, (2) JS into modules behind a single module entry, (3) reduce index.html to a semantic shell while installing the window.* export shim and retaining the inline-bootstrap the regression test requires.

Purpose: Build the safe, modular foundation every later phase depends on, proving non-regression at each step rather than at the end.
Output: `styles/*.css`, `js/*` ES modules, a slimmed semantic-shell `index.html`.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md
@.planning/phases/01-foundation-safety-net-modularization/01-VALIDATION.md
@.planning/phases/01-foundation-safety-net-modularization/01-01-SUMMARY.md

# VERIFIED STRUCTURE OF index.html (grep-confirmed, do not re-derive):
#   - <head> classic scripts: L8 D3 CDN, L9 data/top100-map.js (-> window.SUPPLY_MAP_DATA), L10 data/credit-ratings.js (-> window.CREDIT_RATINGS). NEVER convert these to modules; never touch the data files.
#   - ONE <style> block: L11-317 (~306 lines). L12 is a Google Fonts @import that MUST stay the first rule (or become a <head> <link>).
#   - </head> L318, <body> L319, body HTML markup L319-573 (contains static onclick= at L342,390,470,471).
#   - ONE inline <script>: L574-2474 (~1900 lines). 78 top-level function declarations.
#   - window.* exports today at L2380-2385: toggleHelp, closeCompare, applyFilters, resetFilters, loadView, deleteView (6 of 7).
#   - openCompanyProfile defined at L2466 and referenced via injected onclick at L2436 (top10 list) — it is the ONE function NOT on window today. MUST be added.
#   - Init sequence at script end: L2387-2389 (applyUrlState(); render(); maybeShowOnboarding();) then L2472-2473 (updateStatusIndicator(); renderTop10List();). Preserve this exact order in js/main.js.
#   - Data read at L632 (const DATA = window.SUPPLY_MAP_DATA) and L637 (const CREDIT_RATINGS = window.CREDIT_RATINGS || {...}). Reproduce verbatim in js/data.
#   - escapeHtml() (L920) guards injected labels — preserve its usage everywhere (security: do not drop during the move).

<interfaces>
<!-- Module responsibility map (locked by CONTEXT.md; internal sub-file split is Claude's discretion). -->
js/state.js   -> exports STATE, syncUrlState, applyUrlState
js/data/      -> reads window.SUPPLY_MAP_DATA/CREDIT_RATINGS; exports DATA, CREDIT_RATINGS, normalization + overlap + ratings helpers + escapeHtml
js/viz/       -> exports render, tooltip/highlight/motion helpers, the d3 svg/zoom setup
js/ui/        -> exports applyFilters, resetFilters, closeCompare, toggleHelp, loadView, deleteView, openCompanyProfile, plus onboarding/search/compare/modals/top10/status wiring
js/trust/     -> placeholder; export {} (populated Phases 2-3)
js/main.js    -> imports the above, runs the init sequence, installs window.* shim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CSS into styles/*.css (FOUND-01), keep cascade order, link in head</name>
  <files>styles/base.css, styles/layout.css, styles/components.css, styles/theme.css, index.html</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/index.html (the <style> block L11-317 and <head> L1-318)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Pitfall 4 @import-first, Pitfall 5 cascade order)
    - C:/Users/akgul/Downloads/Downloads/supplychain/tests/index-ui-integrity.test.mjs
  </read_first>
  <action>
    Move the entire contents of the single L11-317 <style> block into four files split by concern — base.css (reset, :root tokens, body/typography), layout.css (grid/positioning, #loading, panels, media queries), components.css (buttons, cards, modals, tooltips, badges, search popovers), theme.css (colors, depth/shadow tokens) — PRESERVING the original top-to-bottom rule order across the four files (base -> layout -> components -> theme) so the cascade is identical. Handle the L12 Google Fonts @import by EITHER keeping it as the very first statement of base.css OR converting it to a <link rel="stylesheet"> placed first in <head> (preferred, avoids the @import-ordering rule). Replace the inline <style> block in index.html with four <link rel="stylesheet"> tags in head in cascade order. Do not change any rule text, selector, or value — only relocate. Then re-run the gate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - The four styles/*.css files exist and together contain every rule from the original <style> block, in original order.
    - The Google Fonts @import is either first in base.css or a <head> <link> placed before the other stylesheet links.
    - index.html head contains <link rel="stylesheet"> for base, layout, components, theme (in that order) and NO inline <style> block remains.
    - `npm test` exits 0 with 0 failures (equal to the plan-01 anchor count).
    - Served via http-server, the page renders with identical fonts/colors/spacing to the pre-extraction baseline (spot-check against the plan-01 baseline screenshot/Lighthouse a11y score).
  </acceptance_criteria>
  <done>CSS fully externalized to styles/*.css, linked in original cascade order, fonts preserved, tests still green and unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract inline JS into js/* ES modules (FOUND-02) behind a single module entry</name>
  <files>js/main.js, js/state.js, js/data/index.js, js/viz/index.js, js/ui/index.js, js/trust/index.js</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/index.html (the inline <script> L574-2474 in full)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Architecture Patterns 1-3, Anti-Patterns, Window Globals Inventory)
  </read_first>
  <action>
    Move the ~1900 lines of inline JS (L574-2474) into ES modules by responsibility per the locked map: js/state.js (STATE object + syncUrlState/applyUrlState), js/data/index.js (reproduce `const DATA = window.SUPPLY_MAP_DATA` and the CREDIT_RATINGS read verbatim, plus normalization, buildSharedSupplierOverlapIndex, ratings helpers, and escapeHtml), js/viz/index.js (d3 svg/zoom setup, render, showTooltip/showLinkTooltip/moveTooltip, connected/highlightNode and motion), js/ui/index.js (modals open/close + focus trap, onboarding, search suggestions/history, filters applyFilters/resetFilters, compare closeCompare, bookmarks loadView/deleteView, top10 renderTop10List, updateStatusIndicator, openProfile/openCompanyProfile, mobile sheet), and js/trust/index.js (empty placeholder exporting nothing functional, with a comment marking it for Phases 2-3). Wire explicit import/export across modules — do NOT rely on the old single-scope function hoisting. Keep the classic head data-script tags and the data files completely untouched. Create js/main.js as the entry that imports the modules. Do NOT yet remove the inline <script> from index.html in this task — instead, temporarily point a <script type="module" src="js/main.js"> alongside, OR validate modules load by serving via http-server; the index.html reduction + shim happens in Task 3. Run the gate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - js/state.js exports STATE, syncUrlState, applyUrlState.
    - js/data/index.js reads window.SUPPLY_MAP_DATA and window.CREDIT_RATINGS (grep `window.SUPPLY_MAP_DATA` present) and still defines escapeHtml.
    - js/viz/index.js exports render and the tooltip/highlight helpers; js/ui/index.js exports the 7 inline-handler functions; js/trust/index.js exists as a stub.
    - No function relies on cross-module hoisting — every cross-file reference uses import/export.
    - The data/top100-map.js and data/credit-ratings.js files and their <head> tags are byte-unchanged.
    - `npm test` exits 0, 0 failures, equal to the anchor count.
  </acceptance_criteria>
  <done>All inline JS lives in js/* ES modules with explicit imports/exports; data contract untouched; tests still green and unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reduce index.html to a semantic shell, install window.* shim + retained inline bootstrap (FOUND-02/03)</name>
  <files>index.html, js/main.js</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/index.html (current state after tasks 1-2)
    - C:/Users/akgul/Downloads/Downloads/supplychain/tests/index-ui-integrity.test.mjs (the regex + node --check + ID assertions)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Pitfall 1, Pitfall 3, Pattern 1, Pattern 3)
  </read_first>
  <action>
    Remove the original inline <script> body from index.html. Add `<script type="module" src="js/main.js"></script>` before </body>. In js/main.js, after imports, install the export shim via Object.assign(window, { applyFilters, resetFilters, closeCompare, toggleHelp, loadView, deleteView, openCompanyProfile }) — explicitly including openCompanyProfile, which was NOT on window in the monolith. Reproduce the exact init order in main.js: applyUrlState(); render(); maybeShowOnboarding(); updateStatusIndicator(); renderTop10List(); plus the resize listener. CRITICAL for the regression test: immediately before </body> retain a tiny NON-module inline `<script>...</script>` block whose body is valid standalone JS (a comment or trivial statement) so the `/<script>([\s\S]*?)<\/script>\s*<\/body>/i` regex matches and `node --check` passes. Keep the `<script src="./data/credit-ratings.js">` tag and ALL container IDs the test asserts (helpModal/compareModal with role=dialog aria-modal=true, fatalError, onboardingPanel, cardRatings, cardOverlap, cardTimeline, provenanceDrawer, searchSuggest, mobileToggle, mobileSheet). Run the gate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<script type="module" ... src="js/main.js">` and a retained tiny inline `<script>...</script>` immediately before `</body>` (grep both).
    - js/main.js installs all 7 window.* assignments including `window.openCompanyProfile` (grep `window.openCompanyProfile` returns a hit).
    - js/main.js init order matches the monolith: applyUrlState -> render -> maybeShowOnboarding -> updateStatusIndicator -> renderTop10List.
    - The credit-ratings.js script tag and all asserted container IDs remain present in index.html.
    - `npm test` exits 0, 0 failures — index-ui-integrity.test.mjs passes UNCHANGED (test file not edited).
    - index.html no longer contains the original ~1900-line inline logic block (only references + tiny bootstrap).
  </acceptance_criteria>
  <done>index.html is a semantic shell linking modules; all 7 handlers (incl. openCompanyProfile) are on window; the inline-bootstrap keeps index-ui-integrity green unchanged; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected data labels -> DOM (innerHTML) | Company/supplier/profile labels from data/*.js are interpolated into HTML strings; untrusted-shaped content crosses into the DOM |
| CDN (D3) -> page | Third-party script loaded over CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DOM XSS via injected company/profile labels (escapeHtml usage) | mitigate | Preserve every existing `escapeHtml()` call when moving code into js/data + js/ui; do not drop or weaken it during extraction (V5) |
| T-01-02 | Tampering | Pinned CDN D3 v7.8.5 | accept | Version stays pinned; SRI not currently used and adding it is out of scope this phase |
| T-01-SC | Tampering | npm install (http-server, playwright) | accept | No NEW production packages added; both are pre-declared in package.json and audited in RESEARCH.md Package Legitimacy Audit (established, Google/Microsoft/http-party sources). No [ASSUMED]/[SUS] packages introduced. |
</threat_model>

<verification>
- `npm test` green and UNCHANGED (0 fail) after EACH task, equal to the plan-01 anchor.
- index.html contains zero inline CSS and only a tiny inline bootstrap + a module script reference.
- All 7 inline-handler functions resolve on window (openCompanyProfile explicitly added).
- Data tags/files and the data contract are byte-unchanged.
- Served over http-server (not file://), the page renders and behaves identically to the baseline.
</verification>

<success_criteria>
FOUND-01 (CSS externalized, no visual change), FOUND-02 (JS in ES modules, index.html a semantic shell, all globals incl. openCompanyProfile exposed), and FOUND-03 (full suite passes unchanged) all satisfied, proven incrementally with `npm test` after every extraction step.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-safety-net-modularization/01-02-SUMMARY.md` when done. Record: final file/module layout, where each major function group landed, confirmation that openCompanyProfile is on window, and the unchanged `npm test` count.
</output>
