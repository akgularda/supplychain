---
phase: 05-hero-moment-investor-narrative
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - js/ui/narrative.js
  - tests/narrative.test.mjs
autonomous: true
requirements: [STORY-02, STORY-05]

must_haves:
  truths:
    - "createHeroController autoplays from step 0 and auto-advances on injected timers (~5.5s/step) when not reduced-motion"
    - "pause() stops the timer; next()/prev() move manually and pause autoplay"
    - "skip()/stop() writes heroSeen=1 via injected storage, calls controls.resetHighlight, and clears the overlay (render(null))"
    - "reduced-motion (injected reducedMotion()===true) suppresses the auto-advance timer; apply() and caption still run; manual Next still works"
    - "replay: play() runs regardless of stored heroSeen value"
  artifacts:
    - path: "js/ui/narrative.js"
      provides: "createHeroController factory with all side-effects injected (steps, controls, storage, reducedMotion, timers, render)"
      exports: ["buildNarrative", "createHeroController"]
      contains: "export function createHeroController"
    - path: "tests/narrative.test.mjs"
      provides: "Controller behavior assertions driven by fake timers/storage/reducedMotion/controls"
      min_lines: 80
  key_links:
    - from: "createHeroController.scheduleNext"
      to: "injected timers.setTimeout"
      via: "auto-advance gated on !reducedMotion()"
      pattern: "timers\\.setTimeout"
    - from: "createHeroController.stop"
      to: "injected storage.write + controls.resetHighlight"
      via: "skip/stop teardown"
      pattern: "storage\\.write.*heroSeen"
---

<objective>
Add the `createHeroController` factory to `js/ui/narrative.js` — the autoplay/stepper state machine that powers both the first-visit hero and the manual tour, with EVERY side-effect injected so it tests DOM-free (RESEARCH Pattern 2).

Signature: `createHeroController({ steps, controls, storage, reducedMotion, timers, render })` returning `{ play, pause, next, prev, skip, getIndex }`. Behaviors (RESEARCH "Autoplay / First-Visit / Reduced-Motion Mechanics"): autoplay advances every ~5.5s via injected `timers.setTimeout` (4 steps within the ~30s STORY-02 target); `pause` stops the timer; `next`/`prev` move manually and pause; `skip`/`stop` write `heroSeen=1` via injected `storage.write`, call `controls.resetHighlight`, and clear the overlay via `render(null)` (RESEARCH Pitfall 4 — never leave the map dimmed); `reducedMotion()===true` suppresses the auto-advance timer while still showing captions and running `apply()` (viz already gates its own transitions to 0 — STATE 04-02); `play()` works regardless of stored `heroSeen` (replay).

Purpose: Deliver the testable hero engine (STORY-02) without touching the DOM — main.js (Plan 03) injects the real deps.
Output: extended `js/ui/narrative.js`, expanded `tests/narrative.test.mjs` (controller assertions go GREEN).
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md
@.planning/phases/05-hero-moment-investor-narrative/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01, verified contract -->
js/ui/narrative.js already exports buildNarrative(data) -> 4 steps, each { id, title, caption, apply(controls) }.

Injected deps shape for createHeroController:
  steps        : array of { id, title, caption, apply }
  controls     : { openGlobal, openProfile, highlightBy, resetHighlight }
  storage      : { read(key), write(key,value) }   // Plan 03 -> safeReadFlag/safeWriteFlag
  reducedMotion: () => boolean                       // Plan 03 -> matchMedia('(prefers-reduced-motion: reduce)').matches
  timers       : { setTimeout, clearTimeout }        // Plan 03 -> window.setTimeout/clearTimeout
  render       : (step|null, index, total) => void   // Plan 03 -> writes #heroTitle/#heroCaption/#heroProgress, toggles #heroOverlay
HERO storage key: "heroSeen" (distinct from "onboardingSeen")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add controller behavior tests to tests/narrative.test.mjs</name>
  <files>tests/narrative.test.mjs</files>
  <behavior>
    - autoplay: play() with reducedMotion=()=>false schedules a timer; firing it advances getIndex() 0->1; after the last step the controller stops (no further timer) and writes heroSeen
    - pause: pause() after play() clears the timer (no further auto-advance when fake timer is fired)
    - next/prev: next() advances and pauses auto; prev() decrements; bounds respected (no negative index, last next() triggers stop)
    - skip/stop: skip() calls storage.write('heroSeen','1'), controls.resetHighlight, and render(null)
    - reduced-motion: play() with reducedMotion=()=>true schedules NO timer (timers.setTimeout never called); render still invoked with step 0; manual next() still advances
    - replay: play() runs even when storage.read('heroSeen')==='1'
  </behavior>
  <read_first>
    - tests/narrative.test.mjs (Plan 01 spec — extend, do not rewrite)
    - .planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md (Pattern 2 + mechanics 1-5)
  </read_first>
  <action>
    Extend tests/narrative.test.mjs with a controller block. Build fake deps: a fake timers object capturing the scheduled callback (setTimeout returns an id, records the fn; a manual fireTimer() helper invokes the last recorded fn; clearTimeout marks cleared); a fake storage with an in-memory map exposing read/write and a writes log; spy controls recording calls; a render spy recording (step,index,total) tuples; reducedMotion as a settable fake. Use the steps from buildNarrative(fixture) from Plan 01.

    Assert each behavior above. For reduced-motion: assert timers.setTimeout call count is 0 after play() and that render received step index 0, then call next() and assert index advanced and render received index 1. For skip: assert storage writes contain ['heroSeen','1'], controls.resetHighlight called once, and render last call argument is null. For end-of-autoplay: fire the timer steps.length-1 times and assert the final state wrote heroSeen and stopped scheduling.
  </action>
  <verify>
    <automated>node --check tests/narrative.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Controller tests cover autoplay advance, pause, next/prev bounds, skip teardown, reduced-motion no-timer, replay
    - Tests use injected fakes only (no DOM, no real timers)
    - File passes node --check
  </acceptance_criteria>
  <done>Controller behavior fully specified with deterministic fake deps; ready to drive implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement createHeroController in js/ui/narrative.js</name>
  <files>js/ui/narrative.js</files>
  <behavior>
    - play(): index=0, playing=true, show() (apply+render current), scheduleNext()
    - scheduleNext(): if reducedMotion() return (no timer); else timers.setTimeout advances or stops at end after STEP_MS (~5500)
    - pause(): playing=false, clearTimeout
    - next()/prev(): pause first; clamp at bounds; last next() -> stop()
    - stop()/skip(): pause; storage.write('heroSeen','1'); controls.resetHighlight?.(); render(null)
    - getIndex(): current index
  </behavior>
  <read_first>
    - js/ui/narrative.js (Plan 01 buildNarrative — append the factory in the same module)
    - .planning/phases/05-hero-moment-investor-narrative/05-RESEARCH.md (Pattern 2 reference implementation)
  </read_first>
  <action>
    Append `export function createHeroController({ steps, controls, storage, reducedMotion, timers, render })` to js/ui/narrative.js, keeping the module DOM-free (no document/window at top level). Implement local index/playing/timerId state and a STEP_MS constant ~5500 (Claude's discretion per CONTEXT; 4*5.5s + reveal stays under ~30s). show() calls steps[index].apply(controls) then render(steps[index], index, steps.length). scheduleNext() returns early when reducedMotion() is truthy (RESEARCH mechanic 5); otherwise schedules via timers.setTimeout to advance and re-schedule, or stop() at the last step. pause() clears via timers.clearTimeout. next()/prev() pause then move within bounds (last next() calls stop()). stop()/skip() pause, write heroSeen=1 via storage.write, call controls.resetHighlight optionally, and render(null) to hide the overlay (RESEARCH Pitfall 4). Return { play, pause, next, prev, skip, getIndex }.
  </action>
  <verify>
    <automated>node --test tests/narrative.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All controller tests in tests/narrative.test.mjs pass
    - reducedMotion()===true path schedules zero timers
    - skip/stop writes heroSeen, resets highlight, and renders null
    - createHeroController exported alongside buildNarrative; module still imports no DOM
  </acceptance_criteria>
  <done>Hero controller green under fake deps; autoplay/skip/replay/reduced-motion all proven DOM-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage <-> controller | heroSeen flag read/written via injected storage; only gates autoplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | localStorage heroSeen flag | accept | Harmless — only controls whether the tour auto-plays; no trust/security decision rides on it (RESEARCH Security Domain). |
| T-05-04 | Denial of Service | runaway autoplay timer | mitigate | Single timerId; pause/stop/skip all clearTimeout; bounded by steps.length then stop(). |
</threat_model>

<verification>
- `node --test tests/narrative.test.mjs` fully green (buildNarrative + controller).
- Controller is importable and drivable in Node with fake timers/storage (no DOM).
</verification>

<success_criteria>
- createHeroController exported from js/ui/narrative.js with injected side-effects.
- Autoplay, pause, next/prev, skip teardown, reduced-motion suppression, and replay all tested green.
- Module remains DOM-free and import-clean.
</success_criteria>

<output>
Create `.planning/phases/05-hero-moment-investor-narrative/05-02-SUMMARY.md` when done
</output>
