// Phase 03: placeholder — real assertions added in plan 03-03.
// Registered in package.json scripts.test up front (Wave-0 gate, plan 03-01).
import { test } from "node:test";
import assert from "node:assert/strict";

test("placeholder — implemented in plan 03-03", () => assert.ok(true));
