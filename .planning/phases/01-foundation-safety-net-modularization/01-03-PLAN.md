---
phase: 01-foundation-safety-net-modularization
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - .github/workflows/deploy-pages.yml
  - docs/perf/baseline-2026-06-20.md
autonomous: false
requirements: [FOUND-05, FOUND-04]

must_haves:
  truths:
    - "The GitHub-Pages deploy workflow copies styles/ and js/ into _site/ so the live site is styled and functional"
    - "The extracted site renders and behaves identically to the pre-extraction monolith (no user-visible regression)"
    - "The post-extraction Lighthouse run is equal-or-better than the pre-extraction baseline"
  artifacts:
    - path: ".github/workflows/deploy-pages.yml"
      provides: "Build step that copies styles/ AND js/ into _site/ (guarded with directory checks), in addition to the existing files"
      contains: "styles"
    - path: "docs/perf/baseline-2026-06-20.md"
      provides: "Pre- AND post-extraction Lighthouse comparison proving no regression (FOUND-04 closed)"
      contains: "post-extraction"
  key_links:
    - from: ".github/workflows/deploy-pages.yml build step"
      to: "_site/styles and _site/js"
      via: "cp -R styles _site/ and cp -R js _site/ (guarded with if [ -d ... ])"
      pattern: "cp -R (styles|js) _site"
    - from: "docs/perf/baseline-2026-06-20.md"
      to: "post-extraction lighthouse run"
      via: "second lighthouse run vs the same served index.html, compared to pre-extraction scores"
      pattern: "post-extraction"
---

<objective>
Close the deploy landmine and prove non-regression. Update `.github/workflows/deploy-pages.yml` to copy the new `styles/` and `js/` directories into `_site/` (without this the live GitHub-Pages site renders blank/unstyled despite green local tests — FOUND-05), then capture a post-extraction Lighthouse run and confirm it is equal-or-better than the pre-extraction baseline, and run the render/behavior parity click-through of all 7 window.* functions.

Purpose: Guarantee the refactor is invisible to end users AND to the deployed site — the safety net is only real if the live deploy still works.
Output: Updated `deploy-pages.yml`; `docs/perf/baseline-2026-06-20.md` extended with a post-extraction comparison; signed-off render parity.
</objective>

<execution_context>
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/workflows/execute-plan.md
@C:/Users/akgul/.claude/plugins/cache/gsd-plugin/gsd/3.5.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md
@.planning/phases/01-foundation-safety-net-modularization/01-VALIDATION.md
@.planning/phases/01-foundation-safety-net-modularization/01-01-SUMMARY.md
@.planning/phases/01-foundation-safety-net-modularization/01-02-SUMMARY.md

# CURRENT deploy-pages.yml build step copies ONLY: index.html, favicon.svg, logo.png, data/, assets/, CNAME, then `touch _site/.nojekyll`.
# It does NOT copy styles/ or js/ — these now exist (from plan 02) and MUST be added or the live site breaks (Pitfall 2 / Runtime State Inventory).
# Manual parity check MUST be done over http-server (http://), NEVER file:// (Pitfall 6 / CORS for modules).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deploy-pages.yml to copy styles/ and js/ into _site/ (FOUND-05)</name>
  <files>.github/workflows/deploy-pages.yml</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/.github/workflows/deploy-pages.yml (the "Build static site artifact" run block)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Pitfall 2; Runtime State Inventory deploy row)
  </read_first>
  <action>
    In the "Build static site artifact" step, add two guarded copy lines alongside the existing ones: `if [ -d styles ]; then cp -R styles _site/; fi` and `if [ -d js ]; then cp -R js _site/; fi`, placed before the `touch _site/.nojekyll` line and styled consistently with the existing guarded copies. Do not change any other part of the workflow (triggers, permissions, concurrency, upload/deploy steps stay identical). Confirm the resulting copy list now includes index.html, favicon, logo, data/, assets/, CNAME, styles/, js/, and .nojekyll.
  </action>
  <verify>
    <automated>grep -Eq "cp -R styles _site" .github/workflows/deploy-pages.yml && grep -Eq "cp -R js _site" .github/workflows/deploy-pages.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - deploy-pages.yml contains a guarded `cp -R styles _site/` AND a guarded `cp -R js _site/` line.
    - Both are inside the existing "Build static site artifact" run block, before `touch _site/.nojekyll`.
    - No other workflow content changed (triggers/permissions/concurrency/upload/deploy intact).
    - YAML remains valid (consistent indentation with the surrounding `run:` block).
  </acceptance_criteria>
  <done>The deploy workflow now packages styles/ and js/ into _site/, so the live GitHub-Pages site will serve the extracted CSS/JS.</done>
</task>

<task type="auto">
  <name>Task 2: Capture post-extraction Lighthouse and append the no-regression comparison (FOUND-04)</name>
  <files>docs/perf/baseline-2026-06-20.md</files>
  <read_first>
    - C:/Users/akgul/Downloads/Downloads/supplychain/docs/perf/baseline-2026-06-20.md (the pre-extraction baseline from plan 01)
    - C:/Users/akgul/Downloads/Downloads/supplychain/.planning/phases/01-foundation-safety-net-modularization/01-RESEARCH.md (Lighthouse capture command; same method as plan 01 for comparability)
  </read_first>
  <action>
    Serve the now-extracted site with `npx http-server . -p 8080 -c-1` and run Lighthouse against http://localhost:8080/index.html with the SAME categories and flags used in plan 01 (use the identical capture method so the comparison is fair). Append a "Post-extraction" section to docs/perf/baseline-2026-06-20.md with the four category scores and FCP/LCP/TBT/CLS, and a short delta table vs the pre-extraction numbers, plus an explicit statement that scores are equal-or-better (or, if any metric regressed beyond noise, flag it for investigation before the phase gate). Do not modify index.html/styles/js in this task.
  </action>
  <verify>
    <automated>grep -qi "post-extraction" docs/perf/baseline-2026-06-20.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - baseline-2026-06-20.md now has a clearly labeled post-extraction section with the four scores + web vitals and a delta vs pre-extraction.
    - The same capture method/flags as plan 01 were used (comparability noted).
    - The file states scores are equal-or-better, OR flags a specific regression for investigation.
    - No source file changed in this task.
  </acceptance_criteria>
  <done>FOUND-04 fully closed: a single tracked file holds both the pre- and post-extraction Lighthouse numbers and proves no performance/accessibility regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The fully modularized site (styles/*.css + js/* ES modules), an updated deploy workflow that copies styles/ and js/, and a post-extraction Lighthouse comparison.</what-built>
  <how-to-verify>
    1. Serve over http: run `npx http-server . -p 8080 -c-1` and open http://localhost:8080/index.html (NOT file:// — modules need http).
    2. Confirm the page looks visually identical to before (fonts, colors, layout, map render).
    3. Exercise all 7 inline-handler functions and confirm each works with NO console error:
       - Click a company node / a Top-10 list item -> profile opens (this exercises openCompanyProfile, the highest-risk one).
       - Apply filters, then Reset filters.
       - Open compare, then Close compare.
       - Open Help, then close it (toggleHelp).
       - Save a view, Load it, Delete it (loadView/deleteView).
    4. Open DevTools Network tab and confirm styles/*.css and js/*.js load with 200 (no 404s).
    5. Confirm docs/perf/baseline-2026-06-20.md shows post-extraction scores equal-or-better than pre-extraction.
    6. Confirm deploy-pages.yml copies styles/ and js/.
  </how-to-verify>
  <resume-signal>Type "approved" if the site is behaviorally identical with no console errors and the deploy/baseline are correct; otherwise describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> GitHub-Pages _site artifact | CI build step decides which files reach the live deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service (broken deploy) | deploy-pages.yml copy list omitting styles/ + js/ | mitigate | Add guarded `cp -R styles _site/` and `cp -R js _site/`; verify built artifact / Pages network tab has no 404s for CSS/JS |
| T-01-04 | Information disclosure | none new (public read-only static site) | accept | No new data inputs, auth, or secrets introduced by this phase |
</threat_model>

<verification>
- deploy-pages.yml copies styles/ and js/ (grep confirms both lines).
- Post-extraction Lighthouse recorded and equal-or-better than pre-extraction in docs/perf/baseline-2026-06-20.md.
- Manual render+behavior parity over http-server: all 7 window.* functions work, no 404s for styles/js, visually identical.
- `npm test` still green (carried from plan 02; re-run as the phase gate).
</verification>

<success_criteria>
FOUND-05 satisfied (deploy packages the new dirs; rendered/behavioral output equivalent; static buildless deploy preserved) and FOUND-04 fully closed (pre+post Lighthouse comparison shows no regression). The phase gate is met: npm test green, baseline committed, all 7 handlers verified, deploy workflow updated.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-safety-net-modularization/01-03-SUMMARY.md` when done. Record: the exact lines added to deploy-pages.yml, the pre/post Lighthouse delta, and the result of the 7-function click-through parity check.
</output>
